# Disable JWT verification at gateway level
# The function handles authentication internally for each action
# - test_key: no user data access, just validates external API keys
# - All other actions: require valid auth via getUserId()
verify_jwt = false
