---
phase: 08-configurable-study-sessions
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/android/lib/studySettings.ts
  - apps/android/contexts/StudySettingsContext.tsx
  - apps/android/hooks/useStudySettings.ts
  - apps/android/App.tsx
  - apps/android/screens/SettingsScreen.tsx
autonomous: true

must_haves:
  truths:
    - "User can select cards-per-session (10/20/50/All) in Settings"
    - "Selected preference persists across app restarts"
    - "Default value is 'all' (backward compatible)"
  artifacts:
    - path: "apps/android/lib/studySettings.ts"
      provides: "AsyncStorage load/save for CardsPerSession"
      exports: ["loadCardsPerSession", "saveCardsPerSession", "CardsPerSession"]
    - path: "apps/android/contexts/StudySettingsContext.tsx"
      provides: "React Context provider and hook for study settings"
      exports: ["StudySettingsProvider", "useStudySettings"]
    - path: "apps/android/hooks/useStudySettings.ts"
      provides: "Convenience re-export of useStudySettings"
      exports: ["useStudySettings"]
    - path: "apps/android/screens/SettingsScreen.tsx"
      provides: "Study section with radio-button presets"
      contains: "cardsPerSession"
  key_links:
    - from: "apps/android/contexts/StudySettingsContext.tsx"
      to: "apps/android/lib/studySettings.ts"
      via: "import loadCardsPerSession, saveCardsPerSession"
      pattern: "import.*from.*lib/studySettings"
    - from: "apps/android/screens/SettingsScreen.tsx"
      to: "apps/android/hooks/useStudySettings.ts"
      via: "useStudySettings hook"
      pattern: "useStudySettings"
    - from: "apps/android/App.tsx"
      to: "apps/android/contexts/StudySettingsContext.tsx"
      via: "StudySettingsProvider wrapping NavigationContainer"
      pattern: "StudySettingsProvider"
---

<objective>
Create the cards-per-session persistence layer, React Context provider, and Settings UI for configuring study session length.

Purpose: Implements STUDY-01 -- users can select how many cards to study per session (10/20/50/All) with the preference persisting across app restarts.
Output: Working "Study" section in SettingsScreen with radio buttons, backed by AsyncStorage persistence via React Context.
</objective>

<execution_context>
@/home/toto/.claude/get-shit-done/workflows/execute-plan.md
@/home/toto/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Reference patterns (read these to match existing code style exactly)
@apps/android/lib/theme.ts
@apps/android/contexts/ThemeContext.tsx
@apps/android/hooks/useTheme.ts
@apps/android/screens/SettingsScreen.tsx
@apps/android/App.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create persistence layer and context provider</name>
  <files>
    apps/android/lib/studySettings.ts
    apps/android/contexts/StudySettingsContext.tsx
    apps/android/hooks/useStudySettings.ts
  </files>
  <action>
Create three files mirroring the existing theme persistence pattern exactly:

**1. `apps/android/lib/studySettings.ts`** (mirror `lib/theme.ts`):
- Storage key: `'@lumio/cards-per-session'`
- Export type `CardsPerSession = 10 | 20 | 50 | 'all'`
- Export `loadCardsPerSession(): Promise<CardsPerSession>` -- reads from AsyncStorage, validates stored value is one of '10'/'20'/'50'/'all', returns `'all'` as default on missing/invalid/error
- Export `saveCardsPerSession(value: CardsPerSession): Promise<void>` -- writes `String(value)` to AsyncStorage
- Use `import AsyncStorage from '@react-native-async-storage/async-storage'` (already installed v2.2.0)

**2. `apps/android/contexts/StudySettingsContext.tsx`** (mirror `contexts/ThemeContext.tsx`):
- Interface `StudySettingsContextType { cardsPerSession: CardsPerSession; setCardsPerSession: (value: CardsPerSession) => void; }`
- Create context with `createContext<StudySettingsContextType | null>(null)`
- `StudySettingsProvider` component: useState initialized to `'all'`, useEffect on mount calls `loadCardsPerSession().then(setState)`, useCallback `setCardsPerSession` that calls both setState and `saveCardsPerSession`
- `useStudySettings()` hook: reads context, throws `'useStudySettings must be used within StudySettingsProvider'` if null
- Export: `StudySettingsProvider`, `useStudySettings`

**3. `apps/android/hooks/useStudySettings.ts`** (mirror `hooks/useTheme.ts`):
- Single line re-export: `export { useStudySettings } from '../contexts/StudySettingsContext';`
  </action>
  <verify>
Run TypeScript check:
```bash
cd apps/android && npx tsc --noEmit 2>&1 | head -30
```
Verify no type errors in the new files.
  </verify>
  <done>
Three files exist with correct exports. `CardsPerSession` type is `10 | 20 | 50 | 'all'`. Load defaults to `'all'`. Save writes to AsyncStorage. Context provides cardsPerSession and setCardsPerSession. Hook re-export exists.
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire provider into App and add Settings UI</name>
  <files>
    apps/android/App.tsx
    apps/android/screens/SettingsScreen.tsx
  </files>
  <action>
**1. `apps/android/App.tsx`:**
- Import `StudySettingsProvider` from `'./contexts/StudySettingsContext'`
- Add `<StudySettingsProvider>` inside `<ThemeProvider>`, wrapping `<NavigationContainer>` and its children
- Result hierarchy: `ThemeProvider > StudySettingsProvider > NavigationContainer > ...`

**2. `apps/android/screens/SettingsScreen.tsx`:**
- Import `useStudySettings` from `'../hooks/useStudySettings'`
- Import `CardsPerSession` type from `'../lib/studySettings'`
- Define `studyOptions` array (mirror `themeOptions` pattern exactly):
  ```typescript
  type StudyOption = {
    value: CardsPerSession;
    label: string;
    icon: React.ComponentProps<typeof Ionicons>['name'];
  };

  const studyOptions: StudyOption[] = [
    { value: 10, label: '10 cards', icon: 'flash-outline' },
    { value: 20, label: '20 cards', icon: 'layers-outline' },
    { value: 50, label: '50 cards', icon: 'library-outline' },
    { value: 'all', label: 'All cards', icon: 'infinite-outline' },
  ];
  ```
- In the component, call `const { cardsPerSession, setCardsPerSession } = useStudySettings();`
- Add a "Study" section AFTER the Appearance section and BEFORE the spacer, using the exact same rendering pattern as the Appearance section:
  - Section header `<Text>` with "Study" text using `styles.sectionHeader`
  - `<View>` with `styles.section` containing mapped `studyOptions` with `TouchableOpacity` rows
  - Each row: icon + label on left, checkmark on right when `cardsPerSession === option.value`
  - Separator line between options using `styles.optionBorder` (same as theme options)
  - `onPress={() => setCardsPerSession(option.value)}`
  - Reuse ALL existing styles (optionRow, optionBorder, optionLeft, optionIcon, optionLabel) -- no new styles needed
  </action>
  <verify>
Run TypeScript check:
```bash
cd apps/android && npx tsc --noEmit 2>&1 | head -30
```
Verify SettingsScreen renders the Study section with 4 radio options. Verify App.tsx has StudySettingsProvider in the provider hierarchy.
  </verify>
  <done>
SettingsScreen shows "Study" section with 4 radio-button options (10/20/50/All). Selecting an option saves to AsyncStorage via context. App.tsx wraps NavigationContainer with StudySettingsProvider. Default shows "All cards" checked.
  </done>
</task>

</tasks>

<verification>
1. TypeScript compiles without errors: `cd apps/android && npx tsc --noEmit`
2. `lib/studySettings.ts` exports `CardsPerSession`, `loadCardsPerSession`, `saveCardsPerSession`
3. `contexts/StudySettingsContext.tsx` exports `StudySettingsProvider`, `useStudySettings`
4. `hooks/useStudySettings.ts` re-exports `useStudySettings`
5. `App.tsx` has `StudySettingsProvider` in provider hierarchy
6. `SettingsScreen.tsx` has "Study" section with 4 radio options using same style pattern as Appearance
</verification>

<success_criteria>
- STUDY-01 is fully implemented: user can select 10/20/50/All in Settings
- Preference persists via AsyncStorage (survives app restart)
- Default value is 'all' (backward compatible with existing behavior)
- No new dependencies added
- Code style matches existing theme preference pattern exactly
</success_criteria>

<output>
After completion, create `.planning/phases/08-configurable-study-sessions/08-01-SUMMARY.md`
</output>
