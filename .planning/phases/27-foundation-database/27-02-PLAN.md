---
phase: 27-foundation-database
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260227000001_email_auth_trigger.sql
autonomous: true
requirements:
  - INFRA-01

must_haves:
  truths:
    - "A user signing up with email/password gets a non-null display_name in public.users derived from the email prefix"
    - "A user signing up with email/password gets an avatar_url pointing to ui-avatars.com with Lumio brand color"
    - "A user signing up with Google OAuth still gets their Google display_name and avatar_url (no regression)"
    - "Email prefix separators (dots, underscores, hyphens) are converted to spaces with title case"
  artifacts:
    - path: "supabase/migrations/20260227000001_email_auth_trigger.sql"
      provides: "Provider-aware handle_new_user trigger function"
      contains: "CREATE OR REPLACE FUNCTION public.handle_new_user"
  key_links:
    - from: "supabase/migrations/20260227000001_email_auth_trigger.sql"
      to: "public.users"
      via: "INSERT INTO public.users in trigger function"
      pattern: "INSERT INTO public.users"
    - from: "supabase/migrations/20260227000001_email_auth_trigger.sql"
      to: "auth.users"
      via: "trigger on INSERT to auth.users (existing on_auth_user_created trigger)"
      pattern: "handle_new_user"
---

<objective>
Update the database trigger to handle email signups alongside Google OAuth.

Purpose: The existing `handle_new_user` trigger assumes Google OAuth metadata (full_name, avatar_url). Email signups have no such metadata, causing NULL display_name and avatar_url. This migration replaces the trigger function with provider-aware logic that derives display_name from the email prefix and generates an avatar_url using ui-avatars.com.

Output: A new SQL migration that replaces the `handle_new_user` function with provider-aware logic.
</objective>

<execution_context>
@/home/toto/.claude/get-shit-done/workflows/execute-plan.md
@/home/toto/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-foundation-database/27-CONTEXT.md
@.planning/phases/27-foundation-database/27-RESEARCH.md
@supabase/migrations/20241230000003_auth_trigger.sql

<interfaces>
<!-- Existing trigger and table structure the executor needs to understand -->

From supabase/migrations/20241230000003_auth_trigger.sql:
```sql
-- Current function (to be replaced with CREATE OR REPLACE):
CREATE OR REPLACE FUNCTION public.handle_new_user()
RETURNS TRIGGER AS $$
BEGIN
    INSERT INTO public.users (id, email, display_name, avatar_url)
    VALUES (
        NEW.id,
        NEW.email,
        COALESCE(NEW.raw_user_meta_data->>'full_name', NEW.raw_user_meta_data->>'name'),
        NEW.raw_user_meta_data->>'avatar_url'
    );
    RETURN NEW;
END;
$$ LANGUAGE plpgsql SECURITY DEFINER;

-- Trigger (DO NOT recreate -- already exists, just references the function name):
CREATE TRIGGER on_auth_user_created
    AFTER INSERT ON auth.users
    FOR EACH ROW
    EXECUTE FUNCTION public.handle_new_user();
```

public.users table columns used by trigger: id (UUID), email (TEXT), display_name (TEXT), avatar_url (TEXT)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create provider-aware handle_new_user migration</name>
  <files>supabase/migrations/20260227000001_email_auth_trigger.sql</files>
  <action>
Create a new SQL migration file `supabase/migrations/20260227000001_email_auth_trigger.sql` that replaces the `handle_new_user` function with provider-aware logic.

**Use `CREATE OR REPLACE FUNCTION` -- do NOT drop/recreate the trigger.** The existing `on_auth_user_created` trigger already references this function name and does not need to be recreated.

**Provider detection:** Use `NEW.raw_app_meta_data->>'provider'` to detect the auth provider. For Google signups it's `'google'`, for email signups it's `'email'`. Use COALESCE with default `'email'` for safety.

**Google OAuth path (provider = 'google'):**
- `display_name` = `COALESCE(NEW.raw_user_meta_data->>'full_name', NEW.raw_user_meta_data->>'name')` (same as current behavior)
- `avatar_url` = `NEW.raw_user_meta_data->>'avatar_url'` (same as current behavior)

**Email signup path (provider != 'google'):**
- Extract email prefix: `split_part(COALESCE(NEW.email, ''), '@', 1)`
- Replace `.`, `_`, `-` with spaces using nested `replace()` calls
- Apply `initcap()` for title case (per user decision: "use as-is for edge cases" -- no special handling needed)
- `avatar_url` = `'https://ui-avatars.com/api/?name=' || replace(_display_name, ' ', '+') || '&background=7C3AED&color=fff&size=128&bold=true'`
  - `7C3AED` = Lumio purple (fixed brand color, not per-user unique)
  - `fff` = white text
  - `128` = pixel size
  - `bold=true` for better visibility

**Key implementation details:**
- Use DECLARE block with variables: `_provider TEXT`, `_display_name TEXT`, `_avatar_url TEXT`, `_email_prefix TEXT`
- Use COALESCE and null-safe operations throughout to prevent trigger failures (a trigger exception would roll back the signup)
- Keep `SECURITY DEFINER` on the function
- Keep `LANGUAGE plpgsql`
- Add header comment explaining this replaces the original trigger to handle email signups
- Update the function COMMENT to reflect both providers

**Edge cases handled by "use as-is" decision (no special code needed):**
- `123@gmail.com` -> `"123"` (initcap on digits is a no-op)
- `a@gmail.com` -> `"A"`
- `john.doe@gmail.com` -> `"John Doe"`
- `john_doe-smith@gmail.com` -> `"John Doe Smith"`

**Anti-patterns to avoid:**
- Do NOT use `RAISE NOTICE` for errors -- use defensive null handling instead
- Do NOT recreate the trigger (it already exists and references the function name)
- Do NOT use DROP COLUMN or any destructive operations
- Do NOT hardcode provider detection by checking if metadata fields are null -- use `raw_app_meta_data->>'provider'` explicitly
  </action>
  <verify>
    <automated>grep -q "CREATE OR REPLACE FUNCTION public.handle_new_user" supabase/migrations/20260227000001_email_auth_trigger.sql && grep -q "raw_app_meta_data" supabase/migrations/20260227000001_email_auth_trigger.sql && grep -q "ui-avatars.com" supabase/migrations/20260227000001_email_auth_trigger.sql && grep -q "initcap" supabase/migrations/20260227000001_email_auth_trigger.sql && grep -q "split_part" supabase/migrations/20260227000001_email_auth_trigger.sql && grep -q "7C3AED" supabase/migrations/20260227000001_email_auth_trigger.sql && grep -q "SECURITY DEFINER" supabase/migrations/20260227000001_email_auth_trigger.sql && echo "PASS: Migration contains all required elements" || echo "FAIL: Migration missing required elements"</automated>
  </verify>
  <done>Migration file exists with provider-aware handle_new_user function that uses raw_app_meta_data for provider detection, derives display_name from email prefix with initcap, generates ui-avatars.com URL with Lumio brand color, and preserves existing Google OAuth behavior</done>
</task>

</tasks>

<verification>
1. Migration uses `CREATE OR REPLACE FUNCTION` (not DROP + CREATE)
2. Provider detection uses `raw_app_meta_data->>'provider'`
3. Google path preserves existing COALESCE(full_name, name) + avatar_url behavior
4. Email path derives display_name from email prefix with separator replacement and initcap
5. Email path generates ui-avatars.com URL with Lumio purple (7C3AED)
6. Function is SECURITY DEFINER and plpgsql
7. No trigger recreation (trigger already exists)
8. No destructive operations (no DROP)
</verification>

<success_criteria>
- Migration file exists and contains provider-aware handle_new_user function
- Google OAuth signup behavior is unchanged (display_name from metadata, Google avatar_url)
- Email signup produces display_name derived from email prefix (separators to spaces, title case)
- Email signup produces avatar_url from ui-avatars.com with Lumio brand color
- Function handles null/edge cases defensively (COALESCE, null-safe operations)
</success_criteria>

<output>
After completion, create `.planning/phases/27-foundation-database/27-02-SUMMARY.md`
</output>
