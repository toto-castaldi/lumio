---
phase: 36-scaffold-auth
plan: 02
type: execute
wave: 2
depends_on: ["36-01"]
files_modified:
  - apps/deck-builder/src/pages/LoginPage.tsx
  - apps/deck-builder/src/pages/SignUpPage.tsx
  - apps/deck-builder/src/pages/AuthCallback.tsx
  - apps/deck-builder/src/pages/OtpVerification.tsx
  - apps/deck-builder/src/pages/ForgotPassword.tsx
  - apps/deck-builder/src/pages/ResetPassword.tsx
  - apps/deck-builder/src/main.tsx
  - apps/deck-builder/public/logo-login.png
autonomous: true
requirements:
  - AUTH-01
  - AUTH-02

must_haves:
  truths:
    - "User can click Google sign-in button and be redirected to Google consent screen"
    - "User returns from Google consent to /auth/callback and is automatically signed in"
    - "User can enter email and password and sign in"
    - "User can sign up with email/password and be navigated to OTP verification"
    - "User can enter 6-digit OTP code and verify their email"
    - "User can request password reset and receive OTP flow"
    - "User can set a new password after OTP verification"
    - "Authenticated user is redirected away from login/signup pages"
    - "Unauthenticated user trying to access / is redirected to /login"
  artifacts:
    - path: "apps/deck-builder/src/pages/LoginPage.tsx"
      provides: "Login form with Google OAuth + email/password"
      min_lines: 60
    - path: "apps/deck-builder/src/pages/SignUpPage.tsx"
      provides: "Signup form with email/password/confirm"
      min_lines: 50
    - path: "apps/deck-builder/src/pages/AuthCallback.tsx"
      provides: "OAuth callback handler"
      min_lines: 15
    - path: "apps/deck-builder/src/pages/OtpVerification.tsx"
      provides: "6-digit OTP verification form"
      min_lines: 40
    - path: "apps/deck-builder/src/pages/ForgotPassword.tsx"
      provides: "Password reset request form"
      min_lines: 30
    - path: "apps/deck-builder/src/pages/ResetPassword.tsx"
      provides: "New password form after OTP verification"
      min_lines: 40
  key_links:
    - from: "apps/deck-builder/src/pages/LoginPage.tsx"
      to: "apps/deck-builder/src/contexts/AuthContext.tsx"
      via: "useAuth() for signInWithGoogle and signInWithEmail"
      pattern: "useAuth\\(\\)"
    - from: "apps/deck-builder/src/pages/SignUpPage.tsx"
      to: "/verify-otp"
      via: "react-router navigate after successful signup"
      pattern: "navigate.*verify-otp"
    - from: "apps/deck-builder/src/pages/AuthCallback.tsx"
      to: "apps/deck-builder/src/lib/supabase.ts"
      via: "onAuthStateChange listens for SIGNED_IN"
      pattern: "onAuthStateChange"
    - from: "apps/deck-builder/src/pages/ForgotPassword.tsx"
      to: "/reset-password"
      via: "navigate after OTP verification"
      pattern: "navigate.*reset-password"
---

<objective>
Implement all authentication pages: login (Google OAuth + email/password), signup, OTP verification, forgot password, and reset password. Users can complete the full auth lifecycle from sign-up through password reset.

Purpose: Deliver the complete auth UI that satisfies AUTH-01 (Google OAuth) and AUTH-02 (email/password). All auth flows must work end-to-end with the Supabase backend.
Output: Six fully functional auth page components wired to the router from Plan 01.
</objective>

<execution_context>
@/home/toto/.claude/get-shit-done/workflows/execute-plan.md
@/home/toto/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/36-scaffold-auth/36-RESEARCH.md
@.planning/phases/36-scaffold-auth/36-01-SUMMARY.md

<interfaces>
<!-- Interfaces created by Plan 01 that this plan depends on -->

From apps/deck-builder/src/contexts/AuthContext.tsx:
```typescript
interface AuthContextType {
  user: User | null;
  session: Session | null;
  state: AuthState;  // 'loading' | 'logged_out' | 'ready'
  signInWithGoogle: () => Promise<void>;
  signInWithEmail: (email: string, password: string) => Promise<void>;
  signUpWithEmail: (email: string, password: string) => Promise<void>;
  verifyEmailOtp: (email: string, token: string) => Promise<void>;
  resetPassword: (email: string) => Promise<void>;
  verifyRecoveryOtp: (email: string, token: string) => Promise<void>;
  updatePassword: (newPassword: string) => Promise<void>;
  signOut: () => Promise<void>;
}
export function useAuth(): AuthContextType;
```

From apps/deck-builder/src/contexts/I18nContext.tsx:
```typescript
interface I18nContextType {
  locale: AppLocale;
  t: (key: string, options?: Record<string, unknown>) => string;
  setLocale: (locale: AppLocale) => void;
}
export function useI18n(): I18nContextType;
```

From apps/deck-builder/src/lib/supabase.ts:
```typescript
export const supabase: SupabaseClient;
```

Router routes (from main.tsx):
- /login -> LoginPage
- /signup -> SignUpPage
- /verify-otp -> OtpVerification
- /forgot-password -> ForgotPassword
- /reset-password -> ResetPassword
- /auth/callback -> AuthCallback
- / -> DashboardPage (protected)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Login page, signup page, and OAuth callback</name>
  <files>
    apps/deck-builder/src/pages/LoginPage.tsx,
    apps/deck-builder/src/pages/SignUpPage.tsx,
    apps/deck-builder/src/pages/AuthCallback.tsx,
    apps/deck-builder/src/main.tsx,
    apps/deck-builder/public/logo-login.png
  </files>
  <action>
    1. Copy Lumio logo from `apps/android/assets/images/logo-login.png` to `apps/deck-builder/public/logo-login.png`. If the file doesn't exist at that path, search for it in `apps/android/assets/` or `apps/android/android/app/src/main/res/`. Use the best available PNG.

    2. Create `apps/deck-builder/src/pages/LoginPage.tsx`:
       - Use useAuth() for signInWithGoogle and signInWithEmail
       - Use useI18n() for t() translations
       - Layout mirrors mobile (per locked decision):
         - Centered container, max-width ~400px, vertically centered on page
         - Lumio logo (img src="/logo-login.png") at top
         - Tagline below logo (use t('common.tagline'))
         - Google OAuth button: prominent, full-width, styled with Google brand colors or neutral button style. Calls signInWithGoogle(). Text: t('login.signInWithGoogle')
         - "or" separator: horizontal line with text in the middle
         - Email field: type="email", full-width input
         - Password field: type="password", full-width input
         - Sign in button: full-width, primary color. Calls signInWithEmail(email, password)
         - Below form: "Forgot password?" link -> navigate to /forgot-password
         - Below: "Don't have an account? Sign up" link -> navigate to /signup
       - State management: email (string), password (string), loading (boolean), error (string | null)
       - On submit: try/catch, show error via toast on failure, loading state on button
       - Use Tailwind classes for all styling:
         - bg-lumio-bg for page background
         - bg-lumio-surface for card/form container
         - text-lumio-text for text
         - bg-lumio-primary for primary buttons
         - border-lumio-border for input borders
         - Rounded inputs and buttons, focus ring on inputs
       - Form must be keyboard-accessible (proper labels, autoFocus on email, form onSubmit)

    3. Create `apps/deck-builder/src/pages/SignUpPage.tsx`:
       - Use useAuth() for signUpWithEmail
       - Use useI18n() for translations
       - Use useNavigate() from react-router
       - Layout: same centered container style as LoginPage
         - Title: t('signup.title')
         - Email field
         - Password field
         - Confirm password field
         - Sign up button
         - Below: "Already have an account? Sign in" link -> /login
       - Validation: passwords must match (show error if not)
       - On submit: call signUpWithEmail(email, password), on success navigate to /verify-otp passing email as state (use navigate('/verify-otp', { state: { email } }))
       - Handle error: show toast with error message

    4. Create `apps/deck-builder/src/pages/AuthCallback.tsx`:
       - This is the OAuth PKCE callback handler
       - Use useNavigate() from react-router
       - Use useEffect to listen to supabase.auth.onAuthStateChange
       - When event === 'SIGNED_IN', navigate('/', { replace: true })
       - Show a centered loading spinner with "Signing in..." text while waiting
       - Clean up subscription on unmount
       - NOTE: With detectSessionInUrl: true, Supabase automatically detects the code parameter and exchanges it. No manual exchangeCodeForSession call needed.

    5. Update `apps/deck-builder/src/main.tsx`:
       - Replace placeholder LoginPage, SignUpPage, and AuthCallback imports with real file imports from ./pages/
       - Keep other placeholders (OtpVerification, ForgotPassword, ResetPassword, DashboardPage) as inline placeholders for now
  </action>
  <verify>
    <automated>cd /home/toto/scm-projects/lumio && pnpm --filter @lumio/deck-builder typecheck</automated>
  </verify>
  <done>
    LoginPage renders with Lumio logo, Google OAuth button, email/password form, forgot password link, and signup link. SignUpPage renders with email, password, confirm password, and navigates to /verify-otp on success. AuthCallback handles OAuth redirect and navigates to / on SIGNED_IN. All pages use i18n translations and Tailwind styling with lumio color tokens.
  </done>
</task>

<task type="auto">
  <name>Task 2: OTP verification, forgot password, and reset password pages</name>
  <files>
    apps/deck-builder/src/pages/OtpVerification.tsx,
    apps/deck-builder/src/pages/ForgotPassword.tsx,
    apps/deck-builder/src/pages/ResetPassword.tsx,
    apps/deck-builder/src/main.tsx
  </files>
  <action>
    1. Create `apps/deck-builder/src/pages/OtpVerification.tsx`:
       - Use useAuth() for verifyEmailOtp
       - Use useI18n() for translations
       - Use useNavigate() and useLocation() from react-router
       - Get email from location.state.email (if missing, show error and link back to signup)
       - Layout: centered container matching auth page style
         - Title: t('otp.title')
         - Subtitle: t('otp.subtitle') + email
         - 6 individual digit input fields (each accepts 1 digit, auto-advances to next on input, supports backspace to go back)
         - Verify button: calls verifyEmailOtp(email, token) where token is the 6 digits concatenated
         - Back to sign in link -> /login
       - State: digits array of 6 strings, loading, error
       - On successful verification: navigate to /login with a toast success message
       - Auto-submit when 6th digit entered (optional but nice UX)
       - Use refs for the 6 input fields to manage focus

    2. Create `apps/deck-builder/src/pages/ForgotPassword.tsx`:
       - Use useAuth() for resetPassword
       - Use useI18n() for translations
       - Use useNavigate()
       - Layout: centered container
         - Title: t('forgotPassword.title')
         - Subtitle: t('forgotPassword.subtitle') explaining the flow
         - Email field
         - Send code button: calls resetPassword(email), on success navigate to /reset-password passing email as state
         - Back to sign in link -> /login
       - State: email, loading, error

    3. Create `apps/deck-builder/src/pages/ResetPassword.tsx`:
       - Use useAuth() for verifyRecoveryOtp and updatePassword
       - Use useI18n() for translations
       - Use useNavigate() and useLocation()
       - Get email from location.state.email
       - Two-step flow within one page:
         - Step 1: OTP verification (same 6-digit input pattern as OtpVerification)
           - On success (verifyRecoveryOtp), advance to Step 2
         - Step 2: New password form
           - New password field
           - Confirm new password field
           - Update password button: validates match, calls updatePassword(newPassword)
           - On success: navigate to /login with toast success message
       - State: step (1 or 2), digits, newPassword, confirmPassword, loading, error
       - Validation: passwords must match

    4. Update `apps/deck-builder/src/main.tsx`:
       - Replace remaining inline placeholder components (OtpVerification, ForgotPassword, ResetPassword) with real imports from ./pages/
       - DashboardPage placeholder remains inline (Plan 03 handles it)
  </action>
  <verify>
    <automated>cd /home/toto/scm-projects/lumio && pnpm --filter @lumio/deck-builder typecheck</automated>
  </verify>
  <done>
    OTP verification page shows 6-digit input and verifies email. Forgot password page sends reset email. Reset password page has 2-step flow (OTP then new password). All auth flows navigate correctly between pages. All pages use i18n and Tailwind styling. TypeScript compiles cleanly.
  </done>
</task>

</tasks>

<verification>
- `pnpm --filter @lumio/deck-builder typecheck` passes
- All auth pages render without errors in the browser
- Navigation flow: /login -> /signup -> /verify-otp -> /login (signup flow)
- Navigation flow: /login -> /forgot-password -> /reset-password -> /login (reset flow)
- Google OAuth button initiates redirect to Google consent screen
- Auth callback page handles return from Google and redirects to /
</verification>

<success_criteria>
- Login page mirrors mobile layout (logo, Google button, separator, email/password, links)
- Both email and password fields visible at once (no progressive disclosure)
- Signup navigates to OTP verification with email passed via router state
- OTP page has 6 individual digit inputs with auto-advance
- Password reset has 2-step OTP + new password flow
- All text uses i18n translations (no hardcoded strings)
- All pages use lumio color tokens from Tailwind theme
- Authenticated users redirected away from auth pages
</success_criteria>

<output>
After completion, create `.planning/phases/36-scaffold-auth/36-02-SUMMARY.md`
</output>
