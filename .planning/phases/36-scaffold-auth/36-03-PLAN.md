---
phase: 36-scaffold-auth
plan: 03
type: execute
wave: 2
depends_on: ["36-01"]
files_modified:
  - apps/deck-builder/src/components/Layout.tsx
  - apps/deck-builder/src/components/Header.tsx
  - apps/deck-builder/src/components/Sidebar.tsx
  - apps/deck-builder/src/components/AvatarDropdown.tsx
  - apps/deck-builder/src/pages/DashboardPage.tsx
  - apps/deck-builder/src/main.tsx
  - apps/deck-builder/public/logo-header.png
autonomous: false
requirements:
  - AUTH-03
  - AUTH-04
  - AUTH-05

must_haves:
  truths:
    - "Sidebar is always visible on desktop (>= 1024px) and hidden behind hamburger on mobile/tablet (< 1024px)"
    - "On mobile, sidebar slides in as overlay drawer with backdrop"
    - "Sidebar closes automatically when user navigates on mobile"
    - "Header shows Lumio logo on left, language toggle + dark mode icon + avatar on right"
    - "Language toggle is a segmented control (IT|EN) that switches language immediately"
    - "Dark mode toggle cycles system -> light -> dark with tooltip showing current mode"
    - "Avatar click opens dropdown with user email and sign out button"
    - "Theme and language preferences persist across page refreshes (localStorage)"
    - "All UI text displays in the selected language"
  artifacts:
    - path: "apps/deck-builder/src/components/Layout.tsx"
      provides: "App shell with sidebar, header, and main content area"
      min_lines: 40
    - path: "apps/deck-builder/src/components/Header.tsx"
      provides: "Header bar with logo, language toggle, dark mode, avatar"
      min_lines: 50
    - path: "apps/deck-builder/src/components/Sidebar.tsx"
      provides: "Collapsible sidebar with placeholder content"
      min_lines: 20
    - path: "apps/deck-builder/src/components/AvatarDropdown.tsx"
      provides: "User avatar with dropdown (email + sign out)"
      min_lines: 30
    - path: "apps/deck-builder/src/pages/DashboardPage.tsx"
      provides: "Placeholder authenticated landing page"
      min_lines: 10
  key_links:
    - from: "apps/deck-builder/src/components/Header.tsx"
      to: "apps/deck-builder/src/contexts/ThemeContext.tsx"
      via: "useTheme() for dark mode toggle"
      pattern: "useTheme\\(\\)"
    - from: "apps/deck-builder/src/components/Header.tsx"
      to: "apps/deck-builder/src/contexts/I18nContext.tsx"
      via: "useI18n() for language toggle and translations"
      pattern: "useI18n\\(\\)"
    - from: "apps/deck-builder/src/components/AvatarDropdown.tsx"
      to: "apps/deck-builder/src/contexts/AuthContext.tsx"
      via: "useAuth() for user email and signOut"
      pattern: "useAuth\\(\\)"
    - from: "apps/deck-builder/src/components/Layout.tsx"
      to: "apps/deck-builder/src/components/Sidebar.tsx"
      via: "renders Sidebar in desktop and mobile drawer"
      pattern: "Sidebar"
    - from: "apps/deck-builder/src/main.tsx"
      to: "apps/deck-builder/src/components/Layout.tsx"
      via: "ProtectedLayout wraps children in Layout"
      pattern: "Layout"
---

<objective>
Build the authenticated app shell: responsive layout with collapsible sidebar, header bar with preference controls (language toggle, dark mode, avatar dropdown), and dashboard placeholder. This is the UI frame that all future phases (deck list, card editor) render within.

Purpose: Deliver the visual application shell satisfying AUTH-03 (responsive layout), AUTH-04 (i18n toggle), and AUTH-05 (dark mode toggle). Users see a polished, functional app frame after logging in.
Output: Layout, Header, Sidebar, AvatarDropdown components and DashboardPage. Visual verification checkpoint.
</objective>

<execution_context>
@/home/toto/.claude/get-shit-done/workflows/execute-plan.md
@/home/toto/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/36-scaffold-auth/36-RESEARCH.md
@.planning/phases/36-scaffold-auth/36-01-SUMMARY.md

<interfaces>
<!-- Interfaces created by Plan 01 that this plan depends on -->

From apps/deck-builder/src/contexts/AuthContext.tsx:
```typescript
interface AuthContextType {
  user: User | null;
  session: Session | null;
  state: AuthState;
  signOut: () => Promise<void>;
  // ... other auth methods
}
export function useAuth(): AuthContextType;
```

From apps/deck-builder/src/contexts/ThemeContext.tsx:
```typescript
interface ThemeContextType {
  preference: ThemePreference;  // 'system' | 'light' | 'dark'
  isDark: boolean;
  toggleTheme: () => void;
}
export function useTheme(): ThemeContextType;
```

From apps/deck-builder/src/contexts/I18nContext.tsx:
```typescript
interface I18nContextType {
  locale: AppLocale;  // 'en' | 'it'
  t: (key: string, options?: Record<string, unknown>) => string;
  setLocale: (locale: AppLocale) => void;
}
export function useI18n(): I18nContextType;
```

Translation keys available (from Plan 01 i18n/en.ts):
- header.language, header.theme, header.account
- sidebar.placeholder ("Your decks will appear here")
- dashboard.welcome, dashboard.placeholder
- common.signOut, common.appName
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: App shell components (Layout, Header, Sidebar, AvatarDropdown, DashboardPage)</name>
  <files>
    apps/deck-builder/src/components/Layout.tsx,
    apps/deck-builder/src/components/Header.tsx,
    apps/deck-builder/src/components/Sidebar.tsx,
    apps/deck-builder/src/components/AvatarDropdown.tsx,
    apps/deck-builder/src/pages/DashboardPage.tsx,
    apps/deck-builder/src/main.tsx,
    apps/deck-builder/public/logo-header.png
  </files>
  <action>
    1. Copy a suitable Lumio logo for the header. Check `apps/android/assets/images/` for a header-sized logo or icon. If a separate header logo doesn't exist, use the same logo-login.png scaled down. Copy to `apps/deck-builder/public/logo-header.png`.

    2. Create `apps/deck-builder/src/components/Header.tsx`:
       - Use useTheme() for preference, isDark, toggleTheme
       - Use useI18n() for locale, t, setLocale
       - Use useAuth() for user
       - Props: onMenuClick (callback for hamburger button)
       - Layout (per locked decision): `[hamburger (mobile only)] [Lumio logo] ... [IT|EN] [theme icon] [Avatar]`
       - Hamburger button: visible only below lg breakpoint (className "lg:hidden"), renders a menu icon (use a simple SVG hamburger icon - 3 horizontal lines)
       - Lumio logo: img src="/logo-header.png", h-8, link to / or just display
       - Language toggle: segmented control with IT and EN buttons side-by-side. Active segment highlighted with bg-lumio-primary text-white, inactive with bg-lumio-surface. Clicking switches locale via setLocale(). Always visible (per locked decision).
       - Dark mode toggle: icon button that cycles through system/light/dark via toggleTheme(). Show appropriate icon:
         - system: computer/monitor icon (SVG)
         - light: sun icon (SVG)
         - dark: moon icon (SVG)
         - Add title/tooltip attribute showing current mode name (t('header.themeSystem'), t('header.themeLight'), t('header.themeDark'))
       - Avatar: use AvatarDropdown component
       - Header styling: fixed top, full width, h-14 or h-16, bg-lumio-surface, border-b border-lumio-border, px-4, flex items-center justify-between, z-30

    3. Create `apps/deck-builder/src/components/AvatarDropdown.tsx`:
       - Use useAuth() for user (email) and signOut
       - Use useI18n() for t()
       - Show user avatar: first letter of email in a colored circle (bg-lumio-primary text-white rounded-full w-8 h-8)
       - On click: toggle dropdown menu
       - Dropdown: positioned below avatar, right-aligned, bg-lumio-surface, border, rounded, shadow-lg, z-50
         - User email (text-lumio-text-secondary, text-sm, truncated if long)
         - Divider
         - Sign out button (text-lumio-danger, hover background)
       - Close dropdown on click outside (useEffect with document click listener)
       - Close dropdown on Escape key

    4. Create `apps/deck-builder/src/components/Sidebar.tsx`:
       - Use useI18n() for t()
       - Phase 36 placeholder content (per Claude's discretion):
         - Section header: app name or "Decks" with an icon
         - Placeholder message: t('sidebar.placeholder') with a subtle folder/deck icon
         - This will be replaced with actual deck list in Phase 38
       - Styling: h-full, p-4, bg-lumio-surface (on mobile overlay), bg-transparent or bg-lumio-bg on desktop

    5. Create `apps/deck-builder/src/components/Layout.tsx`:
       - Use useState for sidebarOpen (boolean, default false)
       - Use useLocation() from react-router to close sidebar on navigation (per Pitfall 5):
         - useEffect watching location.pathname: setSidebarOpen(false) on change
       - Props: children (ReactNode)
       - Structure (per locked decisions):
         - Header at top (pass onMenuClick to toggle sidebar)
         - Below header, flex container:
           - Desktop sidebar: hidden below lg, visible at lg+, fixed width (w-64), full height minus header, border-r
           - Mobile sidebar overlay (when sidebarOpen):
             - Backdrop: fixed inset-0, bg-black/50, z-40, lg:hidden. Click closes sidebar.
             - Sidebar panel: fixed, inset-y-0, left-0, w-64, bg-lumio-surface, z-50, lg:hidden. Add CSS transition for slide-in (transform translateX).
           - Main content: flex-1, p-4 lg:p-6, overflow-y-auto
         - Account for header height: add pt-14 or pt-16 to the flex container (since header is fixed)
       - Responsive behavior:
         - Below 1024px (lg): sidebar hidden, hamburger visible in header, sidebar opens as overlay drawer
         - At/above 1024px (lg): sidebar always visible, no hamburger, no overlay

    6. Create `apps/deck-builder/src/pages/DashboardPage.tsx`:
       - Use useAuth() for user
       - Use useI18n() for t()
       - Placeholder content (per Claude's discretion):
         - Welcome heading: t('dashboard.welcome') (could include user's email or display name)
         - Placeholder message: t('dashboard.placeholder') explaining that deck management will be available in a future update
         - Centered content with some padding, subtle styling
       - This page is the default authenticated route (/)

    7. Update `apps/deck-builder/src/main.tsx`:
       - Import Layout from ./components/Layout
       - Import DashboardPage from ./pages/DashboardPage
       - Update ProtectedLayout component to wrap Outlet in Layout component:
         ```
         function ProtectedLayout() {
           const { state } = useAuth();
           if (state === 'loading') return <LoadingSpinner />;
           if (state === 'logged_out') return <Navigate to="/login" replace />;
           return <Layout><Outlet /></Layout>;
         }
         ```
       - Remove the inline DashboardPage placeholder
       - Add translation keys for theme tooltips if not already in i18n files: header.themeSystem, header.themeLight, header.themeDark (add to both en.ts and it.ts if missing)
  </action>
  <verify>
    <automated>cd /home/toto/scm-projects/lumio && pnpm --filter @lumio/deck-builder typecheck</automated>
  </verify>
  <done>
    Layout renders with fixed header and sidebar. Sidebar is always visible on desktop, hidden behind hamburger on mobile. Header shows Lumio logo, IT/EN segmented toggle, dark mode icon button with tooltips, and avatar dropdown. Avatar dropdown shows user email and sign out button. Dashboard shows placeholder welcome content. All text uses i18n. All components use lumio Tailwind color tokens.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify responsive layout, dark mode, and i18n</name>
  <files>none</files>
  <action>
    Human verification of the complete app shell UI. No code changes -- this is a visual and functional checkpoint.
  </action>
  <what-built>
    Complete app shell with responsive sidebar, header preferences (language toggle, dark mode toggle), and avatar dropdown. Full auth flow (login, signup, OTP, password reset) if Plan 02 has also completed.
  </what-built>
  <how-to-verify>
    1. Start the dev server: `cd /home/toto/scm-projects/lumio && pnpm dev:deck-builder`
    2. Open http://localhost:5173 in browser

    **Auth flow (if Plan 02 complete):**
    3. Verify login page shows: Lumio logo, tagline, Google button, "or" separator, email/password fields, forgot password link, signup link
    4. Click "Sign up" link -> verify signup form with email, password, confirm password
    5. Click "Sign in" link to go back

    **App shell (after logging in or with a test session):**
    6. After authentication, verify:
       - Header: Lumio logo left, IT|EN toggle + dark mode icon + avatar right
       - Sidebar visible on left with placeholder content
       - Main area shows dashboard placeholder

    **Responsive (resize browser):**
    7. At full desktop width (>1024px): sidebar always visible, no hamburger
    8. Resize to <1024px: sidebar disappears, hamburger icon appears in header
    9. Click hamburger: sidebar slides in as overlay with dark backdrop
    10. Click backdrop: sidebar closes
    11. Navigate while sidebar open: sidebar closes automatically

    **Dark mode:**
    12. Click dark mode icon: verify it cycles system -> light -> dark
    13. In dark mode: background should be dark, text light, inputs styled correctly
    14. Refresh page: dark mode persists (no flash of white)

    **Language:**
    15. Click IT in the language toggle: all text switches to Italian immediately
    16. Click EN: text switches back to English
    17. Refresh page: language preference persists

    **Avatar dropdown:**
    18. Click avatar circle: dropdown shows user email and sign out button
    19. Click outside dropdown: it closes
    20. Click sign out: redirected to login page
  </how-to-verify>
  <verify>
    <automated>cd /home/toto/scm-projects/lumio && pnpm --filter @lumio/deck-builder typecheck</automated>
  </verify>
  <done>
    User has visually confirmed: responsive sidebar behavior, dark mode cycling with persistence, language toggle with persistence, avatar dropdown with sign out, and overall app shell layout.
  </done>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
- `pnpm --filter @lumio/deck-builder typecheck` passes
- Responsive layout works at mobile, tablet, and desktop widths
- Dark mode toggle cycles correctly and persists
- Language toggle switches all UI text and persists
- Avatar dropdown opens/closes properly with sign out functionality
- Sidebar closes on navigation (mobile)
- No dark mode FOUC on page load
</verification>

<success_criteria>
- Sidebar always visible on desktop (>= 1024px), hamburger overlay on mobile (< 1024px)
- Header: [hamburger (mobile)] [Logo] ... [IT|EN] [theme icon] [Avatar]
- Language segmented control toggles between IT and EN with immediate effect
- Dark mode cycles system -> light -> dark with icon and tooltip feedback
- Avatar dropdown shows email and sign out
- localStorage persists both preferences across refresh
- All text uses i18n (no hardcoded strings)
- Layout uses lumio color tokens for both light and dark themes
</success_criteria>

<output>
After completion, create `.planning/phases/36-scaffold-auth/36-03-SUMMARY.md`
</output>
