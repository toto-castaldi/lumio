---
phase: 09-internationalization
plan: 03
type: execute
wave: 2
depends_on: ["09-01"]
files_modified:
  - apps/android/components/AddRepoForm.tsx
  - apps/android/components/OfflineBanner.tsx
  - apps/android/components/RepoListItem.tsx
  - apps/android/components/study/ExplanationPanel.tsx
  - apps/android/components/study/CardPreviewModal.tsx
  - apps/android/navigation/MainNavigator.tsx
autonomous: true

must_haves:
  truths:
    - "AddRepoForm validation messages and PAT prompt display in selected language"
    - "OfflineBanner text displays in selected language"
    - "RepoListItem swipe-to-delete label displays in selected language"
    - "ExplanationPanel result text, vote label, and vote buttons display in selected language"
    - "CardPreviewModal fallback title and empty state display in selected language"
    - "Navigation header titles (Repositories, Settings) display in selected language"
    - "No untranslated strings visible when navigating any screen in either language"
  artifacts:
    - path: "apps/android/components/AddRepoForm.tsx"
      provides: "Translated form validation and PAT prompt strings via t()"
      contains: "useI18n"
    - path: "apps/android/components/OfflineBanner.tsx"
      provides: "Translated offline banner text via t()"
      contains: "useI18n"
    - path: "apps/android/components/RepoListItem.tsx"
      provides: "Translated delete action label via t()"
      contains: "useI18n"
    - path: "apps/android/components/study/ExplanationPanel.tsx"
      provides: "Translated result text and vote UI via t()"
      contains: "useI18n"
    - path: "apps/android/components/study/CardPreviewModal.tsx"
      provides: "Translated fallback title and empty state via t()"
      contains: "useI18n"
    - path: "apps/android/navigation/MainNavigator.tsx"
      provides: "Dynamic header titles that update on language change"
      contains: "useI18n"
  key_links:
    - from: "apps/android/navigation/MainNavigator.tsx"
      to: "apps/android/hooks/useI18n"
      via: "t() for screen header titles in tab navigator options"
      pattern: "t\\("
    - from: "apps/android/components/AddRepoForm.tsx"
      to: "apps/android/hooks/useI18n"
      via: "t() for validation error messages set inside component"
      pattern: "useI18n"
---

<objective>
Extract all hardcoded English strings from the 5 remaining component files and the MainNavigator, completing the full i18n coverage across the entire app.

Purpose: Completes I18N-03 (all UI strings translate on language change) by covering the components and navigation layer. After this plan, there are zero untranslated user-visible strings in the app.

Output: All components and navigation use t() for user-visible strings. Phase 9 success criteria are fully met.
</objective>

<execution_context>
@/home/toto/.claude/get-shit-done/workflows/execute-plan.md
@/home/toto/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-internationalization/09-RESEARCH.md
@.planning/phases/09-internationalization/09-01-SUMMARY.md
@apps/android/i18n/en.ts
@apps/android/components/AddRepoForm.tsx
@apps/android/components/OfflineBanner.tsx
@apps/android/components/RepoListItem.tsx
@apps/android/components/study/ExplanationPanel.tsx
@apps/android/components/study/CardPreviewModal.tsx
@apps/android/navigation/MainNavigator.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Translate all component files</name>
  <files>
    apps/android/components/AddRepoForm.tsx
    apps/android/components/OfflineBanner.tsx
    apps/android/components/RepoListItem.tsx
    apps/android/components/study/ExplanationPanel.tsx
    apps/android/components/study/CardPreviewModal.tsx
  </files>
  <action>
For each file: import `useI18n` from the appropriate relative path to `hooks/useI18n`, destructure `const { t } = useI18n()` inside the component, and replace every hardcoded English string.

**AddRepoForm.tsx:**
- Import from `'../hooks/useI18n'`
- `'Repository URL is required'` -> `t('components.urlRequired')`
- `'Enter a valid GitHub repository URL'` -> `t('components.invalidGithubUrl')`
- `'This repository appears to be private. Enter a Personal Access Token:'` -> `t('components.privateRepoPatLabel')`
- `'Cancel'` button -> `t('common.cancel')`
- `'Submit with Token'` button -> `t('components.submitWithToken')`

**OfflineBanner.tsx:**
- Import from `'../hooks/useI18n'`
- `'No internet connection'` -> `t('components.noInternet')`

**RepoListItem.tsx:**
- Import from `'../hooks/useI18n'`
- `'Delete'` (swipe action text) -> `t('components.delete')`

**ExplanationPanel.tsx:**
- Import from `'../../hooks/useI18n'`
- `'Correct!'` / `'Incorrect'` -> `isCorrect ? t('components.correct') : t('components.incorrect')`
- `'Was this question helpful?'` -> `t('components.questionHelpful')`
- `'Yes'` vote button -> `t('components.yes')`
- `'No'` vote button -> `t('components.no')`

**CardPreviewModal.tsx:**
- Import from `'../../hooks/useI18n'`
- `'Card Content'` fallback title -> `t('components.cardContent')`
- `'No card content to display'` -> `t('components.noCardContent')`
  </action>
  <verify>
  - `pnpm --filter @lumio/android exec -- npx tsc --noEmit` passes
  - No hardcoded English strings remain in any of the 5 component files
  - In Italiano mode: open card preview modal -- fallback title is Italian; add repo with invalid URL -- error is in Italian; go offline -- banner text is Italian; swipe to delete -- "Elimina" label shows; answer a quiz -- "Corretto!"/"Sbagliato" and vote text are Italian
  </verify>
  <done>All 5 component files use t() for every user-visible string. Validation messages, toast text, button labels, and informational text all translate on language switch.</done>
</task>

<task type="auto">
  <name>Task 2: Translate MainNavigator header titles and verify full coverage</name>
  <files>
    apps/android/navigation/MainNavigator.tsx
  </files>
  <action>
1. Update `apps/android/navigation/MainNavigator.tsx`:
   - Import `useI18n` from `'../hooks/useI18n'`
   - Destructure `const { t } = useI18n()` inside MainNavigator
   - Repos tab: change `title: 'Repositories'` to `title: t('navigation.repositories')`
   - Settings tab: Currently uses the route name as header title (default behavior). Add explicit `title: t('navigation.settings')` to Settings screen options
   - Dashboard tab: Uses a logo image as headerTitle -- no text to translate (correct, leave as-is)

   NOTE: React Navigation may cache screen options. Since MainNavigator is a function component that re-renders when context changes (because it uses useI18n which depends on locale), the `title` values will be re-evaluated on locale change. This is the standard approach and works because the Tab.Navigator is inside the I18nProvider.

2. Final verification sweep -- check that NO user-visible English strings remain in:
   - All 6 screen files
   - All 5 translatable component files (skip ConnectionTest.tsx per research decision)
   - MainNavigator.tsx
   - EmptyState.tsx (receives all strings via props -- no hardcoded text, nothing to translate)
   - StatCard.tsx (receives label and value via props -- nothing to translate)
   - ProgressBar.tsx (only shows numbers -- nothing to translate)
   - StudyFAB.tsx (check if it has text -- if so, translate)

   If any strings were missed, fix them now.
  </action>
  <verify>
  - `pnpm --filter @lumio/android exec -- npx tsc --noEmit` passes
  - In Italiano mode: Repos tab header says "Repository" (not "Repositories"), Settings header says "Impostazioni" (not "Settings")
  - Navigate through EVERY screen in Italiano mode and verify zero English UI strings are visible:
    * Login: tagline, button text
    * Dashboard: stat labels, empty state, study button
    * Repos: header, empty state, add form validation, delete flow
    * Settings: all section headers, option labels, logout button, language selector
    * Study: all states (loading, no cards, ready, studying, completed), header, skip, alerts
    * Summary: title, stats, button
  - Switch back to English -- everything reverts
  - Card content and quiz questions remain in original language regardless of UI language
  </verify>
  <done>Navigation header titles translate on language change. Full app coverage verified -- zero untranslated user-visible strings in either language. I18N-03 complete. I18N-04 verified (card/quiz content untouched). Phase 9 success criteria all met.</done>
</task>

</tasks>

<verification>
1. TypeScript compiles: `pnpm --filter @lumio/android exec -- npx tsc --noEmit`
2. Full navigation test in Italiano:
   - Login screen: Italian tagline and button
   - Dashboard: Italian stat labels, empty state, study button
   - Repos: Italian header title, empty state, form validation, delete confirm, toasts
   - Settings: Italian section headers, option labels, logout, language selector
   - Study session: Italian in all states (ready, studying, skip, quit alert, complete)
   - Study summary: Italian stat labels and button
   - Card preview: Italian fallback title and empty state
   - Offline banner: Italian text
3. Switch to English: all strings revert to English
4. Card content stays in original language (I18N-04)
5. App restart preserves language choice (I18N-02, verified in Plan 01)
</verification>

<success_criteria>
- All 5 component files and MainNavigator use t() for user-visible strings
- Navigation header titles update on language change
- Zero untranslated strings visible when navigating any screen in either language
- Card content and AI-generated questions remain in original language (I18N-04)
- Phase 9 success criteria #1-5 from ROADMAP.md all verified
</success_criteria>

<output>
After completion, create `.planning/phases/09-internationalization/09-03-SUMMARY.md`
</output>
