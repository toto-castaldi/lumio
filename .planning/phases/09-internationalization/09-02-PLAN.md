---
phase: 09-internationalization
plan: 02
type: execute
wave: 2
depends_on: ["09-01"]
files_modified:
  - apps/android/screens/LoginScreen.tsx
  - apps/android/screens/DashboardScreen.tsx
  - apps/android/screens/ReposScreen.tsx
  - apps/android/screens/StudyScreen.tsx
  - apps/android/screens/StudySummaryScreen.tsx
autonomous: true

must_haves:
  truths:
    - "All screen UI strings update immediately when language changes"
    - "LoginScreen tagline, button text, and error messages display in selected language"
    - "DashboardScreen stat labels, empty state, study button, and relative time suffixes display in selected language"
    - "ReposScreen toasts, alerts, and empty state display in selected language"
    - "StudyScreen all states (loading, no_cards, ready, studying, completed) display in selected language"
    - "StudySummaryScreen title, stat labels, and button display in selected language"
    - "Card content and AI-generated quiz questions remain in original language"
  artifacts:
    - path: "apps/android/screens/LoginScreen.tsx"
      provides: "Translated login UI strings via t()"
      contains: "useI18n"
    - path: "apps/android/screens/DashboardScreen.tsx"
      provides: "Translated dashboard strings including relative time via t()"
      contains: "useI18n"
    - path: "apps/android/screens/ReposScreen.tsx"
      provides: "Translated repo management strings (toasts, alerts, empty state) via t()"
      contains: "useI18n"
    - path: "apps/android/screens/StudyScreen.tsx"
      provides: "Translated study session strings across all states via t()"
      contains: "useI18n"
    - path: "apps/android/screens/StudySummaryScreen.tsx"
      provides: "Translated summary stats and labels via t()"
      contains: "useI18n"
  key_links:
    - from: "apps/android/screens/DashboardScreen.tsx"
      to: "apps/android/hooks/useI18n"
      via: "useI18n().t for all user-visible strings"
      pattern: "useI18n"
    - from: "apps/android/screens/ReposScreen.tsx"
      to: "apps/android/hooks/useI18n"
      via: "t() inside Toast.show and Alert.alert handlers"
      pattern: "t\\("
    - from: "apps/android/screens/StudyScreen.tsx"
      to: "apps/android/hooks/useI18n"
      via: "t() in all render states and imperative handlers"
      pattern: "t\\("
---

<objective>
Extract all hardcoded English strings from the 5 remaining screen files and replace them with t() calls using the translation keys defined in 09-01.

Purpose: Completes I18N-03 for all screen-level UI text. After this plan, every screen in the app responds to language changes.

Output: All 5 screen files use useI18n().t() for every user-visible string. No hardcoded English strings remain in screen files.
</objective>

<execution_context>
@/home/toto/.claude/get-shit-done/workflows/execute-plan.md
@/home/toto/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-internationalization/09-RESEARCH.md
@.planning/phases/09-internationalization/09-01-SUMMARY.md
@apps/android/i18n/en.ts
@apps/android/screens/LoginScreen.tsx
@apps/android/screens/DashboardScreen.tsx
@apps/android/screens/ReposScreen.tsx
@apps/android/screens/StudyScreen.tsx
@apps/android/screens/StudySummaryScreen.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Translate LoginScreen, DashboardScreen, and ReposScreen</name>
  <files>
    apps/android/screens/LoginScreen.tsx
    apps/android/screens/DashboardScreen.tsx
    apps/android/screens/ReposScreen.tsx
  </files>
  <action>
For each file: import `useI18n` from `'../hooks/useI18n'`, destructure `const { t } = useI18n()` inside the component, and replace every hardcoded English string with the corresponding `t()` call.

**LoginScreen.tsx:**
- `accessibilityLabel="Lumio logo"` -> `accessibilityLabel={t('login.lumioLogo')}`
- `"Your flashcards, supercharged"` -> `t('login.tagline')`
- `'Sign in failed'` fallback -> `t('login.signInFailed')`
- `"Sign in with Google"` -> `t('login.signInWithGoogle')`
- The `Google Sign-In not configured.\nSet EXPO_PUBLIC_GOOGLE_WEB_CLIENT_ID` -> `t('login.googleNotConfigured')`

**DashboardScreen.tsx:**
- Modify `formatLastStudied` to accept a `t` function parameter (since it's defined outside the component). Change its signature to `formatLastStudied(dateString: string | null, t: (key: string, options?: Record<string, unknown>) => string): string`. Call it as `formatLastStudied(lastStudied, t)` inside the component.
- `'Not yet'` -> `t('dashboard.notYet')`
- `'Just now'` -> `t('dashboard.justNow')`
- `` `${diffMinutes}m ago` `` -> `t('dashboard.mAgo', { count: diffMinutes })`
- `` `${diffHours}h ago` `` -> `t('dashboard.hAgo', { count: diffHours })`
- `` `${diffDays}d ago` `` -> `t('dashboard.dAgo', { count: diffDays })`
- `"Repositories"` stat label -> `t('dashboard.repositories')`
- `"Cards"` stat label -> `t('dashboard.cards')`
- `"Last Studied"` stat label -> `t('dashboard.lastStudied')`
- `"Start Study Session"` -> `t('dashboard.startStudySession')`
- Empty state: `"No Repositories Yet"` -> `t('dashboard.emptyTitle')`
- Empty state: `"Add a repository..."` -> `t('dashboard.emptySubtitle')`
- Empty state: `"Go to Repositories"` -> `t('dashboard.goToRepos')`

**ReposScreen.tsx:**
- All Toast.show `text1`/`text2` strings must use `t()` at the point of the Toast.show call (inside the async handler). This is correct because toasts are imperative.
- `'Failed to load repositories'` -> `t('repos.failedToLoad')`
- `'Unknown error'` (in fetchRepos catch) -> `t('common.unknownError')`
- `'Repository added'` -> `t('repos.repoAdded')`
- `'Syncing cards from repository...'` -> `t('repos.syncingCards')`
- `'Private repository?'` -> `t('repos.privateRepo')`
- `'Enter a Personal Access Token to add this repo.'` -> `t('repos.enterPat')`
- `'Failed to add repository'` -> `t('repos.failedToAdd')`
- Alert.alert: `'Delete Repository'` -> `t('repos.deleteConfirmTitle')`
- Alert body with interpolation: `t('repos.deleteConfirmBody', { name })`
- Alert buttons: `'Cancel'` -> `t('common.cancel')`, `'Delete'` -> `t('common.delete')`
- `'Repository deleted'` -> `t('repos.repoDeleted')`
- The body text `'"${name}" has been removed.'` -> `t('repos.repoDeletedBody', { name })`
- `'Failed to delete repository'` -> `t('repos.failedToDelete')`
- `'Unknown error'` (in delete catch) -> `t('common.unknownError')`
- Empty state: `"No repositories yet"` -> `t('repos.emptyTitle')`
- Empty state: `"Add a GitHub repository URL above..."` -> `t('repos.emptySubtitle')`

IMPORTANT: The `handleDeleteRepo` and `handleAddRepo` callbacks use `useCallback`. Since they now reference `t`, add `t` to the dependency array. The `t` function is recreated when locale changes (via useCallback([locale]) in the context), which will correctly cause these callbacks to use updated translations.

IMPORTANT: `fetchRepos` also uses `t` now (for the error toast), so add `t` to its dependency array and the dependency arrays of handlers that depend on `fetchRepos`.
  </action>
  <verify>
  - `pnpm --filter @lumio/android exec -- npx tsc --noEmit` passes
  - No hardcoded English strings remain in LoginScreen, DashboardScreen, or ReposScreen (search for quoted English text)
  - Switch to Italiano in Settings, navigate to Dashboard: stat labels show "Repository", "Schede", "Ultimo studio"; study button shows Italian text
  - On Repos screen: empty state shows Italian text; add/delete repo toasts and alerts show Italian
  </verify>
  <done>LoginScreen, DashboardScreen, and ReposScreen use t() for all user-visible strings. Dynamic strings use interpolation. Imperative calls (Toast, Alert) call t() at invocation time inside handlers.</done>
</task>

<task type="auto">
  <name>Task 2: Translate StudyScreen and StudySummaryScreen</name>
  <files>
    apps/android/screens/StudyScreen.tsx
    apps/android/screens/StudySummaryScreen.tsx
  </files>
  <action>
**StudyScreen.tsx:**
- Import `useI18n` and destructure `const { t } = useI18n()` inside StudyScreen
- Header title: `{isReviewing ? 'Review' : 'Study'}` -> `{isReviewing ? t('study.review') : t('study.studyTitle')}`
- Skip button: `{isSkipping ? 'Skipping...' : 'Skip'}` -> `{isSkipping ? t('study.skipping') : t('study.skip')}`
- Toast in onSkip: `'Card skipped'` -> `t('study.cardSkipped')`
- Loading state: `'Loading cards...'` -> `t('study.loadingCards')`
- No cards: `'No cards available'` -> `t('study.noCardsTitle')`
- No cards: `'Questions are being prepared. Try again in a few minutes.'` -> `t('study.noCardsSubtitle')`
- No cards button: `'Back to Dashboard'` -> `t('study.backToDashboard')`
- Ready: `'Ready to study'` -> `t('study.readyTitle')`
- Ready subtitle with interpolation: `` `Studying ${effectiveLimit} of ${session.cards.length} cards` `` -> `t('study.studyingXOfY', { limit: effectiveLimit, total: session.cards.length })`
- Ready subtitle: `` `${session.cards.length} cards available` `` -> `t('study.cardsAvailable', { count: session.cards.length })`
- Start button: `'Start'` -> `t('study.start')`
- Loading question: `'Loading question...'` -> `t('study.loadingQuestion')`
- Quit alert title: `'End Session?'` -> `t('study.endSessionTitle')`
- Quit alert body: `'Your progress will be saved.'` -> `t('study.endSessionBody')`
- Quit alert buttons: `'Continue Studying'` -> `t('study.continueStudying')`, `'End Session'` -> `t('study.endSession')`
- Prev Card button: `'Prev Card'` -> `t('study.prevCard')` (both instances -- regular and review mode)
- Next Card / Finish: `{isLastCard ? 'Finish' : 'Next Card'}` -> `{isLastCard ? t('study.finish') : t('study.nextCard')}`
- Review button: `'Back to Current Card'` -> `t('study.backToCurrentCard')`
- Completed: `'Session Complete'` -> `t('study.sessionComplete')`
- Completed subtitle: `'You studied all available cards'` -> `t('study.studiedAllCards')`
- Completed button: `'Back to Dashboard'` -> `t('study.backToDashboard')`

IMPORTANT: The beforeRemove Alert.alert calls t() inside the event handler callback. Since the handler is inside useEffect, it captures `t` from the render scope. Add `t` to the useEffect dependency array alongside `navigation` and `session.state`.

**StudySummaryScreen.tsx:**
- Import `useI18n` and destructure `const { t } = useI18n()`
- `'Session Complete!'` -> `t('summary.title')`
- `'Score'` -> `t('summary.score')`
- `'Correct'` -> `t('summary.correct')`
- `'Incorrect'` -> `t('summary.incorrect')`
- `'Skipped'` -> `t('summary.skipped')`
- `'Time'` -> `t('summary.time')`
- `'Return to Dashboard'` -> `t('summary.returnToDashboard')`
  </action>
  <verify>
  - `pnpm --filter @lumio/android exec -- npx tsc --noEmit` passes
  - No hardcoded English strings remain in StudyScreen or StudySummaryScreen
  - In Italiano mode: start a study session and verify all states show Italian text (ready screen, studying, quit alert, summary)
  - Card content and quiz question text remain in their original language (I18N-04 verified)
  </verify>
  <done>StudyScreen (all 4 states + header + bottom bars) and StudySummaryScreen use t() for every user-visible string. Imperative Alert.alert strings called via t() inside handlers. Card content untouched (I18N-04).</done>
</task>

</tasks>

<verification>
1. TypeScript compiles: `pnpm --filter @lumio/android exec -- npx tsc --noEmit`
2. Switch to Italiano, navigate through every screen -- all UI text is in Italian
3. Start a study session: ready screen, skip toast, quit alert, navigation buttons, summary -- all Italian
4. Card content and quiz questions remain in their original language
5. Switch back to English -- all strings revert immediately
</verification>

<success_criteria>
- All 5 screen files import useI18n and use t() for every user-visible string
- Zero hardcoded English text remains in screen files (except code-level constants like route names)
- Dynamic strings use i18n-js %{variable} interpolation (counts, names)
- Imperative calls (Toast.show, Alert.alert) invoke t() at call time inside handlers
- Card content and AI-generated questions are NOT translated (I18N-04)
</success_criteria>

<output>
After completion, create `.planning/phases/09-internationalization/09-02-SUMMARY.md`
</output>
