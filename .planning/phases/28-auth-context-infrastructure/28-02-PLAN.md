---
phase: 28-auth-context-infrastructure
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/android/i18n/en.ts
  - apps/android/i18n/it.ts
autonomous: true
requirements: [INFRA-06]

must_haves:
  truths:
    - "All auth-related UI strings are available in English"
    - "All auth-related UI strings are available in Italian"
    - "Italian translations use informal tu tone"
    - "i18n keys are organized under auth.login, auth.signup, auth.reset, auth.updatePassword namespaces"
    - "Error messages guide the user to the next action"
  artifacts:
    - path: "apps/android/i18n/en.ts"
      provides: "English auth translations under auth.* namespace"
      contains: "auth:"
    - path: "apps/android/i18n/it.ts"
      provides: "Italian auth translations matching EN structure"
      contains: "auth:"
  key_links:
    - from: "apps/android/i18n/it.ts"
      to: "apps/android/i18n/en.ts"
      via: "DeepStringify<typeof en> type constraint"
      pattern: "Translations"
---

<objective>
Add approximately 30 i18n keys for email auth screens (login, signup, reset password, update password) in both English and Italian.

Purpose: Phase 29-30 screens will need translated strings for all auth UI elements. Adding them now (infrastructure phase) means screens can reference i18n keys immediately without context fragmentation. The DeepStringify type constraint ensures IT matches EN structure at compile time.

Output: ~30 new keys under auth.login.*, auth.signup.*, auth.reset.*, auth.updatePassword.* namespaces in both en.ts and it.ts.
</objective>

<execution_context>
@/home/toto/.claude/get-shit-done/workflows/execute-plan.md
@/home/toto/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-auth-context-infrastructure/28-CONTEXT.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From apps/android/i18n/en.ts:
```typescript
// en.ts is the source of truth. Object exported as `const`.
// DeepStringify<typeof en> type enforces IT has the exact same nested key structure.
const en = {
  common: { ... },
  login: { ... },
  // ... other namespaces
} as const;

export default en;

type DeepStringify<T> = {
  [K in keyof T]: T[K] extends Record<string, unknown>
    ? DeepStringify<T[K]>
    : string;
};

export type Translations = DeepStringify<typeof en>;
```

From apps/android/i18n/it.ts:
```typescript
import type { Translations } from './en';

const it: Translations = {
  // Must match exact structure of en
};

export default it;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add auth i18n keys to EN and IT translation files</name>
  <files>apps/android/i18n/en.ts, apps/android/i18n/it.ts</files>
  <action>
Add an `auth` namespace to both translation files with four sub-namespaces: login, signup, reset, updatePassword.

**In en.ts — add `auth` property to the `en` object (before the closing `} as const`):**

```typescript
auth: {
  login: {
    emailLabel: 'Email',
    passwordLabel: 'Password',
    signIn: 'Sign in',
    forgotPassword: 'Forgot password?',
    noAccount: "Don't have an account?",
    signUpLink: 'Sign up',
    invalidCredentials: 'Invalid email or password. Please try again.',
    emailNotConfirmed: 'Please verify your email before signing in.',
    or: 'or',
  },
  signup: {
    title: 'Create Account',
    emailLabel: 'Email',
    passwordLabel: 'Password',
    confirmPasswordLabel: 'Confirm password',
    signUp: 'Sign up',
    hasAccount: 'Already have an account?',
    signInLink: 'Sign in',
    emailExists: 'That email is already registered. Try signing in instead.',
    weakPassword: 'Password is too weak. Use at least 6 characters.',
    checkEmail: 'Check your email',
    verificationSent: 'We sent a verification code to %{email}',
    passwordMismatch: 'Passwords do not match.',
  },
  reset: {
    title: 'Reset Password',
    emailLabel: 'Email',
    sendCode: 'Send reset code',
    backToLogin: 'Back to sign in',
    codeSent: 'Reset code sent',
    codeSentDescription: 'Check your email for the reset code.',
    userNotFound: 'No account found with that email.',
    rateLimited: 'Too many attempts. Please try again later.',
  },
  updatePassword: {
    title: 'Set New Password',
    newPasswordLabel: 'New password',
    confirmPasswordLabel: 'Confirm new password',
    update: 'Update password',
    success: 'Password updated',
    successDescription: 'Your password has been updated successfully.',
    continue: 'Continue',
    passwordMismatch: 'Passwords do not match.',
    samePassword: 'New password must be different from the current one.',
  },
},
```

**In it.ts — add matching `auth` property with Italian translations (informal "tu" tone per CONTEXT decision):**

```typescript
auth: {
  login: {
    emailLabel: 'Email',
    passwordLabel: 'Password',
    signIn: 'Accedi',
    forgotPassword: 'Password dimenticata?',
    noAccount: 'Non hai un account?',
    signUpLink: 'Registrati',
    invalidCredentials: 'Email o password non validi. Riprova.',
    emailNotConfirmed: 'Verifica la tua email prima di accedere.',
    or: 'oppure',
  },
  signup: {
    title: 'Crea Account',
    emailLabel: 'Email',
    passwordLabel: 'Password',
    confirmPasswordLabel: 'Conferma password',
    signUp: 'Registrati',
    hasAccount: 'Hai già un account?',
    signInLink: 'Accedi',
    emailExists: 'Questa email è già registrata. Prova ad accedere.',
    weakPassword: 'La password è troppo debole. Usa almeno 6 caratteri.',
    checkEmail: 'Controlla la tua email',
    verificationSent: 'Abbiamo inviato un codice di verifica a %{email}',
    passwordMismatch: 'Le password non corrispondono.',
  },
  reset: {
    title: 'Reimposta Password',
    emailLabel: 'Email',
    sendCode: 'Invia codice di reset',
    backToLogin: 'Torna al login',
    codeSent: 'Codice di reset inviato',
    codeSentDescription: 'Controlla la tua email per il codice di reset.',
    userNotFound: 'Nessun account trovato con questa email.',
    rateLimited: 'Troppi tentativi. Riprova più tardi.',
  },
  updatePassword: {
    title: 'Imposta Nuova Password',
    newPasswordLabel: 'Nuova password',
    confirmPasswordLabel: 'Conferma nuova password',
    update: 'Aggiorna password',
    success: 'Password aggiornata',
    successDescription: 'La tua password è stata aggiornata con successo.',
    continue: 'Continua',
    passwordMismatch: 'Le password non corrispondono.',
    samePassword: 'La nuova password deve essere diversa dalla precedente.',
  },
},
```

**Key design decisions (from CONTEXT.md):**
- Keys organized by screen: auth.login.*, auth.signup.*, auth.reset.*, auth.updatePassword.*
- Error messages guide user to next action (e.g., "Try signing in instead")
- Italian uses informal "tu" tone (e.g., "Inserisci la tua email" not "Inserisca la Sua email")
- `or`/`oppure` key added for the login screen separator between Google and email auth
- `rateLimited` key added for Supabase's `over_email_send_rate_limit` error code

**Placement:** Add the `auth` property after the last existing top-level key (before `navigation` or at the end, maintaining alphabetical order is not required — place after existing keys for clarity).

**Verification:** The DeepStringify<typeof en> type on IT file enforces structural parity. If IT is missing a key, TypeScript will error.
  </action>
  <verify>
    <automated>cd /home/toto/scm-projects/lumio && pnpm --filter @lumio/android exec -- npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>
Both en.ts and it.ts contain auth.login.*, auth.signup.*, auth.reset.*, auth.updatePassword.* namespaces with ~30 keys each. TypeScript compiles without errors (DeepStringify enforces EN/IT structural parity).
  </done>
</task>

</tasks>

<verification>
1. TypeScript check passes: `pnpm --filter @lumio/android exec -- npx tsc --noEmit`
2. en.ts contains `auth:` with login, signup, reset, updatePassword sub-objects
3. it.ts contains matching `auth:` structure with Italian translations
4. IT translations use informal "tu" tone (e.g., "Controlla la tua email" not "Controlli la Sua email")
5. Error messages include guidance (e.g., "Try signing in instead" / "Prova ad accedere")
6. `or`/`oppure` separator key exists for login screen layout
</verification>

<success_criteria>
- ~30 new i18n keys under auth.* namespace in both EN and IT
- TypeScript compiles cleanly (IT structure matches EN)
- Italian translations use informal "tu" tone throughout
- Error messages guide user to corrective action
</success_criteria>

<output>
After completion, create `.planning/phases/28-auth-context-infrastructure/28-02-SUMMARY.md`
</output>
