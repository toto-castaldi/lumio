---
phase: 12-dashboard-repo-bugfixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/android/screens/DashboardScreen.tsx
  - apps/android/screens/StudyScreen.tsx
  - apps/android/components/RepoListItem.tsx
autonomous: true

must_haves:
  truths:
    - "After completing a study session and returning to the dashboard, the 'Last studied' stat card shows a relative timestamp (e.g., 'Just now', '2m ago') instead of 'Non ancora'"
    - "Public repositories display a globe icon next to the name, private repositories display a lock icon"
  artifacts:
    - path: "apps/android/screens/DashboardScreen.tsx"
      provides: "Dashboard with AsyncStorage-based last studied timestamp"
      contains: "AsyncStorage"
    - path: "apps/android/components/RepoListItem.tsx"
      provides: "Repository list item with visibility icon for both public and private repos"
      contains: "globe-outline"
  key_links:
    - from: "apps/android/screens/DashboardScreen.tsx"
      to: "AsyncStorage"
      via: "store/retrieve lastStudiedAt timestamp"
      pattern: "AsyncStorage\\.getItem.*lastStudiedAt"
    - from: "apps/android/screens/StudyScreen.tsx"
      to: "AsyncStorage"
      via: "save timestamp on session completion"
      pattern: "AsyncStorage\\.setItem.*lastStudiedAt"
---

<objective>
Fix the two data display bugs on the dashboard and repository list screens.

Purpose: BUG-01 -- the dashboard always shows "Non ancora" because it queries a `study_sessions` table that does not exist in the database. BUG-02 -- public repos show no visibility indicator while private repos show a lock; public repos should show a globe/open icon.

Output: Working dashboard "last studied" display using AsyncStorage, and correct visibility icons on the repository list.
</objective>

<execution_context>
@/home/toto/.claude/get-shit-done/workflows/execute-plan.md
@/home/toto/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@apps/android/screens/DashboardScreen.tsx
@apps/android/screens/StudyScreen.tsx
@apps/android/components/RepoListItem.tsx
@apps/android/i18n/en.ts
@apps/android/i18n/it.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix dashboard "last studied" using AsyncStorage persistence</name>
  <files>
    apps/android/screens/DashboardScreen.tsx
    apps/android/screens/StudyScreen.tsx
  </files>
  <action>
The `study_sessions` table does not exist in the database, so the direct Supabase query on DashboardScreen.tsx (lines 78-88) always returns no data. Fix this by using AsyncStorage to persist the last study session timestamp.

**In DashboardScreen.tsx:**
1. Add `import AsyncStorage from '@react-native-async-storage/async-storage';`
2. Replace the entire `study_sessions` query block (lines 77-88) with an AsyncStorage read:
   ```
   const stored = await AsyncStorage.getItem('@lumio/lastStudiedAt');
   setLastStudied(stored);
   ```
3. Remove the `getSupabaseClient` import since it is no longer needed (getUserStats is still needed from @lumio/core).

**In StudyScreen.tsx:**
1. Add `import AsyncStorage from '@react-native-async-storage/async-storage';`
2. In the `useEffect` that detects `session.state === 'completed'` (around line 48-62), add a line BEFORE the navigation.replace call to save the current timestamp:
   ```
   await AsyncStorage.setItem('@lumio/lastStudiedAt', new Date().toISOString());
   ```
   Make the effect callback async or use `.then()` / fire-and-forget pattern. The navigation should still happen immediately -- do NOT await the AsyncStorage write before navigating.

Use the key `@lumio/lastStudiedAt` consistently in both files.
  </action>
  <verify>
1. `pnpm --filter @lumio/android exec -- npx tsc --noEmit` passes without errors
2. Grep for `study_sessions` in DashboardScreen.tsx returns no matches (removed)
3. Grep for `@lumio/lastStudiedAt` returns matches in both DashboardScreen.tsx and StudyScreen.tsx
  </verify>
  <done>
After completing a study session and navigating to the summary then back to dashboard, the "Last studied" stat card shows a relative timestamp (e.g., "Just now") instead of "Non ancora". The timestamp persists across app restarts.
  </done>
</task>

<task type="auto">
  <name>Task 2: Fix repository visibility icon for public repos</name>
  <files>
    apps/android/components/RepoListItem.tsx
    apps/android/i18n/en.ts
    apps/android/i18n/it.ts
  </files>
  <action>
Currently, RepoListItem shows a lock-closed icon only for private repos (`repo.isPrivate === true`) and shows NO icon for public repos. The fix: always show a visibility icon -- globe-outline for public, lock-closed for private.

**In RepoListItem.tsx:**
Replace the conditional rendering block (lines 71-78):
```tsx
{repo.isPrivate && (
  <Ionicons
    name="lock-closed"
    size={14}
    color={colors.textSecondary}
    style={styles.lockIcon}
  />
)}
```

With an always-present icon that changes based on visibility:
```tsx
<Ionicons
  name={repo.isPrivate ? "lock-closed" : "globe-outline"}
  size={14}
  color={colors.textSecondary}
  style={styles.lockIcon}
/>
```

This ensures:
- Private repos: lock-closed icon (same as before)
- Public repos: globe-outline icon (new, was previously missing)

Rename the `lockIcon` style key to `visibilityIcon` for clarity (update both the JSX and the StyleSheet). The style values remain the same (`marginLeft: 6`).

**In i18n/en.ts and i18n/it.ts:**
No translation changes needed -- the icon has no text label. If there are any aria/accessibility labels referencing "private" on the icon, update them to be conditional. (Check if there are any -- if not, skip this step.)
  </action>
  <verify>
1. `pnpm --filter @lumio/android exec -- npx tsc --noEmit` passes without errors
2. Grep for `globe-outline` in RepoListItem.tsx returns a match
3. Grep for `visibilityIcon` in RepoListItem.tsx returns matches in both JSX and StyleSheet
  </verify>
  <done>
Public repositories display a globe-outline icon next to the name. Private repositories display a lock-closed icon. Every repository always has a visible icon indicating its visibility status.
  </done>
</task>

</tasks>

<verification>
1. TypeScript check passes: `pnpm --filter @lumio/android exec -- npx tsc --noEmit`
2. No references to the non-existent `study_sessions` table remain in the codebase
3. RepoListItem always renders a visibility icon (globe or lock) regardless of `isPrivate` value
4. AsyncStorage key `@lumio/lastStudiedAt` is written on session completion and read on dashboard mount
</verification>

<success_criteria>
- BUG-01: Dashboard "Last studied" stat card shows correct relative timestamp after completing a study session
- BUG-02: Public repos show globe-outline icon, private repos show lock-closed icon
- TypeScript compiles without errors
</success_criteria>

<output>
After completion, create `.planning/phases/12-dashboard-repo-bugfixes/12-01-SUMMARY.md`
</output>
