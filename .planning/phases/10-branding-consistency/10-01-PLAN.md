---
phase: 10-branding-consistency
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/android/assets/icon.png
  - apps/android/assets/adaptive-icon.png
  - apps/android/assets/splash-icon.png
autonomous: true

must_haves:
  truths:
    - "The Android launcher icon shows the Lumio tri-color pie logo, not the default Expo placeholder"
    - "The adaptive icon foreground shows the Lumio logo centered on a transparent background"
    - "The splash screen shows the Lumio logo"
  artifacts:
    - path: "apps/android/assets/icon.png"
      provides: "1024x1024 Lumio logo for standard Android icon"
    - path: "apps/android/assets/adaptive-icon.png"
      provides: "1024x1024 Lumio logo foreground for Android adaptive icon"
    - path: "apps/android/assets/splash-icon.png"
      provides: "1024x1024 Lumio logo for splash screen"
  key_links:
    - from: "apps/android/app.json"
      to: "apps/android/assets/icon.png"
      via: "expo.icon config field"
      pattern: "icon.*icon\\.png"
    - from: "apps/android/app.json"
      to: "apps/android/assets/adaptive-icon.png"
      via: "expo.android.adaptiveIcon.foregroundImage config field"
      pattern: "foregroundImage.*adaptive-icon\\.png"
---

<objective>
Replace the default Expo placeholder icons with Lumio-branded launcher icons generated from the project SVG source files.

Purpose: BRAND-01 -- the Android home screen and app drawer must show the Lumio logo, not the generic Expo circles.
Output: Three PNG files (icon.png, adaptive-icon.png, splash-icon.png) all showing the Lumio tri-color pie logo.
</objective>

<execution_context>
@/home/toto/.claude/get-shit-done/workflows/execute-plan.md
@/home/toto/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@logo.svg
@logo-circle.svg
@apps/android/app.json
@apps/android/assets/icon.png
@apps/android/assets/adaptive-icon.png
</context>

<tasks>

<task type="auto">
  <name>Task 1: Generate Lumio launcher icon PNGs from SVG source</name>
  <files>
    apps/android/assets/icon.png
    apps/android/assets/adaptive-icon.png
    apps/android/assets/splash-icon.png
  </files>
  <action>
Generate three 1024x1024 PNG files from the SVG source files using ImageMagick:

1. **icon.png** (standard launcher icon) -- Use `logo-circle.svg` (has white circle background). The white circle will serve as the icon's visible background. Render at 1024x1024 with the logo centered. Use a square viewBox approach (the SVG viewBox is `0 0 400 400` which is already square). Omit the signature line element (line at y=330) by creating a temp SVG without it. Command pattern:
   - Create temp SVG from logo-circle.svg with the `<line>` element removed
   - `convert -background none -density 300 temp.svg -resize 1024x1024 -gravity center -extent 1024x1024 apps/android/assets/icon.png`

2. **adaptive-icon.png** (adaptive icon foreground) -- Use `logo.svg` (no background circle -- transparent). The adaptive icon system adds its own background via `app.json` `backgroundColor: "#ffffff"`. Render at 1024x1024. The logo should be centered and sized to fit within the safe zone (~66% of the full area per Android adaptive icon guidelines, so the logo should occupy roughly 680x680 pixels centered in 1024x1024). Omit signature line. Command pattern:
   - Create temp SVG from logo.svg with the `<line>` element removed
   - `convert -background none -density 300 temp.svg -resize 680x680 -gravity center -extent 1024x1024 apps/android/assets/adaptive-icon.png`

3. **splash-icon.png** -- Same as icon.png (use logo-circle.svg variant with white background, 1024x1024). Copy icon.png to splash-icon.png or generate separately.

After generation, verify:
- All three files are PNG, 1024x1024
- icon.png has the white circle background (not transparent)
- adaptive-icon.png has transparent background

No changes to `app.json` needed -- it already references these file paths correctly.
  </action>
  <verify>
Run `file apps/android/assets/icon.png apps/android/assets/adaptive-icon.png apps/android/assets/splash-icon.png` and confirm all are `PNG image data, 1024 x 1024`. Visually confirm (via `identify -verbose`) that icon.png is not the default Expo gray circles pattern (channel statistics should show multi-color data, not near-uniform gray).
  </verify>
  <done>
All three PNG files at 1024x1024 showing the Lumio tri-color pie logo. icon.png and splash-icon.png have white circle background. adaptive-icon.png has transparent background with logo in safe zone.
  </done>
</task>

</tasks>

<verification>
1. `file apps/android/assets/icon.png` shows `PNG image data, 1024 x 1024`
2. `file apps/android/assets/adaptive-icon.png` shows `PNG image data, 1024 x 1024`
3. `file apps/android/assets/splash-icon.png` shows `PNG image data, 1024 x 1024`
4. `identify apps/android/assets/adaptive-icon.png` shows alpha channel present (transparent background)
5. All PNGs show colored pixels (not uniform gray like default Expo icon)
</verification>

<success_criteria>
The three Expo icon asset files contain the Lumio tri-color pie logo at 1024x1024. The app.json already references these paths, so no config change is needed. After a native rebuild (`expo prebuild --clean`), the launcher will show the Lumio logo.
</success_criteria>

<output>
After completion, create `.planning/phases/10-branding-consistency/10-01-SUMMARY.md`
</output>
