---
phase: 10-branding-consistency
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/android/screens/LoginScreen.tsx
  - apps/android/navigation/MainNavigator.tsx
autonomous: true

must_haves:
  truths:
    - "The Login screen displays the Lumio logo image with the text 'Lumio' visible below it"
    - "The Dashboard header displays the Lumio logo image with the text 'Lumio' visible next to it"
    - "Both logo+text presentations adapt to dark and light theme"
  artifacts:
    - path: "apps/android/screens/LoginScreen.tsx"
      provides: "Login screen with logo Image + 'Lumio' Text, themed"
      contains: "Lumio"
    - path: "apps/android/navigation/MainNavigator.tsx"
      provides: "Dashboard header with logo Image + 'Lumio' Text, themed"
      contains: "Lumio"
  key_links:
    - from: "apps/android/screens/LoginScreen.tsx"
      to: "apps/android/assets/logo-login.png"
      via: "require('../assets/logo-login.png')"
      pattern: "require.*logo-login"
    - from: "apps/android/navigation/MainNavigator.tsx"
      to: "apps/android/assets/logo-header.png"
      via: "require('../assets/logo-header.png')"
      pattern: "require.*logo-header"
    - from: "apps/android/screens/LoginScreen.tsx"
      to: "apps/android/lib/theme.ts"
      via: "useTheme() hook for text color"
      pattern: "colors\\.text"
    - from: "apps/android/navigation/MainNavigator.tsx"
      to: "apps/android/lib/theme.ts"
      via: "headerTintColor for themed text"
      pattern: "headerTintColor"
---

<objective>
Add the "Lumio" text alongside the logo image on both the Login screen and Dashboard header, with dark/light theme adaptation.

Purpose: BRAND-02 and BRAND-03 -- the word "Lumio" must be visible text on both the Login and Dashboard screens, rendered by the app (not baked into the image). The logo is the hero on Login, and a compact brand mark + name in the Dashboard header.
Output: Updated LoginScreen.tsx and MainNavigator.tsx with logo + text, themed.
</objective>

<execution_context>
@/home/toto/.claude/get-shit-done/workflows/execute-plan.md
@/home/toto/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@apps/android/screens/LoginScreen.tsx
@apps/android/navigation/MainNavigator.tsx
@apps/android/lib/theme.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add "Lumio" text to Login screen below logo</name>
  <files>apps/android/screens/LoginScreen.tsx</files>
  <action>
Update LoginScreen.tsx to add a "Lumio" Text component below the logo Image, before the tagline.

Current layout:
```
Image (logo-login.png, 128x128)
Text (tagline)
```

Target layout:
```
Image (logo-login.png, 128x128)
Text "Lumio" (large, bold, themed)
Text (tagline)
```

Implementation:
1. Below the `<Image ... logo-login.png>` and before the tagline `<Text>`, add:
   ```tsx
   <Text style={[styles.logoText, { color: colors.text }]}>Lumio</Text>
   ```
2. Add `logoText` style to the StyleSheet:
   ```ts
   logoText: {
     fontSize: 32,
     fontWeight: 'bold',
     marginBottom: 8,
   },
   ```
3. The `colors.text` is `#333333` in light mode and `#f9fafb` in dark mode, providing automatic theme adaptation.
4. Do NOT change the logo Image itself (it stays as-is at 128x128).

The "Lumio" text uses `colors.text` for dark/light theme. No need for separate dark/light logo variants -- the tri-color pie logo works on both backgrounds.
  </action>
  <verify>
Run `pnpm --filter @lumio/android exec -- npx tsc --noEmit` to confirm TypeScript compiles. Grep LoginScreen.tsx for the string "Lumio" to confirm the text is present.
  </verify>
  <done>
LoginScreen shows: logo image (128px), "Lumio" text (32px bold, themed color), tagline text. The "Lumio" text adapts to dark/light theme via colors.text.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add "Lumio" text to Dashboard header next to logo</name>
  <files>apps/android/navigation/MainNavigator.tsx</files>
  <action>
Update MainNavigator.tsx Dashboard tab's `headerTitle` to render both the logo Image and "Lumio" Text in a horizontal row.

Current headerTitle:
```tsx
headerTitle: () => (
  <Image
    source={require('../assets/logo-header.png')}
    style={{ width: 28, height: 28 }}
    resizeMode="contain"
    accessibilityLabel="Lumio"
  />
),
```

Target headerTitle:
```tsx
headerTitle: () => (
  <View style={{ flexDirection: 'row', alignItems: 'center' }}>
    <Image
      source={require('../assets/logo-header.png')}
      style={{ width: 28, height: 28, marginRight: 8 }}
      resizeMode="contain"
    />
    <Text style={{ color: '#ffffff', fontSize: 18, fontWeight: 'bold' }}>
      Lumio
    </Text>
  </View>
),
```

Implementation details:
1. Import `View` and `Text` from 'react-native' (add to existing import alongside Image).
2. Wrap the Image in a `<View style={{ flexDirection: 'row', alignItems: 'center' }}>` with a `<Text>` after it.
3. The header background is `colors.primary` (blue) and `headerTintColor` is `#ffffff`. The "Lumio" text should use `#ffffff` to match the header text color convention. This works for both light and dark themes because the header itself stays `colors.primary` background in both.
4. Add `marginRight: 8` to the Image style for spacing.
5. Move `accessibilityLabel="Lumio"` to the outer View (or keep on Image -- either works since the Text says "Lumio" explicitly).

No additional style changes needed. The header background color (`colors.primary`) already adapts to light/dark theme from the `screenOptions`.
  </action>
  <verify>
Run `pnpm --filter @lumio/android exec -- npx tsc --noEmit` to confirm TypeScript compiles. Grep MainNavigator.tsx for the string "Lumio" in a Text component to confirm it's present.
  </verify>
  <done>
Dashboard header shows: logo image (28px) + "Lumio" text (18px bold, white) in a horizontal row. Both light and dark themes show the same header appearance (primary-colored background with white text).
  </done>
</task>

</tasks>

<verification>
1. `pnpm --filter @lumio/android exec -- npx tsc --noEmit` passes
2. LoginScreen.tsx contains a `<Text>` with "Lumio" and `colors.text` styling
3. MainNavigator.tsx Dashboard headerTitle contains a `<Text>` with "Lumio"
4. No hardcoded colors on Login "Lumio" text (uses theme `colors.text`)
5. Dashboard header text uses `#ffffff` matching `headerTintColor`
</verification>

<success_criteria>
The Login screen displays the Lumio logo image (128px) with "Lumio" text (32px bold) below it, adapting to dark/light theme. The Dashboard header displays the logo image (28px) with "Lumio" text (18px bold, white) next to it. TypeScript compiles without errors.
</success_criteria>

<output>
After completion, create `.planning/phases/10-branding-consistency/10-02-SUMMARY.md`
</output>
