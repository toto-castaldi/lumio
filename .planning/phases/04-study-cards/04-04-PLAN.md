---
phase: 04-study-cards
plan: 04
type: execute
wave: 3
depends_on: ["04-02", "04-03"]
files_modified:
  - apps/android/screens/StudySummaryScreen.tsx
  - apps/android/screens/StudyScreen.tsx
  - apps/android/navigation/AppNavigator.tsx
  - apps/android/screens/DashboardScreen.tsx
autonomous: false

must_haves:
  truths:
    - "Session ends with summary screen showing score, correct/incorrect count, time spent"
    - "Summary screen has 'Return to Dashboard' button that navigates back"
    - "Study session completion navigates to summary screen automatically"
    - "Study button on dashboard is disabled when no cards available"
    - "Native rebuild produces working APK with all study features"
  artifacts:
    - path: "apps/android/screens/StudySummaryScreen.tsx"
      provides: "End-of-session summary with statistics"
      exports: ["StudySummaryScreen"]
  key_links:
    - from: "apps/android/screens/StudyScreen.tsx"
      to: "apps/android/screens/StudySummaryScreen.tsx"
      via: "navigation.replace('StudySummary', { ...stats })"
      pattern: "navigate.*StudySummary|replace.*StudySummary"
    - from: "apps/android/screens/StudySummaryScreen.tsx"
      to: "apps/android/navigation/AppNavigator.tsx"
      via: "navigation.navigate('Main') to return to dashboard"
      pattern: "navigate.*Main"
    - from: "apps/android/navigation/AppNavigator.tsx"
      to: "apps/android/screens/StudySummaryScreen.tsx"
      via: "Stack.Screen component registration"
      pattern: "Stack\\.Screen.*StudySummary"
---

<objective>
Create the StudySummaryScreen (end-of-session statistics), wire the complete study flow from dashboard to summary and back, replace the AppNavigator StudySummary placeholder, and perform the native rebuild needed for expo-haptics and react-native-webview.

Purpose: This completes the study lifecycle: Dashboard -> Study -> Summary -> Dashboard. The summary screen (per locked decision) shows score, correct/incorrect count, and time spent. The native rebuild is required because expo-haptics and react-native-webview contain native modules that must be compiled into the APK.

Output: Complete working study flow from end to end, verified on physical device.
</objective>

<execution_context>
@/home/toto/.claude/get-shit-done/workflows/execute-plan.md
@/home/toto/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-study-cards/04-01-SUMMARY.md
@.planning/phases/04-study-cards/04-02-SUMMARY.md
@.planning/phases/04-study-cards/04-03-SUMMARY.md
@apps/android/navigation/AppNavigator.tsx
@apps/android/screens/DashboardScreen.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create StudySummaryScreen and wire complete study flow</name>
  <files>
    apps/android/screens/StudySummaryScreen.tsx
    apps/android/screens/StudyScreen.tsx
    apps/android/navigation/AppNavigator.tsx
    apps/android/screens/DashboardScreen.tsx
  </files>
  <action>
1. Create `StudySummaryScreen.tsx`:
   - Receives route params: `{ totalCards, correctCount, incorrectCount, skippedCount, timeSpentSeconds }` from `RootStackParamList['StudySummary']`
   - Layout (centered, theme-aware):
     - Large result icon at top: green checkmark circle (like PWA completed state)
     - Title: "Session Complete!"
     - Stats grid (2x2 or vertical list):
       - Correct: green text, count with checkmark icon
       - Incorrect: red text, count with X icon
       - Skipped: gray text, count with skip icon
       - Time: clock icon, formatted as "Xm Ys" (convert seconds to minutes:seconds)
     - Score percentage: `Math.round((correctCount / totalCards) * 100)%` displayed prominently
     - "Return to Dashboard" button (primary color, full width)
   - On "Return to Dashboard" press: `navigation.navigate('Main')` to pop back to the tab navigator
   - Prevent back gesture/button from going to StudyScreen (there's nothing to go back to): use `navigation.setOptions({ gestureEnabled: false })` or handle `beforeRemove` to redirect to Main
   - Use `useTheme()` for all colors

2. Update StudyScreen.tsx:
   - When `session.state === 'completed'`, navigate to StudySummary screen:
     ```typescript
     navigation.replace('StudySummary', {
       totalCards: session.answeredCards.length + session.skippedCount,
       correctCount: session.answeredCards.filter(a => a.isCorrect).length,
       incorrectCount: session.answeredCards.filter(a => !a.isCorrect).length,
       skippedCount: session.skippedCount,
       timeSpentSeconds: Math.floor((Date.now() - session.startedAt.getTime()) / 1000),
     });
     ```
   - Use `navigation.replace` (not navigate) so the user can't go back to a completed study session
   - Add a `useEffect` that watches `session.state` and triggers navigation when it becomes 'completed'

3. Update AppNavigator.tsx:
   - Replace the StudySummary placeholder component with the real `StudySummaryScreen` import
   - Ensure `StudySummary` screen has `gestureEnabled: false` in its options (prevent swipe-back to study)

4. Verify DashboardScreen study button:
   - Ensure `isStudyDisabled` still works correctly: disabled when `cardCount === 0` (per locked decision: study button disabled when no cards available)
   - The button should be properly navigating to 'Study' (this should already be done from plan 01, just verify)

5. Native rebuild:
   - Run: `cd /home/toto/scm-projects/lumio/apps/android && npx expo prebuild --platform android --clean`
   - Run: `cd /home/toto/scm-projects/lumio/apps/android/android && ./gradlew assembleDebug`
   - Output APK path: `apps/android/android/app/build/outputs/apk/debug/app-debug.apk`
   - NOTE: If the build fails, troubleshoot the error. Common issues: missing peer deps, babel config changes needed. Do NOT install react-native-reanimated unless absolutely necessary.
  </action>
  <verify>
1. TypeScript check: `cd /home/toto/scm-projects/lumio && pnpm --filter @lumio/android exec -- npx tsc --noEmit`
2. APK builds: verify `apps/android/android/app/build/outputs/apk/debug/app-debug.apk` exists
3. Install on device: `adb install -r /home/toto/scm-projects/lumio/apps/android/android/app/build/outputs/apk/debug/app-debug.apk`
  </verify>
  <done>
StudySummaryScreen displays session statistics (correct/incorrect/skipped/time/score%). Study completion auto-navigates to summary. Summary has "Return to Dashboard" button. AppNavigator has all real screen components. APK is rebuilt with native modules (expo-haptics, react-native-webview) and installed on device.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify complete study flow on device</name>
  <what-built>
Complete study session flow: Dashboard -> Study -> Quiz with haptics -> Card content view -> Summary -> Dashboard.

Features to verify:
- Quiz with 4 answer options and inline feedback
- Haptic feedback (correct = light tap, incorrect = heavier buzz)
- Explanation panel with vote buttons (like/dislike)
- Progress bar
- Skip card functionality
- Swipe left/right for card navigation
- "View Card" modal with markdown/code/LaTeX rendering
- Quit confirmation on back press
- Summary screen with statistics
- Study button disabled when no cards
  </what-built>
  <how-to-verify>
1. Start Metro: `cd /home/toto/scm-projects/lumio/apps/android && npx expo start --dev-client --clear`
2. Open app on FP5 device
3. Verify study button is disabled if no cards exist (or enabled if cards exist)
4. Tap "Start Study Session" on Dashboard
5. Verify loading state appears, then "Ready to study" screen
6. Tap "Start" -- verify quiz question appears with 4 options
7. Tap an answer:
   - Verify haptic feedback (feel the difference between correct and incorrect)
   - Verify correct answer highlights green, wrong answer highlights red
   - Verify explanation panel appears inline with explanation text
   - Verify like/dislike vote buttons appear alongside explanation
8. Tap like or dislike -- verify it registers (button highlights)
9. Tap "Next Card" to advance
10. Try swiping left to advance, right to go back to previous card
11. Verify progress bar updates
12. Tap the eye icon to view card content:
    - Verify markdown renders (headings, lists, bold)
    - Verify code blocks have syntax highlighting (if cards have code)
    - Verify LaTeX renders (if cards have formulas)
    - Verify images load (if cards have images)
    - Try pinch-to-zoom on the card content
13. Skip a card -- verify it advances and doesn't reappear
14. Press back button during study -- verify "End session?" confirmation
15. Complete all cards or press "End Session" -- verify summary screen shows:
    - Correct count, incorrect count, skipped count
    - Time spent
    - Score percentage
16. Tap "Return to Dashboard" -- verify return to main screen
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues found</resume-signal>
</task>

</tasks>

<verification>
- TypeScript compiles: `pnpm --filter @lumio/android exec -- npx tsc --noEmit`
- APK builds and installs on FP5 device
- Complete study flow works: Dashboard -> Study -> Quiz -> Summary -> Dashboard
- All 13 success criteria from Phase 4 are met
</verification>

<success_criteria>
- StudySummaryScreen shows score, correct/incorrect count, time spent (per locked decision)
- Session completion auto-navigates to summary (not silent return, per locked decision)
- Summary "Return to Dashboard" navigates back correctly
- Study button disabled when no cards (per locked decision)
- Native rebuild succeeds with expo-haptics and react-native-webview
- Human verifies complete study flow on physical device
</success_criteria>

<output>
After completion, create `.planning/phases/04-study-cards/04-04-SUMMARY.md`
</output>
