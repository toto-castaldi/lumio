---
phase: 20-cleanup-legacy-versioning
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/TECHNICAL-ARCHITECTURE.md
autonomous: true
requirements: [CLEAN-01, CLEAN-02, CLEAN-03, CLEAN-04, CLEAN-05, CLEAN-06]
gap_closure: true

must_haves:
  truths:
    - "docs/TECHNICAL-ARCHITECTURE.md does not describe auto-release, release-please, or conventional commits as active systems"
    - "The project structure diagram does not list .release-please-manifest.json"
    - "The CI/CD description matches the actual current workflow (lint, typecheck, deploy — no auto-release)"
  artifacts:
    - path: "docs/TECHNICAL-ARCHITECTURE.md"
      provides: "Architecture documentation with accurate CI/CD and versioning descriptions"
  key_links:
    - from: "docs/TECHNICAL-ARCHITECTURE.md"
      to: ".github/workflows/ci-deploy.yml"
      via: "CI/CD description matches actual workflow"
      pattern: "no 'auto-release' in docs/TECHNICAL-ARCHITECTURE.md"
---

<objective>
Update docs/TECHNICAL-ARCHITECTURE.md to remove stale references to the legacy auto-release versioning system that was removed in Plans 01 and 02.

Purpose: Verification found that this file still describes the removed auto-release CI job, lists .release-please-manifest.json in the project structure, and references conventional commits — all as if they are active. This is the only remaining documentation gap in Phase 20.
Output: Accurate architecture documentation that reflects the current state of the project.
</objective>

<execution_context>
@/home/toto/.claude/get-shit-done/workflows/execute-plan.md
@/home/toto/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-cleanup-legacy-versioning/20-VERIFICATION.md
@docs/TECHNICAL-ARCHITECTURE.md
@.github/workflows/ci-deploy.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update TECHNICAL-ARCHITECTURE.md to remove legacy versioning references</name>
  <files>docs/TECHNICAL-ARCHITECTURE.md</files>
  <action>
    Edit `docs/TECHNICAL-ARCHITECTURE.md` to fix three stale areas identified by verification:

    1. **Line 148 — Project structure tree CI comment:**
       Change: `ci-deploy.yml       # CI/CD unificato (auto-release, lint, typecheck, deploy)`
       To: `ci-deploy.yml       # CI/CD unificato (lint, typecheck, deploy)`
       Reason: auto-release job no longer exists.

    2. **Line 150 — Project structure tree file listing:**
       Delete the entire line: `├── .release-please-manifest.json # Versione corrente (tracking)`
       Reason: This file was deleted in Plan 01. It no longer exists in the repository.

    3. **Lines 843-893 — Section "6.3 Versioning con Auto-Release":**
       Replace the entire section (from `### 6.3 Versioning con Auto-Release` through the conventional commits table and the final `> Per dettagli completi` line) with a short updated section that accurately describes the current state:

       ```markdown
       ### 6.3 Versioning

       Il sistema di versioning legacy (auto-release, release-please, conventional commits) è stato rimosso nella fase 20 di v1.7.

       **Stato attuale:**
       - La versione è un valore statico in `packages/shared/src/version.ts`
       - Il CI non crea tag Git o commit di release
       - Il build APK usa un placeholder `0.0.0` per versionName

       La fase 21 collegherà `.planning/STATE.md` come unica fonte di verità per la versione.

       > Per dettagli vedere [docs/VERSIONING.md](./VERSIONING.md)
       ```

    Do NOT modify any other sections of the file. The rest of the architecture documentation is accurate.
  </action>
  <verify>
    Run `grep "auto-release" docs/TECHNICAL-ARCHITECTURE.md` — should return nothing.
    Run `grep "release-please-manifest" docs/TECHNICAL-ARCHITECTURE.md` — should return nothing.
    Run `grep "Conventional Commits" docs/TECHNICAL-ARCHITECTURE.md` — should return nothing.
    Run `grep "6.3 Versioning" docs/TECHNICAL-ARCHITECTURE.md` — should return the new section header.
    Run `grep "feat:" docs/TECHNICAL-ARCHITECTURE.md` — should return nothing (conventional commit types table removed).
  </verify>
  <done>docs/TECHNICAL-ARCHITECTURE.md accurately reflects the current project state: no auto-release in CI description, no .release-please-manifest.json in project tree, and section 6.3 describes the current versioning status instead of the removed system.</done>
</task>

</tasks>

<verification>
1. `grep -cE "auto-release|release-please-manifest|Conventional Commits|feat:|fix:.*PATCH" docs/TECHNICAL-ARCHITECTURE.md` — returns 0
2. `grep "CI/CD unificato" docs/TECHNICAL-ARCHITECTURE.md` — shows "(lint, typecheck, deploy)" without "auto-release"
3. `grep "6.3 Versioning" docs/TECHNICAL-ARCHITECTURE.md` — section exists with updated content
4. No other sections of the file were modified
</verification>

<success_criteria>
- docs/TECHNICAL-ARCHITECTURE.md has zero references to auto-release as an active system
- Project structure diagram does not list .release-please-manifest.json
- Section 6.3 accurately describes the current versioning state (static placeholder, Phase 21 will wire STATE.md)
- CI/CD comment in project tree matches actual workflow
</success_criteria>

<output>
After completion, create `.planning/phases/20-cleanup-legacy-versioning/20-03-SUMMARY.md`
</output>
