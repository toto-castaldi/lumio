---
phase: 20-cleanup-legacy-versioning
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/workflows/ci-deploy.yml
  - packages/shared/src/version.ts
  - docs/VERSIONING.md
  - README.md
autonomous: true
requirements: [CLEAN-05]

must_haves:
  truths:
    - "CI workflow runs without any auto-release job or git tag creation step"
    - "CI deploy jobs no longer depend on auto-release outputs"
    - "build-apk job builds with a hardcoded fallback version (not from auto-release)"
    - "All 53 version git tags are deleted from both local and remote"
    - "Documentation no longer references legacy versioning tools"
  artifacts:
    - path: ".github/workflows/ci-deploy.yml"
      provides: "CI/CD workflow without auto-release job"
    - path: "packages/shared/src/version.ts"
      provides: "Version file without release-please comments"
    - path: "docs/VERSIONING.md"
      provides: "Versioning doc updated to reflect removal of old tooling (placeholder for Phase 22)"
    - path: "README.md"
      provides: "README without references to CHANGELOG or release-please"
  key_links:
    - from: ".github/workflows/ci-deploy.yml"
      to: "build-apk job"
      via: "versionName no longer from auto-release output"
      pattern: "no needs.auto-release"
---

<objective>
Remove the auto-release CI job, delete all git tags (local + remote), and update documentation to remove references to legacy versioning tools.

Purpose: The CI pipeline must not create version bumps, git tags, or release commits. All 53 existing version tags are cleaned from local and remote. Documentation is updated to reflect the removal (Phase 22 will add the new GSD versioning docs).
Output: Clean CI workflow and documentation with no legacy versioning references.
</objective>

<execution_context>
@/home/toto/.claude/get-shit-done/workflows/execute-plan.md
@/home/toto/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-cleanup-legacy-versioning/20-CONTEXT.md
@.github/workflows/ci-deploy.yml
@packages/shared/src/version.ts
@docs/VERSIONING.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove auto-release job from CI and fix job dependencies</name>
  <files>
    .github/workflows/ci-deploy.yml
  </files>
  <action>
    Edit `.github/workflows/ci-deploy.yml` to remove the entire auto-release infrastructure:

    1. **Delete the `auto-release` job** entirely (lines 17-96 approximately) — the whole job block including its outputs, steps, git config, bump version script, tag creation, and push.

    2. **Update `permissions`:** Change `contents: write` to `contents: read` since we no longer push tags/commits from CI. WAIT — build-apk uploads to GitHub Release via softprops/action-gh-release which needs `contents: write`. Keep `contents: write` for now.

    3. **Fix `lint-and-typecheck` job:**
       - Remove `needs: auto-release` — this job should have no dependencies
       - Keep the `if` condition but remove the `always() &&` prefix since there's no longer a potentially-skipped dependency. Simplify to: `if: github.event_name == 'push' && !startsWith(github.event.head_commit.message, 'docs:') && !startsWith(github.event.head_commit.message, 'docs(') || github.event_name == 'pull_request'`
       - Actually, the original condition was `always() && !startsWith(...)` to run even if auto-release was skipped. Without auto-release, simplify to just the docs skip: `if: "!startsWith(github.event.head_commit.message, 'docs:') && !startsWith(github.event.head_commit.message, 'docs(')"`. But this must also handle PR events where head_commit might not exist. Safest: keep it simple and just skip docs-only pushes.

    4. **Fix `build-apk` job:**
       - Change `needs: [lint-and-typecheck, auto-release]` to `needs: [lint-and-typecheck]`
       - Update the `if` condition: remove `needs.auto-release.outputs.released == 'true'` check. The new condition should be: `if: github.ref == 'refs/heads/main' && github.event_name == 'push' && needs.lint-and-typecheck.result == 'success'`
       - Remove `fetch-depth: 0` (no longer need full history for tags)
       - Remove the "Pull latest changes" step (no longer needed since auto-release doesn't push commits)
       - **versionName:** Replace `${{ needs.auto-release.outputs.new_version }}` with a hardcoded placeholder `"0.0.0"`. Phase 21 will wire this to STATE.md. Use: `-PversionName=0.0.0`
       - **tag_name in Upload to GitHub Release:** Remove the entire "Upload to GitHub Release" step since we no longer create tags. The APK upload depended on the auto-release tag. Without tags, there's no GitHub Release to upload to. Keep the APK build but remove the upload step.
       - Actually, per user decision: "Keep GitHub Releases if any exist (only delete the git tags themselves)". So we should NOT create new releases either. Remove the upload step entirely.

    5. **Fix `deploy-landing` job:**
       - Change `needs: [lint-and-typecheck, auto-release]` to `needs: [lint-and-typecheck]`
       - Update `if`: remove `always() &&` since no skippable dependency. Result: `if: github.ref == 'refs/heads/main' && github.event_name == 'push' && needs.lint-and-typecheck.result == 'success'`

    6. **Fix `deploy-migrations` job:**
       - Change `needs: [lint-and-typecheck, auto-release]` to `needs: [lint-and-typecheck]`
       - Update `if` same pattern as deploy-landing.

    7. **Fix `deploy-functions` job:**
       - Change `needs: [deploy-migrations, auto-release]` to `needs: [deploy-migrations]`
       - Update `if`: remove `always() &&`. Result: `if: github.ref == 'refs/heads/main' && github.event_name == 'push' && needs.deploy-migrations.result == 'success'`
       - The "Extract version info" step reads from `packages/shared/src/version.ts` using grep — this is fine, keep it. Phase 21 will update how version.ts gets its value.

    8. **Remove the `chore(release)` skip** in lint-and-typecheck `if` — the condition `!startsWith(github.event.head_commit.message, 'chore(release)')` was to skip CI on auto-release commits. Since auto-release no longer exists, this check is dead code. Remove it.
  </action>
  <verify>
    Run `grep -c "auto-release" .github/workflows/ci-deploy.yml` — should return 0.
    Run `grep "needs.*auto-release" .github/workflows/ci-deploy.yml` — should return nothing.
    Run `grep "released" .github/workflows/ci-deploy.yml` — should return nothing.
    Run `grep "chore(release)" .github/workflows/ci-deploy.yml` — should return nothing.
    Run `grep "git tag" .github/workflows/ci-deploy.yml` — should return nothing.
    Validate YAML syntax: `python3 -c "import yaml; yaml.safe_load(open('.github/workflows/ci-deploy.yml'))"` — should succeed.
  </verify>
  <done>CI workflow has no auto-release job, no tag creation, no release commit. All downstream jobs depend only on lint-and-typecheck or deploy-migrations. YAML is valid.</done>
</task>

<task type="auto">
  <name>Task 2: Delete git tags and update docs</name>
  <files>
    packages/shared/src/version.ts
    docs/VERSIONING.md
    README.md
  </files>
  <action>
    **Part A: Delete all 53 version git tags (local + remote)**

    Delete all local tags:
    ```bash
    git tag -l | xargs git tag -d
    ```

    Delete all remote tags (push empty refs):
    ```bash
    git tag -l | xargs -I{} git push origin --delete {} 2>/dev/null || true
    ```

    NOTE: The tags must be read BEFORE deleting locally. So first capture the list, delete remote, then delete local:
    ```bash
    TAGS=$(git tag -l)
    for tag in $TAGS; do git push origin --delete "$tag" 2>/dev/null || true; done
    git tag -d $TAGS
    ```

    Per user decision: Keep GitHub Releases — only delete the git tags themselves.

    **Part B: Update `packages/shared/src/version.ts`**

    Update the file comment to remove the release-please reference. Change:
    ```
    * Questa versione viene aggiornata automaticamente da release-please.
    * NON modificare manualmente.
    ```
    To:
    ```
    * Version will be derived from .planning/STATE.md in Phase 21.
    * For now, this is a static placeholder.
    ```

    Keep the VERSION constant, BUILD_INFO, and exported functions unchanged — Phase 21 will update the value and mechanism.

    **Part C: Update `docs/VERSIONING.md`**

    Replace the entire content with a short placeholder that acknowledges the old system was removed and the new one is coming in Phase 21-22. Content:

    ```markdown
    # Versioning Strategy

    > Legacy versioning infrastructure (husky, commitlint, commitizen, release-please, auto-release CI, git tags) was removed in v1.7 Phase 20.

    ## Current State

    The version is currently a static value in `packages/shared/src/version.ts`.

    Phase 21 will wire `STATE.md` milestone field as the single source of truth for versioning.
    Phase 22 will update all public surfaces (landing page, edge function, this documentation).

    ## Version File

    ```
    packages/shared/src/version.ts  ← static placeholder (Phase 21 wires to STATE.md)
    ```

    ---
    *Updated: Phase 20 cleanup*
    ```

    **Part D: Update `README.md`**

    1. Remove the line: `- [Versioning](./docs/VERSIONING.md) - release-please, conventional commits`
       Replace with: `- [Versioning](./docs/VERSIONING.md) - version strategy`
    2. Change the line: `Leggi CHANGELOG.md, CLAUDE.md, README.md e tutta la cartella DOCS. Ora sei pronto.`
       Replace with: `Leggi CLAUDE.md, README.md e tutta la cartella DOCS. Ora sei pronto.`
       (Remove CHANGELOG.md reference since the file no longer exists)
  </action>
  <verify>
    Run `git tag -l` — should return nothing (0 tags).
    Run `grep "release-please" packages/shared/src/version.ts` — should return nothing.
    Run `grep -E "release-please|commitlint|commitizen|husky|auto-release" docs/VERSIONING.md` — should return nothing (except the historical note).
    Run `grep "CHANGELOG.md" README.md` — should return nothing.
  </verify>
  <done>All 53 git tags deleted from local and remote. version.ts comment updated. docs/VERSIONING.md replaced with placeholder. README.md cleaned of legacy references.</done>
</task>

</tasks>

<verification>
1. `grep -rE "auto-release|chore\(release\)" .github/workflows/` — returns nothing
2. `git tag -l | wc -l` — returns 0
3. `python3 -c "import yaml; yaml.safe_load(open('.github/workflows/ci-deploy.yml'))"` — succeeds
4. `grep -E "release-please|commitlint|husky" docs/VERSIONING.md README.md packages/shared/src/version.ts` — returns nothing (except the historical removal note in VERSIONING.md)
5. CI workflow YAML is valid and all jobs have correct dependencies
</verification>

<success_criteria>
- auto-release job completely removed from CI workflow
- No CI step creates git tags or release commits
- All 53 git tags deleted from local and remote
- build-apk uses hardcoded version placeholder (Phase 21 wires STATE.md)
- Documentation updated to remove legacy versioning references
- version.ts comment no longer mentions release-please
</success_criteria>

<output>
After completion, create `.planning/phases/20-cleanup-legacy-versioning/20-02-SUMMARY.md`
</output>
