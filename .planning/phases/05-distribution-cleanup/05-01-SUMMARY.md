---
phase: 05-distribution-cleanup
plan: 01
subsystem: infra
tags: [android, signing, gradle, expo-config-plugin, keystore, versioning, ci-cd]

# Dependency graph
requires:
  - phase: 04-study-cards
    provides: "Complete Android app ready for release builds"
provides:
  - "Release signing config via Expo config plugin"
  - "Dynamic versionCode/versionName injection via Gradle properties"
  - "GitHub Secrets for keystore credentials"
affects: [05-03, 05-04]

# Tech tracking
tech-stack:
  added: []
  patterns: ["Expo config plugin for build.gradle modifications (withAppBuildGradle)"]

key-files:
  created:
    - apps/android/plugins/withReleaseSigning.js
  modified:
    - apps/android/app.json

key-decisions:
  - "SIGN-01: Expo config plugin instead of direct build.gradle edit (android/ is gitignored, regenerated by prebuild)"
  - "SIGN-02: Debug keystore fallbacks in release signingConfig for local dev without env vars"

patterns-established:
  - "Expo config plugin pattern: plugins/ directory for build.gradle customizations that survive prebuild"

# Metrics
duration: 3min
completed: 2026-02-08
---

# Phase 5 Plan 01: Release Signing Summary

**Expo config plugin for Android release signing with env-var keystore credentials and dynamic Gradle versioning**

## Performance

- **Duration:** 3 min (code task) + human checkpoint (keystore generation and GitHub Secrets setup)
- **Started:** 2026-02-08T10:15:00Z
- **Completed:** 2026-02-08T10:50:00Z
- **Tasks:** 2 (1 auto + 1 checkpoint:human-action)
- **Files modified:** 2

## Accomplishments
- Release signing config that reads keystore path and passwords from environment variables, with debug keystore fallbacks for local development
- Dynamic versionCode and versionName injectable via Gradle properties (`-PversionCode=N -PversionName=X.Y.Z`) for CI builds
- Developer generated release keystore and configured all required GitHub Secrets (ANDROID_KEYSTORE_BASE64, ANDROID_KEYSTORE_PASSWORD, ANDROID_KEY_ALIAS, ANDROID_KEY_PASSWORD)

## Task Commits

Each task was committed atomically:

1. **Task 1: Add release signing config and dynamic versioning** - `208e11d` (feat)
2. **Task 2: Generate release keystore and configure GitHub Secrets** - checkpoint:human-action (no commit, developer performed manual setup)

## Files Created/Modified
- `apps/android/plugins/withReleaseSigning.js` - Expo config plugin that injects release signingConfig and dynamic versioning into build.gradle during prebuild
- `apps/android/app.json` - Registered withReleaseSigning plugin in plugins array

## Decisions Made
- **SIGN-01:** Used Expo config plugin (`withAppBuildGradle`) instead of direct build.gradle edit because `apps/android/android/` is gitignored and regenerated by `npx expo prebuild`. The plugin is the source of truth that survives prebuild cycles.
- **SIGN-02:** Release signingConfig falls back to debug keystore values (`'android'` password, `'androiddebugkey'` alias) when env vars are not set, allowing local dev builds without keystore configuration.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] Used Expo config plugin instead of direct build.gradle edit**
- **Found during:** Task 1 (release signing config)
- **Issue:** Plan specified editing `apps/android/android/app/build.gradle` directly, but `apps/android/android/` is gitignored and regenerated by Expo prebuild -- direct edits would be lost
- **Fix:** Created `apps/android/plugins/withReleaseSigning.js` as an Expo config plugin that modifies build.gradle at prebuild time, registered it in `app.json`
- **Files modified:** `apps/android/plugins/withReleaseSigning.js` (created), `apps/android/app.json` (modified)
- **Verification:** Plugin registered in app.json plugins array, code correctly patches signingConfigs, buildTypes, and version fields
- **Committed in:** `208e11d` (Task 1 commit)

---

**Total deviations:** 1 auto-fixed (1 blocking)
**Impact on plan:** Essential adaptation -- without config plugin, changes would be lost on every prebuild. Same functional outcome achieved through Expo-compatible mechanism.

## Issues Encountered
None beyond the deviation noted above.

## User Setup Required

Developer completed the following manual steps (confirmed "secrets configured"):
- Generated release keystore with `keytool`
- Base64-encoded keystore for GitHub Secrets
- Configured GitHub Secrets: ANDROID_KEYSTORE_BASE64, ANDROID_KEYSTORE_PASSWORD, ANDROID_KEY_ALIAS, ANDROID_KEY_PASSWORD
- Verified SUPABASE_URL, SUPABASE_ANON_KEY, EXPO_PUBLIC_GOOGLE_WEB_CLIENT_ID secrets exist

## Next Phase Readiness
- Release signing config is ready for the CI/CD pipeline (plan 05-03)
- Dynamic versioning enables CI to inject version numbers at build time
- All GitHub Secrets are configured for automated APK builds

## Self-Check: PASSED

- FOUND: `apps/android/plugins/withReleaseSigning.js`
- FOUND: `apps/android/app.json`
- FOUND: commit `208e11d`

---
*Phase: 05-distribution-cleanup*
*Completed: 2026-02-08*
