---
phase: 05-distribution-cleanup
plan: 04
type: execute
wave: 3
depends_on: ["05-03"]
files_modified:
  - package.json
autonomous: false

must_haves:
  truths:
    - "apps/web directory no longer exists in the repository"
    - "apps/mobile directory no longer exists in the repository"
    - "Root package.json has no web/mobile scripts"
    - "pnpm install succeeds after cleanup"
    - "pnpm typecheck succeeds after cleanup"
  artifacts:
    - path: "package.json"
      provides: "Root package.json without web/mobile scripts"
  key_links:
    - from: "package.json"
      to: "pnpm-workspace.yaml"
      via: "workspace packages glob"
      pattern: "apps/\\*"
---

<objective>
Remove legacy apps/web and apps/mobile directories, clean up root package.json scripts, and verify the monorepo still works correctly. Also clean up the server-side /var/www/lumio-mobile directory.

Purpose: Complete the migration from dual PWA apps to a single native Android app by removing all legacy code. This is the final cleanup step that makes the codebase match its new architecture.

Output: Clean repository with only apps/android and apps/landing, working pnpm workspace, passing typecheck.
</objective>

<execution_context>
@/home/toto/.claude/get-shit-done/workflows/execute-plan.md
@/home/toto/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@package.json
@pnpm-workspace.yaml
@.planning/phases/05-distribution-cleanup/05-RESEARCH.md
@.planning/phases/05-distribution-cleanup/05-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove apps/web and apps/mobile, update root package.json</name>
  <files>package.json</files>
  <action>
1. **Remove apps/web directory:**
```bash
rm -rf apps/web
```

2. **Remove apps/mobile directory:**
```bash
rm -rf apps/mobile
```

3. **Update root package.json** -- remove these scripts:
- `"dev:web"` line
- `"dev:mobile"` line
- `"build:web"` line
- `"build:mobile"` line

Keep all other scripts unchanged (build:packages, lint, typecheck, test, clean, supabase:*, commit, prepare).

4. **Verify pnpm-workspace.yaml** -- it uses `apps/*` glob, which will now match only `apps/android` and `apps/landing`. No change needed.

5. **Run verification:**
```bash
pnpm install
pnpm build:packages
pnpm typecheck
```

All commands must succeed. `pnpm typecheck` runs `-r typecheck` which will skip web/mobile since they no longer exist. It should still typecheck packages/shared, packages/core, and apps/android.

IMPORTANT: Do NOT modify pnpm-workspace.yaml. Do NOT modify packages/core or packages/shared (keep them separate per RESEARCH.md recommendation). Do NOT remove any Edge Functions.
  </action>
  <verify>
- `test ! -d apps/web && echo "web removed"` prints "web removed"
- `test ! -d apps/mobile && echo "mobile removed"` prints "mobile removed"
- `grep -c 'dev:web\|dev:mobile\|build:web\|build:mobile' package.json` returns 0
- `pnpm install` succeeds
- `pnpm build:packages` succeeds
- `pnpm --filter @lumio/android exec -- npx tsc --noEmit` succeeds
  </verify>
  <done>Legacy web and mobile apps removed, root package.json cleaned up, monorepo builds and typechecks successfully</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify cleanup and server state</name>
  <what-built>
Removed apps/web and apps/mobile directories, cleaned up root package.json scripts, verified monorepo builds.

The server-side `/var/www/lumio-mobile` directory from the old mobile PWA deploy should also be cleaned up. This requires SSH access to the server.
  </what-built>
  <how-to-verify>
1. Verify no web/mobile references remain:
   ```bash
   grep -r "lumio/web\|lumio/mobile\|@lumio/web\|@lumio/mobile" --include="*.json" --include="*.yaml" --include="*.yml" . | grep -v node_modules | grep -v .git
   ```
   Should return no results.

2. Verify apps directory has only android and landing:
   ```bash
   ls apps/
   ```
   Should show: `android  landing`

3. (Optional server cleanup) SSH to server and remove old mobile PWA:
   ```bash
   ssh user@server 'rm -rf /var/www/lumio-mobile'
   ```

4. Verify the full build chain works:
   ```bash
   pnpm install && pnpm build:packages && pnpm typecheck
   ```
  </how-to-verify>
  <resume-signal>Type "cleanup verified" when checks pass (server cleanup is optional)</resume-signal>
</task>

</tasks>

<verification>
- apps/web gone, apps/mobile gone
- Root package.json has no web/mobile scripts
- pnpm-workspace.yaml unchanged (glob still works)
- packages/core and packages/shared unchanged
- All Edge Functions unchanged
- pnpm install + build:packages + typecheck all pass
</verification>

<success_criteria>
- Zero references to apps/web or apps/mobile in the codebase (outside git history)
- Monorepo builds and typechecks successfully
- apps/ contains only android/ and landing/
- CI pipeline would run successfully with the cleaned-up codebase
</success_criteria>

<output>
After completion, create `.planning/phases/05-distribution-cleanup/05-04-SUMMARY.md`
</output>
