---
phase: 49-deck-builder-navigation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/landing/index.html
  - apps/landing/styles.css
autonomous: false
requirements:
  - NAV-01
  - NAV-02

must_haves:
  truths:
    - "Visitor sees a purple 'Crea Deck' / 'Create Deck' link in the header, left of the language toggle"
    - "Visitor sees an outline purple 'Crea Deck' / 'Create Deck' button in the hero section below the Download APK button"
    - "Both navigation elements open deck.lumio.toto-castaldi.com in a new tab"
    - "Both elements switch label between IT and EN when the language toggle is clicked"
  artifacts:
    - path: "apps/landing/index.html"
      provides: "Header link and hero button HTML elements with bilingual span tags"
      contains: "deck.lumio.toto-castaldi.com"
    - path: "apps/landing/styles.css"
      provides: "Outline button styles (.btn-deck-builder) and header link styles (.header-link)"
      contains: ".btn-deck-builder"
  key_links:
    - from: "apps/landing/index.html"
      to: "https://deck.lumio.toto-castaldi.com"
      via: "anchor href with target=_blank"
      pattern: 'href="https://deck.lumio.toto-castaldi.com"'
    - from: "apps/landing/index.html"
      to: "apps/landing/styles.css"
      via: "CSS classes .btn-deck-builder and .header-link"
      pattern: "btn-deck-builder|header-link"
    - from: "apps/landing/index.html"
      to: "apps/landing/script.js"
      via: "existing <span lang> pattern — no JS changes needed"
      pattern: '<span lang="en">Create Deck</span>'
---

<objective>
Add two navigation elements to the Lumio landing page that link to the deck builder web app: a header link and a hero CTA button.

Purpose: Visitors can discover and reach the deck builder directly from the landing page, increasing deck builder adoption.
Output: Updated index.html with new navigation elements and updated styles.css with new CSS classes.
</objective>

<execution_context>
@/home/toto/.claude/get-shit-done/workflows/execute-plan.md
@/home/toto/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/49-deck-builder-navigation/49-CONTEXT.md

@apps/landing/index.html
@apps/landing/styles.css
@apps/landing/script.js

<interfaces>
<!-- Current landing page structure the executor needs to understand -->

From apps/landing/index.html:
```html
<!-- Header: flex row with logo left, lang-toggle right -->
<header class="header">
  <a href="/" class="logo" aria-label="Lumio home">...</a>
  <button id="lang-toggle" class="lang-toggle" aria-label="Switch language">IT</button>
</header>

<!-- Hero: centered section with h1, subtitle, single .btn-download -->
<section class="hero">
  <h1>...</h1>
  <p class="subtitle">...</p>
  <a href="https://github.com/toto-castaldi/lumio/releases/latest/download/lumio.apk" class="btn-download">
    <span lang="en">Download APK</span>
    <span lang="it">Scarica APK</span>
  </a>
</section>
```

From apps/landing/styles.css:
```css
/* Header: flex, space-between, centers vertically */
.header {
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: 1.5rem 0;
}

/* Existing button style to share sizing/border-radius with new outline button */
.btn-download {
  display: inline-block;
  background-color: var(--amber-primary);
  color: #1A1230;
  font-size: 1.1rem;
  font-weight: 700;
  padding: 0.9rem 2.5rem;
  border-radius: 12px;
  text-decoration: none;
  transition: transform 0.15s, box-shadow 0.15s;
  box-shadow: 0 4px 14px rgba(245, 158, 11, 0.3);
}

/* CSS custom properties available */
:root {
  --purple-primary: #7C3AED;
  --amber-primary: #F59E0B;
  --bg-dark: #0F0A1A;
  --text-light: #F5F3FF;
  --card-bg: #1A1230;
  --text-secondary: #A78BFA;
}

/* Bilingual toggle pattern: <span lang="en/it"> elements hidden by CSS */
html[lang="en"] [lang="it"] { display: none !important; }
html[lang="it"] [lang="en"] { display: none !important; }

/* Responsive breakpoints: 768px (desktop), 480px (small screens) */
```

From apps/landing/script.js:
```js
// Language toggle already handles ALL <span lang> elements automatically.
// Adding new <span lang="en"> and <span lang="it"> elements requires NO JS changes.
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add header link and hero CTA button for deck builder</name>
  <files>apps/landing/index.html, apps/landing/styles.css</files>

  <read_first>
    apps/landing/index.html
    apps/landing/styles.css
    apps/landing/script.js
  </read_first>

  <action>
**In apps/landing/index.html:**

1. **Header modification** — Replace the current header structure to group the new link and lang toggle on the right side. The header currently has logo left and lang-toggle right with `justify-content: space-between`. Add a wrapper div for right-side items:

Replace:
```html
<button id="lang-toggle" class="lang-toggle" aria-label="Switch language">IT</button>
```

With:
```html
<div class="header-right">
  <a href="https://deck.lumio.toto-castaldi.com" target="_blank" rel="noopener" class="header-link">
    <span lang="en">Create Deck</span>
    <span lang="it">Crea Deck</span>
  </a>
  <button id="lang-toggle" class="lang-toggle" aria-label="Switch language">IT</button>
</div>
```

2. **Hero modification** — Wrap the existing `.btn-download` and the new button in a `.hero-buttons` container for stacked layout. Replace the single `<a>` button:

Replace the current hero `.btn-download` anchor with:
```html
<div class="hero-buttons">
  <a href="https://github.com/toto-castaldi/lumio/releases/latest/download/lumio.apk" class="btn-download">
    <span lang="en">Download APK</span>
    <span lang="it">Scarica APK</span>
  </a>
  <a href="https://deck.lumio.toto-castaldi.com" target="_blank" rel="noopener" class="btn-deck-builder">
    <span lang="en">Create Deck</span>
    <span lang="it">Crea Deck</span>
  </a>
</div>
```

**In apps/landing/styles.css:**

3. **Add `.header-right` class** — After the `.lang-toggle:hover` rule block (after line 89):

```css
.header-right {
  display: flex;
  align-items: center;
  gap: 1rem;
}
```

4. **Add `.header-link` class** — Immediately after `.header-right`:

```css
.header-link {
  color: var(--purple-primary);
  text-decoration: none;
  font-size: 0.95rem;
  font-weight: 600;
  transition: text-decoration 0.2s;
}

.header-link:hover {
  text-decoration: underline;
}
```

5. **Add `.hero-buttons` class** — After the `.btn-download:hover` rule block (after line 133):

```css
.hero-buttons {
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 1rem;
}
```

6. **Add `.btn-deck-builder` class** — Immediately after `.hero-buttons`:

```css
.btn-deck-builder {
  display: inline-block;
  background-color: transparent;
  color: var(--purple-primary);
  font-size: 1.1rem;
  font-weight: 700;
  padding: 0.9rem 2.5rem;
  border-radius: 12px;
  border: 2px solid var(--purple-primary);
  text-decoration: none;
  transition: background-color 0.2s, color 0.2s, transform 0.15s;
}

.btn-deck-builder:hover {
  background-color: var(--purple-primary);
  color: var(--text-light);
  transform: translateY(-2px);
}
```

7. **Responsive: small screens (480px)** — Inside the existing `@media (max-width: 480px)` block, add:

```css
.hero-buttons {
  width: 100%;
}

.btn-download,
.btn-deck-builder {
  width: 100%;
  text-align: center;
}
```

No changes to `script.js` — the existing language toggle automatically handles all `<span lang>` elements.
  </action>

  <verify>
    <automated>cd /home/toto/scm-projects/lumio && grep -q 'deck.lumio.toto-castaldi.com' apps/landing/index.html && grep -q 'header-link' apps/landing/index.html && grep -q 'btn-deck-builder' apps/landing/index.html && grep -q 'hero-buttons' apps/landing/index.html && grep -q 'header-right' apps/landing/index.html && grep -q '.btn-deck-builder' apps/landing/styles.css && grep -q '.header-link' apps/landing/styles.css && grep -q '.hero-buttons' apps/landing/styles.css && grep -q '.header-right' apps/landing/styles.css && grep -c 'deck.lumio.toto-castaldi.com' apps/landing/index.html | grep -q '2' && echo "ALL CHECKS PASSED"</automated>
  </verify>

  <acceptance_criteria>
    - apps/landing/index.html contains exactly 2 occurrences of `deck.lumio.toto-castaldi.com` (one in header, one in hero)
    - apps/landing/index.html contains `class="header-link"` on an anchor element
    - apps/landing/index.html contains `class="btn-deck-builder"` on an anchor element
    - apps/landing/index.html contains `class="hero-buttons"` wrapping both buttons
    - apps/landing/index.html contains `class="header-right"` wrapping header-link and lang-toggle
    - apps/landing/index.html contains `target="_blank" rel="noopener"` on both deck builder links
    - apps/landing/index.html contains `<span lang="en">Create Deck</span>` (exactly 2 times, header + hero)
    - apps/landing/index.html contains `<span lang="it">Crea Deck</span>` (exactly 2 times, header + hero)
    - apps/landing/styles.css contains `.header-right` with `display: flex`
    - apps/landing/styles.css contains `.header-link` with `color: var(--purple-primary)`
    - apps/landing/styles.css contains `.btn-deck-builder` with `border: 2px solid var(--purple-primary)`
    - apps/landing/styles.css contains `.hero-buttons` with `flex-direction: column`
    - apps/landing/styles.css contains `.btn-deck-builder` with `background-color: transparent`
    - apps/landing/script.js is NOT modified (no changes needed)
  </acceptance_criteria>

  <done>
    Header contains a purple "Create Deck" / "Crea Deck" link left of the language toggle.
    Hero section contains a stacked button pair: amber "Download APK" on top, purple outline "Crea Deck" below.
    Both links point to https://deck.lumio.toto-castaldi.com with target="_blank" rel="noopener".
    Both links use bilingual <span lang> pattern for IT/EN switching.
    Responsive: buttons go full-width on mobile (480px breakpoint).
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of landing page navigation</name>
  <files>apps/landing/index.html</files>

  <action>
    Present the landing page to the user for visual verification. Open `apps/landing/index.html` in a browser. No code changes in this task — purely visual/functional confirmation of Task 1 output.
  </action>

  <verify>
    <automated>cd /home/toto/scm-projects/lumio && test -f apps/landing/index.html && grep -q 'header-link' apps/landing/index.html && grep -q 'btn-deck-builder' apps/landing/index.html && echo "FILES READY FOR VISUAL CHECK"</automated>
  </verify>

  <acceptance_criteria>
    - User confirms header link is visible and positioned left of the IT/EN toggle
    - User confirms hero section shows two stacked buttons (amber Download APK on top, purple outline Crea Deck below)
    - User confirms both links open https://deck.lumio.toto-castaldi.com in a new tab
    - User confirms language toggle switches both new elements between IT and EN
  </acceptance_criteria>

  <what-built>
    Two new navigation elements on the landing page linking to the deck builder:
    1. A purple text link "Crea Deck" / "Create Deck" in the header, left of the IT/EN toggle
    2. A purple outline button "Crea Deck" / "Create Deck" in the hero section, stacked below the amber "Download APK" button
  </what-built>

  <how-to-verify>
    1. Open the landing page in a browser (serve locally or open `apps/landing/index.html` directly)
    2. Verify the header shows "Crea Deck" link in purple text, positioned left of the IT/EN toggle button
    3. Click the IT/EN toggle — verify the header link switches between "Create Deck" and "Crea Deck"
    4. Verify the hero section shows two stacked buttons: amber "Download APK" (or "Scarica APK") on top, purple outline "Crea Deck" (or "Create Deck") below
    5. Click the "Crea Deck" header link — verify it opens https://deck.lumio.toto-castaldi.com in a new tab
    6. Click the "Crea Deck" hero button — verify it also opens https://deck.lumio.toto-castaldi.com in a new tab
    7. Resize browser to mobile width (below 480px) — verify both hero buttons go full-width and remain stacked
    8. Hover over the purple outline button — verify it fills purple with white text
  </how-to-verify>

  <done>User has visually confirmed both navigation elements look correct and function as expected.</done>

  <resume-signal>Type "approved" or describe any visual/functional issues</resume-signal>
</task>

</tasks>

<verification>
Phase-level checks after all tasks complete:
1. Landing page header has a visible "Crea Deck" / "Create Deck" link that opens the deck builder in a new tab
2. Landing page hero has a secondary outline button "Crea Deck" / "Create Deck" below the existing "Download APK" button
3. Language toggle switches both new elements between IT and EN
4. No regressions to existing landing page elements (logo, download button, features, screenshots, footer)
</verification>

<success_criteria>
- NAV-01: Header link to deck builder is visible and functional
- NAV-02: Hero CTA button for deck builder is visible and functional
- Both links open https://deck.lumio.toto-castaldi.com in a new tab
- Bilingual labels work with existing language toggle
- Responsive layout maintained on mobile
</success_criteria>

<output>
After completion, create `.planning/phases/49-deck-builder-navigation/49-01-SUMMARY.md`
</output>
