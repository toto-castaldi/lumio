---
phase: 02-auth-navigation
plan: 03
type: execute
wave: 3
depends_on: ["02-01", "02-02"]
files_modified:
  - apps/android/navigation/AppNavigator.tsx
  - apps/android/navigation/AuthNavigator.tsx
  - apps/android/navigation/MainNavigator.tsx
  - apps/android/components/StudyFAB.tsx
  - apps/android/App.tsx
autonomous: true

must_haves:
  truths:
    - "App shows LoginScreen when state is 'logged_out'"
    - "App shows MainNavigator (tabs) when state is 'ready'"
    - "App shows loading indicator when state is 'loading'"
    - "FAB is visible on all tab screens"
    - "Tab bar shows icons only (no labels)"
  artifacts:
    - path: "apps/android/navigation/AppNavigator.tsx"
      provides: "Root navigator switching between auth and main flows"
      exports: ["AppNavigator"]
    - path: "apps/android/navigation/AuthNavigator.tsx"
      provides: "Auth flow stack (login screen)"
      exports: ["AuthNavigator"]
    - path: "apps/android/navigation/MainNavigator.tsx"
      provides: "Main app with custom tab bar and FAB"
      exports: ["MainNavigator"]
    - path: "apps/android/components/StudyFAB.tsx"
      provides: "Floating action button for study"
      exports: ["StudyFAB"]
  key_links:
    - from: "apps/android/navigation/AppNavigator.tsx"
      to: "apps/android/contexts/AuthContext.tsx"
      via: "useAuth hook"
      pattern: "useAuth\\(\\)"
    - from: "apps/android/App.tsx"
      to: "apps/android/contexts/AuthContext.tsx"
      via: "AuthProvider wrapper"
      pattern: "<AuthProvider>"
    - from: "apps/android/navigation/MainNavigator.tsx"
      to: "apps/android/components/StudyFAB.tsx"
      via: "absolute positioned overlay"
      pattern: "StudyFAB"
---

<objective>
Create navigation structure with auth/main routing and custom tab bar with FAB.

Purpose: Implement the navigation shell that switches between login flow and main app based on auth state. FAB provides primary "Study" action accessible from all tabs.
Output: Complete navigation structure with AppNavigator -> AuthNavigator | MainNavigator, custom tab bar with icons only, FAB overlay.
</objective>

<execution_context>
@/home/toto/.claude/get-shit-done/workflows/execute-plan.md
@/home/toto/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-auth-navigation/02-CONTEXT.md
@.planning/phases/02-auth-navigation/02-RESEARCH.md
@apps/android/App.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create navigation structure (AppNavigator, AuthNavigator, MainNavigator)</name>
  <files>apps/android/navigation/AppNavigator.tsx, apps/android/navigation/AuthNavigator.tsx, apps/android/navigation/MainNavigator.tsx</files>
  <action>
    Create `apps/android/navigation/` directory with three navigators.

    **AuthNavigator.tsx:**
    - Simple stack navigator with just LoginScreen
    - screenOptions: headerShown: false (login is full screen)
    - Export AuthNavigator

    **MainNavigator.tsx:**
    - Bottom tab navigator with Dashboard, Repos, Settings tabs
    - screenOptions:
      - tabBarShowLabel: false (icons only per CONTEXT)
      - tabBarIcon: Use Ionicons from @expo/vector-icons
        - Dashboard: 'home' / 'home-outline'
        - Repos: 'folder' / 'folder-outline'
        - Settings: 'settings' / 'settings-outline'
      - tabBarActiveTintColor: '#3B82F6'
      - tabBarInactiveTintColor: '#6B7280'
      - headerShown: true
    - Wrap in View with flex:1, render StudyFAB as absolute overlay
    - Export MainNavigator

    **AppNavigator.tsx:**
    - Use useAuth() to get state
    - Render based on state:
      - 'loading': ActivityIndicator centered (blue, large)
      - 'logged_out': AuthNavigator
      - 'ready': MainNavigator
    - No NavigationContainer here (that stays in App.tsx)
    - Export AppNavigator

    **Placeholder screens (will be replaced in Task 2):**
    For now, create simple placeholder components for Dashboard, Repos, Settings in the navigation files or inline. These will be replaced by actual screens in Phase 3.

    LoginScreen placeholder: Just text "Login" centered - will be implemented in 02-04.
  </action>
  <verify>
    - Files exist:
      - `test -f apps/android/navigation/AppNavigator.tsx`
      - `test -f apps/android/navigation/AuthNavigator.tsx`
      - `test -f apps/android/navigation/MainNavigator.tsx`
    - AppNavigator uses useAuth: `grep -q "useAuth" apps/android/navigation/AppNavigator.tsx`
    - MainNavigator has icons only: `grep -q "tabBarShowLabel.*false" apps/android/navigation/MainNavigator.tsx`
    - TypeScript compiles: `pnpm --filter @lumio/android tsc --noEmit`
  </verify>
  <done>
    Navigation structure created with auth state-based routing and icons-only tab bar.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create StudyFAB component</name>
  <files>apps/android/components/StudyFAB.tsx</files>
  <action>
    Create `apps/android/components/StudyFAB.tsx` as floating action button.

    **Styling (from CONTEXT - Claude's discretion on exact values):**
    - Position: absolute, bottom 70 (above tab bar), alignSelf 'center'
    - Size: 56x56 with borderRadius 28 (circle)
    - Background: '#3B82F6' (blue)
    - Icon: Ionicons 'play' in white, size 24
    - Elevation: 8 (Android shadow)
    - Shadow for iOS: shadowColor black, offset {0,2}, opacity 0.25, radius 4

    **Behavior:**
    - onPress: For now, console.log('Study pressed') - actual navigation to Study screen in Phase 4
    - Use TouchableOpacity for press feedback

    **Export:** Named export StudyFAB
  </action>
  <verify>
    - File exists: `test -f apps/android/components/StudyFAB.tsx`
    - Has absolute positioning: `grep -q "position.*absolute" apps/android/components/StudyFAB.tsx`
    - Has TouchableOpacity: `grep -q "TouchableOpacity" apps/android/components/StudyFAB.tsx`
    - TypeScript compiles: `pnpm --filter @lumio/android tsc --noEmit`
  </verify>
  <done>
    StudyFAB provides floating action button positioned above tab bar.
  </done>
</task>

<task type="auto">
  <name>Task 3: Update App.tsx to use AuthProvider and AppNavigator</name>
  <files>apps/android/App.tsx</files>
  <action>
    Update `apps/android/App.tsx` to integrate auth system.

    **Structure:**
    ```tsx
    <SafeAreaProvider>
      <AuthProvider>
        <NavigationContainer>
          <OfflineBanner />
          <AppNavigator />
        </NavigationContainer>
      </AuthProvider>
      <StatusBar style="light" />
    </SafeAreaProvider>
    ```

    **Changes from current:**
    - Import AuthProvider from contexts/AuthContext
    - Import AppNavigator from navigation/AppNavigator
    - Import OfflineBanner from components/OfflineBanner
    - Remove inline Tab.Navigator and screen components
    - Remove ConnectionTest import (no longer needed)
    - Wrap everything in AuthProvider (inside SafeAreaProvider)
    - OfflineBanner at top of NavigationContainer (shows above navigation when offline)
    - AppNavigator handles all navigation logic

    **Remove:**
    - DashboardScreen, RepositoryScreen, SettingsScreen inline functions
    - Tab.Navigator setup
    - ConnectionTest import and usage
  </action>
  <verify>
    - Uses AuthProvider: `grep -q "AuthProvider" apps/android/App.tsx`
    - Uses AppNavigator: `grep -q "AppNavigator" apps/android/App.tsx`
    - Uses OfflineBanner: `grep -q "OfflineBanner" apps/android/App.tsx`
    - No ConnectionTest: `! grep -q "ConnectionTest" apps/android/App.tsx`
    - TypeScript compiles: `pnpm --filter @lumio/android tsc --noEmit`
  </verify>
  <done>
    App.tsx integrates AuthProvider, OfflineBanner, and AppNavigator.
  </done>
</task>

</tasks>

<verification>
Overall plan verification:
1. Navigation files exist:
   - `test -f apps/android/navigation/AppNavigator.tsx`
   - `test -f apps/android/navigation/AuthNavigator.tsx`
   - `test -f apps/android/navigation/MainNavigator.tsx`
2. StudyFAB exists: `test -f apps/android/components/StudyFAB.tsx`
3. App.tsx uses AuthProvider: `grep "AuthProvider" apps/android/App.tsx`
4. App.tsx uses AppNavigator: `grep "AppNavigator" apps/android/App.tsx`
5. TypeScript clean: `pnpm --filter @lumio/android tsc --noEmit`
</verification>

<success_criteria>
- AppNavigator switches between AuthNavigator and MainNavigator based on auth state
- Loading state shows ActivityIndicator
- MainNavigator has icons-only tab bar with Dashboard, Repos, Settings
- StudyFAB positioned as floating button above tab bar
- OfflineBanner displays at top when offline
- App.tsx wraps everything in AuthProvider
- No TypeScript errors
</success_criteria>

<output>
After completion, create `.planning/phases/02-auth-navigation/02-03-SUMMARY.md`
</output>
