---
phase: 03-core-screens
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/android/screens/DashboardScreen.tsx
  - apps/android/components/AddRepoForm.tsx
  - apps/android/screens/ReposScreen.tsx
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "Go to Repositories button in Dashboard empty state navigates to Repos tab"
    - "PAT input field is clearly visible with proper contrast in both light and dark themes"
    - "PAT section has a dedicated Submit button and a Cancel button"
    - "PAT prompt dismisses when user cancels or changes URL"
  artifacts:
    - path: "apps/android/screens/DashboardScreen.tsx"
      provides: "Navigation from empty state to Repos tab"
      contains: "navigation.navigate"
    - path: "apps/android/components/AddRepoForm.tsx"
      provides: "Usable PAT input with submit and cancel buttons"
      contains: "onCancel"
    - path: "apps/android/screens/ReposScreen.tsx"
      provides: "PAT cancel handler and URL-change reset"
      contains: "setShowPatPrompt(false)"
  key_links:
    - from: "apps/android/screens/DashboardScreen.tsx"
      to: "MainNavigator tab 'Repos'"
      via: "useNavigation + navigate('Repos')"
      pattern: "navigation\\.navigate.*Repos"
    - from: "apps/android/components/AddRepoForm.tsx"
      to: "apps/android/screens/ReposScreen.tsx"
      via: "onCancel prop callback"
      pattern: "onCancel"
---

<objective>
Fix two UAT failures from Phase 3 verification:
1. Dashboard "Go to Repositories" button does nothing (placeholder console.log)
2. PAT input field is unusable (poor visibility, no submit/cancel buttons, never dismissed)

Purpose: Close the last two gaps before Phase 3 can be marked fully complete.
Output: Both issues resolved; Dashboard navigation works, PAT flow is usable.
</objective>

<execution_context>
@/home/toto/.claude/get-shit-done/workflows/execute-plan.md
@/home/toto/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@apps/android/screens/DashboardScreen.tsx
@apps/android/components/AddRepoForm.tsx
@apps/android/screens/ReposScreen.tsx
@apps/android/navigation/MainNavigator.tsx
@apps/android/lib/theme.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire Dashboard "Go to Repositories" navigation</name>
  <files>apps/android/screens/DashboardScreen.tsx</files>
  <action>
    In DashboardScreen.tsx:

    1. Import `useNavigation` from `@react-navigation/native` and `BottomTabNavigationProp` from `@react-navigation/bottom-tabs`.
    2. Import `MainTabParamList` from `../navigation/MainNavigator`.
    3. Inside the `DashboardScreen` component, add: `const navigation = useNavigation<BottomTabNavigationProp<MainTabParamList>>();`
    4. Replace the `onAction` callback on the EmptyState component (lines 108-111) from:
       ```
       onAction={() => {
         // Navigation to Repos tab will be wired in a later phase
         console.log('Navigate to Repos');
       }}
       ```
       to:
       ```
       onAction={() => navigation.navigate('Repos')}
       ```

    Do NOT change anything else in the file. Keep all existing imports, styles, and logic intact.
  </action>
  <verify>
    Run TypeScript check: `pnpm --filter @lumio/android exec -- npx tsc --noEmit`
    Confirm no errors related to navigation types or imports.
  </verify>
  <done>Tapping "Go to Repositories" in the Dashboard empty state navigates to the Repos tab.</done>
</task>

<task type="auto">
  <name>Task 2: Fix PAT input usability (visibility, submit, cancel, dismiss)</name>
  <files>apps/android/components/AddRepoForm.tsx, apps/android/screens/ReposScreen.tsx</files>
  <action>
    **In AddRepoForm.tsx:**

    1. Add `onCancel?: () => void` to the `AddRepoFormProps` interface.
    2. Destructure `onCancel` from props alongside the existing props.
    3. Remove `secureTextEntry` from the PAT TextInput. PATs are not passwords -- users need to see what they paste. This fixes the visibility issue.
    4. After the PAT TextInput, add a row with two buttons:
       - A "Cancel" button (outline style): calls `onCancel?.()`, also clears the local `pat` state via `setPat('')`.
       - A "Submit with Token" button (filled primary style): calls `handleSubmit()`, same as the + button does.
       Both buttons should be disabled when `isAdding` is true.
       Style the button row as `flexDirection: 'row'`, `gap: 8`, `marginTop: 12`. Each button gets `flex: 1`, `height: 44`, `borderRadius: 8`, centered text.
       - Cancel button: `borderWidth: 1`, `borderColor: colors.border`, `backgroundColor: 'transparent'`, text color `colors.text`.
       - Submit button: `backgroundColor: colors.primary`, text color `'#ffffff'`.
       Use `TouchableOpacity` for both buttons with `Text` children (no icons needed).
    5. Add the new styles to the StyleSheet: `patButtonRow`, `cancelButton`, `submitButton`, `cancelButtonText`, `submitButtonText`.

    **In ReposScreen.tsx:**

    1. Pass `onCancel={() => setShowPatPrompt(false)}` prop to the `<AddRepoForm>` component.
    2. In the `handleAddRepo` callback, in the `onChangeText` handler of the URL input -- wait, the URL input is inside AddRepoForm, not ReposScreen. Instead: AddRepoForm already clears `urlError` on text change. We need to reset `showPatPrompt` when URL changes. The cleanest approach: Add an `onUrlChange?: (url: string) => void` callback prop to AddRepoFormProps. In AddRepoForm, call `onUrlChange?.(text)` inside the URL TextInput's `onChangeText`. In ReposScreen, pass `onUrlChange={() => setShowPatPrompt(false)}` to AddRepoForm. This way, typing a new URL dismisses the PAT prompt.
    3. Update the AddRepoForm interface and component to accept and call `onUrlChange`.

    **Summary of AddRepoFormProps after changes:**
    ```typescript
    interface AddRepoFormProps {
      onAdd: (url: string, accessToken?: string) => Promise<void>;
      isAdding: boolean;
      showPatPrompt?: boolean;
      onCancel?: () => void;
      onUrlChange?: (url: string) => void;
    }
    ```

    All colors must come from `useTheme()` -- no hardcoded color values except `'#ffffff'` for text on primary-colored buttons (consistent with existing pattern in the codebase, see DARK-02 decision).
  </action>
  <verify>
    Run TypeScript check: `pnpm --filter @lumio/android exec -- npx tsc --noEmit`
    Confirm no type errors. Visually confirm in code that:
    - PAT TextInput has no secureTextEntry
    - Cancel and Submit buttons exist in the PAT section
    - onCancel and onUrlChange props are wired in ReposScreen
  </verify>
  <done>
    PAT input is visible (no secureTextEntry masking), has Submit and Cancel buttons,
    Cancel dismisses the PAT prompt, and changing the URL also dismisses it.
  </done>
</task>

</tasks>

<verification>
1. `pnpm --filter @lumio/android exec -- npx tsc --noEmit` passes with zero errors
2. DashboardScreen imports useNavigation and calls navigation.navigate('Repos')
3. AddRepoForm PAT section has no secureTextEntry, has Submit and Cancel buttons
4. ReposScreen passes onCancel and onUrlChange to AddRepoForm
5. No hardcoded colors (except #ffffff on primary backgrounds per DARK-02)
</verification>

<success_criteria>
- Dashboard "Go to Repositories" button navigates to Repos tab (no more console.log placeholder)
- PAT field text is visible while typing/pasting (not masked)
- PAT section has explicit "Submit with Token" and "Cancel" buttons
- Cancelling PAT prompt hides it; changing URL also hides it
- TypeScript compiles without errors
</success_criteria>

<output>
After completion, create `.planning/phases/03-core-screens/03-05-SUMMARY.md`
</output>
