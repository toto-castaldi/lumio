---
phase: 03-core-screens
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - apps/android/components/StatCard.tsx
  - apps/android/components/EmptyState.tsx
  - apps/android/screens/DashboardScreen.tsx
autonomous: true

must_haves:
  truths:
    - "Dashboard shows repository count in a stat card"
    - "Dashboard shows total card count in a stat card"
    - "Dashboard shows last studied timestamp (or placeholder if never studied)"
    - "Study button is prominent CTA below stat cards"
    - "Study button is disabled when card count is 0"
    - "Study button shows loading state while stats are being fetched"
    - "Pull-to-refresh works on dashboard"
    - "Dashboard has empty state when no repos exist"
    - "All colors use useTheme() for dark mode support"
  artifacts:
    - path: "apps/android/components/StatCard.tsx"
      provides: "Reusable stat tile with icon, label, value, loading skeleton"
      exports: ["StatCard"]
    - path: "apps/android/components/EmptyState.tsx"
      provides: "Reusable empty state with icon, title, subtitle, optional CTA"
      exports: ["EmptyState"]
    - path: "apps/android/screens/DashboardScreen.tsx"
      provides: "Dashboard with stat cards, study CTA, pull-to-refresh"
      exports: ["DashboardScreen"]
  key_links:
    - from: "apps/android/screens/DashboardScreen.tsx"
      to: "@lumio/core"
      via: "getUserStats() for repo and card counts"
      pattern: "getUserStats"
    - from: "apps/android/screens/DashboardScreen.tsx"
      to: "hooks/useTheme.ts"
      via: "useTheme() for dynamic colors"
      pattern: "useTheme"
    - from: "apps/android/screens/DashboardScreen.tsx"
      to: "components/StatCard.tsx"
      via: "StatCard component for each metric"
      pattern: "StatCard"
---

<objective>
Implement the Dashboard screen with stat cards (repository count, card count, last studied), a prominent Study CTA button with proper disabled/loading states, pull-to-refresh, and empty state design.

Purpose: Dashboard is the app's home screen. Users need to see their study statistics at a glance and have a clear path to start studying.
Output: Working DashboardScreen with real data from @lumio/core, reusable StatCard and EmptyState components.
</objective>

<execution_context>
@/home/toto/.claude/get-shit-done/workflows/execute-plan.md
@/home/toto/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-core-screens/03-01-SUMMARY.md
@apps/android/screens/DashboardScreen.tsx
@packages/core/src/supabase/repositories.ts
@packages/shared/src/types/index.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create StatCard and EmptyState components</name>
  <files>
    apps/android/components/StatCard.tsx
    apps/android/components/EmptyState.tsx
  </files>
  <action>
    1. Create `components/StatCard.tsx`:
       - Props: icon (Ionicons name), iconColor (string), iconBgColor (string), label (string), value (string | number), subtitle? (string), isLoading? (boolean)
       - Use useTheme() for colors.surface (card bg), colors.text (value), colors.textSecondary (label, subtitle)
       - Layout: card container with borderRadius 12, padding 16, elevation 1
         - Icon in a 40x40 rounded container (borderRadius 10) with iconBgColor background
         - Label text below icon (13px, fontWeight 500, textSecondary color)
         - Value text large (28px, bold, text color) - or skeleton view if isLoading
         - Optional subtitle below value (11px, textSecondary)
       - Skeleton: View with backgroundColor colors.border, width 40, height 28, borderRadius 4
       - Use StyleSheet.create for static styles, dynamic colors via style arrays

    2. Create `components/EmptyState.tsx`:
       - Props: icon (Ionicons name), title (string), subtitle (string), actionLabel? (string), onAction? () => void
       - Use useTheme() for colors
       - Layout: centered View with padding 32
         - Large Ionicons icon (64px, textSecondary color)
         - Title text (18px, semibold, text color, marginTop 16)
         - Subtitle text (14px, textSecondary, marginTop 8, textAlign center)
         - Optional TouchableOpacity button if actionLabel provided (primary bg, white text, borderRadius 8, paddingV 12, paddingH 24, marginTop 24)
  </action>
  <verify>
    - `pnpm --filter @lumio/android exec -- npx tsc --noEmit` compiles without errors
    - `grep "useTheme" apps/android/components/StatCard.tsx` shows theme usage
    - `grep "useTheme" apps/android/components/EmptyState.tsx` shows theme usage
  </verify>
  <done>
    StatCard renders icon + label + value with loading skeleton. EmptyState renders icon + title + subtitle + optional CTA. Both use useTheme() for dark mode.
  </done>
</task>

<task type="auto">
  <name>Task 2: Rewrite DashboardScreen with stats, study button, and pull-to-refresh</name>
  <files>apps/android/screens/DashboardScreen.tsx</files>
  <action>
    Rewrite `screens/DashboardScreen.tsx` completely:

    1. Imports:
       - React, useState, useEffect, useCallback from 'react'
       - View, Text, StyleSheet, TouchableOpacity, FlatList, ActivityIndicator from 'react-native'
       - Ionicons from '@expo/vector-icons'
       - getUserStats from '@lumio/core'
       - getSupabaseClient from '@lumio/core' (for last studied query)
       - useTheme from '../hooks/useTheme'
       - StatCard from '../components/StatCard'
       - EmptyState from '../components/EmptyState'

    2. State:
       - repoCount: number (default 0)
       - cardCount: number (default 0)
       - lastStudied: string | null (default null)
       - isLoading: boolean (default true)
       - isRefreshing: boolean (default false)
       - error: string | null (default null)

    3. Data fetching (fetchStats function):
       - Call getUserStats() to get repositoryCount and cardCount
       - Query last studied: `getSupabaseClient().from('study_sessions').select('started_at').order('started_at', { ascending: false }).limit(1)` - extract first row's started_at or null
       - Set state from results
       - Wrap in try/catch, set error on failure
       - Called on mount (useEffect) and on pull-to-refresh

    4. Layout using FlatList (for pull-to-refresh):
       - FlatList with data={[1]} (single item to enable pull-to-refresh)
       - renderItem returns the dashboard content
       - onRefresh: set isRefreshing true, await fetchStats, set isRefreshing false
       - refreshing={isRefreshing}
       - style={{ flex: 1, backgroundColor: colors.background }}

    5. Dashboard content (inside renderItem):
       a. Stat cards row: View with flexDirection 'row', gap 12, paddingHorizontal 16, marginTop 16
          - StatCard: icon='folder-outline', iconColor=colors.primary, iconBgColor=colors.primaryLight, label='Repositories', value=repoCount, isLoading
          - StatCard: icon='documents-outline', iconColor='#8B5CF6' (purple), iconBgColor='#EDE9FE', label='Cards', value=cardCount, isLoading

       b. Last studied card: View paddingHorizontal 16, marginTop 12
          - StatCard: icon='time-outline', iconColor='#F59E0B' (amber), iconBgColor='#FEF3C7', label='Last Studied', value=formatLastStudied(lastStudied), subtitle (relative time like "2 hours ago"), isLoading

       c. Study button: View paddingHorizontal 16, marginTop 24
          - TouchableOpacity disabled when isLoading OR cardCount === 0
          - Background: cardCount === 0 ? colors.border : colors.primary (disabled look)
          - Row layout: play icon + "Start Study Session" text
          - If isLoading: show ActivityIndicator instead of play icon
          - Opacity 0.5 when disabled
          - Below button when disabled and not loading: Text "Add repositories with cards to start studying" in textSecondary, centered

       d. If error: show error text with retry button

    6. Empty state: If !isLoading && repoCount === 0, replace stat cards with EmptyState component:
       - icon='library-outline', title='Welcome to Lumio', subtitle='Add a repository to get started with your flashcards'
       - No action button (user navigates to Repos tab)

    7. Helper: formatLastStudied(dateString: string | null):
       - null -> 'Not yet'
       - Otherwise format as relative time: "Just now", "X minutes ago", "X hours ago", "Yesterday", date string
       - Keep it simple with manual relative time calculation (no external library)
  </action>
  <verify>
    - `pnpm --filter @lumio/android exec -- npx tsc --noEmit` compiles without errors
    - `grep "getUserStats" apps/android/screens/DashboardScreen.tsx` shows API integration
    - `grep "useTheme" apps/android/screens/DashboardScreen.tsx` shows theme usage
    - `grep "onRefresh" apps/android/screens/DashboardScreen.tsx` shows pull-to-refresh
    - `grep "disabled" apps/android/screens/DashboardScreen.tsx` shows study button disabled logic
  </verify>
  <done>
    DashboardScreen shows repo count, card count, last studied as stat cards. Study button is prominent CTA, disabled when no cards, shows loading state. Pull-to-refresh works. Empty state shown when no repos. All colors from useTheme().
  </done>
</task>

</tasks>

<verification>
- TypeScript compiles: `pnpm --filter @lumio/android exec -- npx tsc --noEmit`
- Dashboard renders stat cards with real data from getUserStats()
- Study button disabled when cardCount === 0
- Pull-to-refresh fetches fresh data
- Empty state displays when no repositories
- All UI elements use theme colors (no hardcoded color values)
</verification>

<success_criteria>
- Dashboard shows repository count and card count (DASH-01)
- Study button visible and disabled when no cards (DASH-02)
- Pull-to-refresh works on dashboard (from CONTEXT decisions)
- All colors from useTheme() (DASH-03 preparation)
- StatCard and EmptyState are reusable components
</success_criteria>

<output>
After completion, create `.planning/phases/03-core-screens/03-02-SUMMARY.md`
</output>
