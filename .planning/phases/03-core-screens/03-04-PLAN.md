---
phase: 03-core-screens
plan: 04
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - apps/android/screens/SettingsScreen.tsx
  - apps/android/screens/LoginScreen.tsx
  - apps/android/components/OfflineBanner.tsx
autonomous: true

must_haves:
  truths:
    - "Settings screen has a dark mode toggle with system/light/dark options"
    - "Dark mode preference persists across app restarts"
    - "LoginScreen uses theme colors (no hardcoded values)"
    - "OfflineBanner uses theme colors"
    - "Settings screen uses theme colors"
  artifacts:
    - path: "apps/android/screens/SettingsScreen.tsx"
      provides: "Settings with user info, dark mode toggle, and logout"
      exports: ["SettingsScreen"]
      contains: "useTheme"
    - path: "apps/android/screens/LoginScreen.tsx"
      provides: "Login screen with dark mode support"
      exports: ["LoginScreen"]
      contains: "useTheme"
    - path: "apps/android/components/OfflineBanner.tsx"
      provides: "Offline banner with dark mode support"
      exports: ["OfflineBanner"]
      contains: "useTheme"
  key_links:
    - from: "apps/android/screens/SettingsScreen.tsx"
      to: "contexts/ThemeContext.tsx"
      via: "useTheme() for preference and setPreference"
      pattern: "setPreference"
---

<objective>
Add dark mode toggle to Settings screen, apply theme colors to LoginScreen and OfflineBanner so all screens support dark mode consistently.

Purpose: Dark mode is a locked user decision (DASH-03). The in-app toggle in Settings allows users to override system preference. All existing screens must use theme colors to avoid jarring color inconsistencies.
Output: Settings with dark mode toggle, LoginScreen and OfflineBanner updated for dark mode.
</objective>

<execution_context>
@/home/toto/.claude/get-shit-done/workflows/execute-plan.md
@/home/toto/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-core-screens/03-01-SUMMARY.md
@apps/android/screens/SettingsScreen.tsx
@apps/android/screens/LoginScreen.tsx
@apps/android/components/OfflineBanner.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enhance SettingsScreen with dark mode toggle</name>
  <files>apps/android/screens/SettingsScreen.tsx</files>
  <action>
    Rewrite `screens/SettingsScreen.tsx`:

    1. Imports:
       - Add useTheme from '../hooks/useTheme'
       - Add ScrollView from 'react-native'
       - Add Ionicons from '@expo/vector-icons'
       - Keep useAuth import

    2. Use useTheme() to get colors, preference, setPreference, isDark

    3. Layout (ScrollView instead of View, for future settings expansion):
       - backgroundColor: colors.background

       a. User info section (existing, but with theme colors):
          - backgroundColor: colors.surface
          - label: colors.textSecondary
          - email: colors.text

       b. Theme section (NEW):
          - Section header: "Appearance" (14px, uppercase, colors.textSecondary, letterSpacing 1, paddingHorizontal 16, marginTop 24, marginBottom 8)
          - Three option rows inside a surface container (backgroundColor colors.surface, borderRadius 8, marginHorizontal 16):
            - "System" option: icon 'phone-portrait-outline', selected when preference === 'system'
            - "Light" option: icon 'sunny-outline', selected when preference === 'light'
            - "Dark" option: icon 'moon-outline', selected when preference === 'dark'
          - Each option row:
            - TouchableOpacity, paddingVertical 14, paddingHorizontal 16
            - Row layout: icon (20px) + label text (16px, colors.text) + checkmark on right if selected (Ionicons 'checkmark', colors.primary)
            - borderBottomWidth 1, borderBottomColor colors.border (except last)
            - onPress: setPreference('system' | 'light' | 'dark')

       c. Logout section (existing, pushed to bottom):
          - Keep existing logout button with danger color
          - Update colors to use colors.danger

       d. App version at very bottom: Text "Lumio v1.0.0" (12px, colors.textSecondary, textAlign center, padding 16)

    4. Remove all hardcoded color values from StyleSheet. Use dynamic colors via style arrays: [styles.staticStyle, { backgroundColor: colors.xxx }]
  </action>
  <verify>
    - `pnpm --filter @lumio/android exec -- npx tsc --noEmit` compiles without errors
    - `grep "setPreference" apps/android/screens/SettingsScreen.tsx` shows toggle implementation
    - `grep "useTheme" apps/android/screens/SettingsScreen.tsx` shows theme usage
    - `grep -c "hardcoded color like #f5f5f5"` returns 0 — no hardcoded colors in static StyleSheet
  </verify>
  <done>
    SettingsScreen has system/light/dark toggle that persists via AsyncStorage. All colors use useTheme(). User info and logout sections styled with theme.
  </done>
</task>

<task type="auto">
  <name>Task 2: Apply theme colors to LoginScreen and OfflineBanner</name>
  <files>
    apps/android/screens/LoginScreen.tsx
    apps/android/components/OfflineBanner.tsx
  </files>
  <action>
    1. Update `screens/LoginScreen.tsx`:
       - Import useTheme from '../hooks/useTheme'
       - Call useTheme() inside LoginScreen component
       - Replace hardcoded colors with theme colors:
         - container backgroundColor: isDark ? colors.background : '#ffffff' (white for login feels right in light mode)
         - logo color: colors.primary
         - tagline color: colors.textSecondary
         - error color: colors.danger
         - configWarning: '#f97316' can stay (it's a warning color, same in both themes)
         - googleButton: keep '#4285F4' (Google brand color, doesn't change with theme)
         - googleButtonText: keep '#ffffff'
         - googleIconContainer background: keep '#ffffff'
       - Key changes: container bg, text colors, error color. Google button stays branded.

    2. Update `components/OfflineBanner.tsx`:
       - Read the current file first to understand its structure
       - Import useTheme from '../hooks/useTheme'
       - Call useTheme() inside component
       - Replace hardcoded colors with theme-aware colors:
         - Banner background: colors.danger or a specific offline color
         - Text color: '#ffffff' (white on danger bg works for both themes)
       - The banner is a warning indicator, so it should be consistently red regardless of theme. The main change is ensuring it doesn't look jarring in dark mode.
  </action>
  <verify>
    - `pnpm --filter @lumio/android exec -- npx tsc --noEmit` compiles without errors
    - `grep "useTheme" apps/android/screens/LoginScreen.tsx` shows theme usage
    - `grep "useTheme" apps/android/components/OfflineBanner.tsx` shows theme usage
  </verify>
  <done>
    LoginScreen and OfflineBanner use theme colors. Login screen adapts background to dark mode. Offline banner remains consistently visible in both themes.
  </done>
</task>

</tasks>

<verification>
- TypeScript compiles: `pnpm --filter @lumio/android exec -- npx tsc --noEmit`
- Settings has system/light/dark theme toggle with visual selection
- Theme preference persists (AsyncStorage)
- LoginScreen adapts to dark mode
- OfflineBanner visible in both themes
- No hardcoded color values remain in modified files (except brand colors like Google blue)
</verification>

<success_criteria>
- Dark mode automatic from system (DASH-03)
- In-app toggle overrides system (per CONTEXT locked decision)
- All screens use useTheme() for colors
- Preference persists across restarts
</success_criteria>

<output>
After completion, create `.planning/phases/03-core-screens/03-04-SUMMARY.md`
</output>
