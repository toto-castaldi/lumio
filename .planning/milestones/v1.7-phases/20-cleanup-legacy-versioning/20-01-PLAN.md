---
phase: 20-cleanup-legacy-versioning
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - pnpm-lock.yaml
  - .husky/commit-msg
  - .husky/prepare-commit-msg
  - .husky/_/
  - .commitlintrc.json
  - .czrc
  - .release-please-manifest.json
  - release-please-config.json
  - CHANGELOG.md
autonomous: true
requirements: [CLEAN-01, CLEAN-02, CLEAN-03, CLEAN-04, CLEAN-06]

must_haves:
  truths:
    - "Running `git commit` does not trigger any husky pre-commit or commit-msg hooks"
    - "No commitlint, commitizen, or release-please configuration files exist in the repository"
    - "CHANGELOG.md no longer exists in the repository root"
    - "`pnpm install` succeeds without installing husky, commitlint, commitizen, or cz-conventional-changelog"
  artifacts:
    - path: "package.json"
      provides: "Clean package.json without legacy versioning dependencies or scripts"
      contains: "typescript"
  key_links:
    - from: "package.json"
      to: ".husky/"
      via: "prepare script removed"
      pattern: "no 'husky' in scripts.prepare"
---

<objective>
Remove all local versioning tooling: husky hooks, commitlint config, commitizen config, release-please config files, and CHANGELOG.md.

Purpose: Eliminate legacy automated versioning infrastructure so the project has zero trace of the old tooling chain. Phase 21 will wire up STATE.md as the new version source.
Output: Clean repository with no local versioning tooling remnants.
</objective>

<execution_context>
@/home/toto/.claude/get-shit-done/workflows/execute-plan.md
@/home/toto/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-cleanup-legacy-versioning/20-CONTEXT.md
@package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove legacy config files and husky directory</name>
  <files>
    .husky/commit-msg
    .husky/prepare-commit-msg
    .husky/_/
    .commitlintrc.json
    .czrc
    .release-please-manifest.json
    release-please-config.json
    CHANGELOG.md
  </files>
  <action>
    Delete the following files and directories using `git rm` (to stage for commit):

    1. `git rm .husky/commit-msg` — husky commit-msg hook that ran commitlint
    2. `git rm .husky/prepare-commit-msg` — husky hook that ran commitizen interactively
    3. `git rm -r .husky/_/` — husky internal directory
    4. After removing all files in `.husky/`, remove the now-empty directory: `rmdir .husky` (or `git rm -r .husky` if git tracks it)
    5. `git rm .commitlintrc.json` — commitlint configuration (extends @commitlint/config-conventional)
    6. `git rm .czrc` — commitizen config file (path: cz-conventional-changelog)
    7. `git rm .release-please-manifest.json` — release-please version tracking manifest
    8. `git rm release-please-config.json` — release-please configuration (changelog sections, extra-files)
    9. `git rm CHANGELOG.md` — auto-generated changelog (git history preserves this content)

    Do NOT delete `.git/hooks/` — those are just git sample files, not custom hooks.
  </action>
  <verify>
    Run `ls .husky/ 2>/dev/null` — should return error/empty.
    Run `ls .commitlintrc.json .czrc .release-please-manifest.json release-please-config.json CHANGELOG.md 2>/dev/null` — should return nothing.
    Run `git status` — all deletions should be staged.
  </verify>
  <done>All 9 legacy config/hook files are deleted and staged for commit. The `.husky/` directory no longer exists.</done>
</task>

<task type="auto">
  <name>Task 2: Clean package.json and uninstall legacy dependencies</name>
  <files>
    package.json
    pnpm-lock.yaml
  </files>
  <action>
    Edit `package.json` to remove all legacy versioning references:

    1. **Remove scripts:**
       - Remove `"commit": "cz"` from scripts
       - Remove `"prepare": "husky"` from scripts

    2. **Remove devDependencies:**
       - Remove `"@commitlint/cli": "^18.6.0"`
       - Remove `"@commitlint/config-conventional": "^18.6.0"`
       - Remove `"commitizen": "^4.3.0"`
       - Remove `"cz-conventional-changelog": "^3.3.0"`
       - Remove `"husky": "^9.0.0"`
       - Keep `"typescript": "^5.7.0"` (still needed)

    3. **Remove config section:**
       - Remove the entire `"config": { "commitizen": { "path": "cz-conventional-changelog" } }` block

    4. **Version field:** Leave `"version": "1.6.1"` as-is per Claude's discretion — Phase 21 will wire this to STATE.md. Leaving it doesn't hurt anything.

    5. **Run `pnpm install`** to update the lockfile (removes the uninstalled packages from pnpm-lock.yaml).

    After editing, the devDependencies should contain only `"typescript": "^5.7.0"`.
  </action>
  <verify>
    Run `cat package.json | grep -E "husky|commitlint|commitizen|cz-conventional"` — should return nothing.
    Run `cat package.json | grep -E '"commit"|"prepare"'` — should return nothing (no commit or prepare scripts).
    Run `cat package.json | grep "config"` — should not show commitizen config.
    Run `pnpm install` — should succeed without errors.
    Run `git commit --allow-empty -m "test commit message" --dry-run` — should succeed without any hook running (no commitlint validation).
  </verify>
  <done>package.json has zero references to husky, commitlint, commitizen, or cz-conventional-changelog. pnpm-lock.yaml is updated. `git commit` runs without any hooks.</done>
</task>

</tasks>

<verification>
1. `ls .husky/ .commitlintrc.json .czrc .release-please-manifest.json release-please-config.json CHANGELOG.md 2>/dev/null` — returns nothing
2. `grep -E "husky|commitlint|commitizen|cz-conventional|release-please" package.json` — returns nothing
3. `git commit --allow-empty -m "test" --dry-run` — succeeds without hook errors
4. `pnpm install` — succeeds
</verification>

<success_criteria>
- All legacy config files deleted from repository
- package.json contains no versioning tooling references
- `git commit` works without any pre-commit or commit-msg hooks
- pnpm-lock.yaml updated to reflect removed dependencies
</success_criteria>

<output>
After completion, create `.planning/phases/20-cleanup-legacy-versioning/20-01-SUMMARY.md`
</output>
