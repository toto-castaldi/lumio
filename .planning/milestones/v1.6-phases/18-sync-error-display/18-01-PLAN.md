---
phase: 18-sync-error-display
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/shared/src/types/index.ts
  - packages/core/src/supabase/repositories.ts
  - supabase/functions/git-sync/index.ts
  - apps/android/lib/theme.ts
  - apps/android/i18n/en.ts
  - apps/android/i18n/it.ts
  - apps/android/components/RepoListItem.tsx
autonomous: true
requirements: [ERRDSP-01, ERRDSP-02]

must_haves:
  truths:
    - "Repository with sync_status='failed' shows a visible error indicator (warning icon + red badge) in the repo list"
    - "Error message from Docora is displayed as a third line below the URL for failed repos"
    - "Auth errors are visually distinct from non-auth errors (amber vs red coloring)"
    - "Repositories with sync_status='synced' show no error indicator"
    - "Repositories with sync_status='syncing' show a subtle syncing indicator"
    - "Repositories with sync_status='pending' show a pending indicator"
  artifacts:
    - path: "packages/shared/src/types/index.ts"
      provides: "Updated SyncStatus type with 'failed' value and Repository interface with syncErrorType, isAuthError, syncFailedAt fields"
      contains: "failed"
    - path: "packages/core/src/supabase/repositories.ts"
      provides: "Updated mapRepository to include syncErrorType, isAuthError, syncFailedAt"
      contains: "isAuthError"
    - path: "apps/android/lib/theme.ts"
      provides: "Warning color added to both light and dark palettes"
      contains: "warning"
    - path: "apps/android/components/RepoListItem.tsx"
      provides: "Error indicator, error message, and status indicators for all sync states"
      contains: "warning-outline"
  key_links:
    - from: "packages/shared/src/types/index.ts"
      to: "packages/core/src/supabase/repositories.ts"
      via: "Repository type import"
      pattern: "import.*Repository.*@lumio/shared"
    - from: "packages/core/src/supabase/repositories.ts"
      to: "apps/android/components/RepoListItem.tsx"
      via: "Repository type flows through ReposScreen to RepoListItem props"
      pattern: "syncStatus.*syncErrorMessage.*isAuthError"
    - from: "apps/android/lib/theme.ts"
      to: "apps/android/components/RepoListItem.tsx"
      via: "useTheme hook provides warning color"
      pattern: "colors\\.warning"
---

<objective>
Add sync error indicators and messages to the repository list. Users see which repos have sync problems, with distinct visual treatment for auth errors (amber, user-fixable in Phase 19) vs other errors (red). Also show syncing and pending states.

Purpose: Phase 17 stored sync failure data in the DB. This phase makes that data visible so users understand why a repo isn't syncing.
Output: Updated types, mapping, theme colors, i18n translations, and RepoListItem component with full sync status visualization.
</objective>

<execution_context>
@/home/toto/.claude/get-shit-done/workflows/execute-plan.md
@/home/toto/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-sync-failure-backend/17-01-SUMMARY.md
@.planning/phases/17-sync-failure-backend/17-02-SUMMARY.md
@packages/shared/src/types/index.ts
@packages/core/src/supabase/repositories.ts
@supabase/functions/git-sync/index.ts
@apps/android/lib/theme.ts
@apps/android/i18n/en.ts
@apps/android/i18n/it.ts
@apps/android/components/RepoListItem.tsx
@apps/android/screens/ReposScreen.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update types, mapping, edge function types, theme, and i18n</name>
  <files>
    packages/shared/src/types/index.ts
    packages/core/src/supabase/repositories.ts
    supabase/functions/git-sync/index.ts
    apps/android/lib/theme.ts
    apps/android/i18n/en.ts
    apps/android/i18n/it.ts
  </files>
  <action>
    **1. Update SyncStatus and Repository in @lumio/shared (packages/shared/src/types/index.ts):**
    - Change `SyncStatus` from `'pending' | 'syncing' | 'synced' | 'error'` to `'pending' | 'syncing' | 'synced' | 'error' | 'failed'`
    - Add these fields to the `Repository` interface:
      - `syncErrorType?: string;` — Raw error type from Docora (e.g., "auth_error")
      - `isAuthError: boolean;` — Whether the failure is an auth error (computed by webhook)
      - `syncFailedAt?: string;` — Timestamp of when sync failure occurred
    - Note: `syncErrorMessage` already exists in Repository

    **2. Update mapRepository in @lumio/core (packages/core/src/supabase/repositories.ts):**
    - Add mapping for the 3 new fields in the `mapRepository` function:
      - `syncErrorType: dbRepo.sync_error_type as string | undefined`
      - `isAuthError: (dbRepo.is_auth_error as boolean) ?? false`
      - `syncFailedAt: dbRepo.sync_failed_at as string | undefined`

    **3. Update git-sync edge function types (supabase/functions/git-sync/index.ts):**
    - Add `'failed'` to the local `SyncStatus` type on line 12
    - Add to the local `Repository` interface (around line 14-28):
      - `sync_error_type?: string;`
      - `is_auth_error: boolean;`
      - `sync_failed_at?: string;`

    **4. Add warning color to theme (apps/android/lib/theme.ts):**
    - Add `warning: '#f59e0b'` to `lightColors` (amber-500, for auth errors)
    - Add `warningLight: '#FEF3C7'` to `lightColors` (amber-100)
    - Add `warning: '#fbbf24'` to `darkColors` (amber-400, brighter for dark bg)
    - Add `warningLight: '#78350f'` to `darkColors` (amber-900)
    - These colors will be used for auth errors (user-fixable) while danger/red is for non-auth errors

    **5. Add i18n translations for sync status messages:**

    In `apps/android/i18n/en.ts`, add a `syncStatus` section at the top level:
    ```
    syncStatus: {
      pending: 'Waiting for sync...',
      syncing: 'Syncing...',
      authError: 'Authentication failed',
      syncError: 'Sync failed',
      unknownError: 'Sync error occurred',
    },
    ```

    In `apps/android/i18n/it.ts`, add the matching section:
    ```
    syncStatus: {
      pending: 'In attesa di sincronizzazione...',
      syncing: 'Sincronizzazione...',
      authError: 'Autenticazione fallita',
      syncError: 'Sincronizzazione fallita',
      unknownError: 'Errore di sincronizzazione',
    },
    ```

    **6. Rebuild shared package** after type changes:
    Run `pnpm build:packages` from the project root to rebuild @lumio/shared so @lumio/core and the app pick up the new types.
  </action>
  <verify>
    - `pnpm build:packages` succeeds without errors
    - `grep 'failed' packages/shared/src/types/index.ts` shows 'failed' in SyncStatus
    - `grep 'isAuthError' packages/core/src/supabase/repositories.ts` shows the new mapping
    - `grep 'warning' apps/android/lib/theme.ts` shows warning colors in both palettes
    - `grep 'syncStatus' apps/android/i18n/en.ts` shows new translations
    - `grep 'syncStatus' apps/android/i18n/it.ts` shows Italian translations
  </verify>
  <done>
    SyncStatus type includes 'failed', Repository interface has syncErrorType/isAuthError/syncFailedAt fields, mapRepository maps all new fields, git-sync types updated, theme has warning/warningLight colors, i18n has sync status messages in EN and IT, packages rebuilt successfully.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add sync status indicators and error messages to RepoListItem</name>
  <files>
    apps/android/components/RepoListItem.tsx
  </files>
  <action>
    Update the RepoListItem component to display sync status indicators and error messages.

    **1. Update the props interface:**
    Extend the `repo` object in `RepoListItemProps` to include the new fields:
    ```typescript
    repo: {
      id: string;
      name: string;
      url: string;
      isPrivate: boolean;
      syncStatus?: string;
      syncErrorMessage?: string;
      syncErrorType?: string;
      isAuthError?: boolean;
    };
    ```

    **2. Add the useI18n hook** (already imported).

    **3. Determine sync state and styling:**
    Inside the component, compute display state:
    ```typescript
    const isFailed = repo.syncStatus === 'failed';
    const isSyncing = repo.syncStatus === 'syncing';
    const isPending = repo.syncStatus === 'pending';
    const isAuthError = isFailed && repo.isAuthError;
    const isOtherError = isFailed && !repo.isAuthError;
    ```

    **4. Repo name text color changes:**
    - Default: `colors.text` (normal)
    - Auth error: `colors.warning` (amber — user-fixable)
    - Non-auth error: `colors.danger` (red — system issue)
    - Syncing/Pending: `colors.text` (normal, status shown via trailing indicator)

    **5. Add trailing status indicator (right side of row, replacing or alongside visibility icon area):**
    The status indicator goes on the right edge of the name row. Use these indicators:

    - **Failed (auth):** `Ionicons` `warning-outline` icon, size 16, color `colors.warning` (amber)
    - **Failed (non-auth):** `Ionicons` `alert-circle-outline` icon, size 16, color `colors.danger` (red)
    - **Syncing:** `ActivityIndicator` size 14, color `colors.primary`
    - **Pending:** `Ionicons` `time-outline` icon, size 14, color `colors.textSecondary`
    - **Synced:** No extra indicator (clean state)

    Position: Add a `View` with `style={{ marginLeft: 'auto' }}` at the end of the `row` View to push the indicator to the right. Keep the visibility icon (globe/lock) where it is next to the name.

    **6. Error message as third line (below URL):**
    When `isFailed && repo.syncErrorMessage`:
    - Show user-friendly translated message first, then raw error message below if different
    - For auth errors: show `t('syncStatus.authError')` as the primary message
    - For non-auth errors: show `t('syncStatus.syncError')` as the primary message, then `repo.syncErrorMessage` as detail
    - Style: `fontSize: 13`, color `colors.warning` for auth errors or `colors.danger` for non-auth errors
    - `numberOfLines={2}` with ellipsize mode tail

    When `isPending`:
    - Show `t('syncStatus.pending')` as third line in `colors.textSecondary`, `fontSize: 13`

    When `isSyncing`:
    - Show `t('syncStatus.syncing')` as third line in `colors.primary`, `fontSize: 13`

    **7. Add new styles:**
    ```typescript
    statusIndicator: {
      marginLeft: 'auto',
      paddingLeft: 8,
    },
    errorMessage: {
      fontSize: 13,
      marginTop: 2,
    },
    statusMessage: {
      fontSize: 13,
      marginTop: 2,
    },
    ```

    **Important:** Import `ActivityIndicator` from 'react-native' for the syncing spinner.

    **Design rationale per user decisions:**
    - Badge overlay on right side of row (trailing edge) per specifics
    - Error message below URL line, making failed repos taller per specifics
    - Auth errors amber (user-fixable), non-auth errors red per decisions
    - No hint about "tap to update token" since Phase 19 isn't built yet
    - Non-auth errors just show the translated status + raw message from Docora
    - Syncing uses ActivityIndicator (small spinner) per decisions (spinner vs text: chose both — spinner icon + text label)
    - Pending shows time-outline icon + text
  </action>
  <verify>
    - `pnpm --filter @lumio/android exec -- npx tsc --noEmit` passes (TypeScript check)
    - RepoListItem.tsx contains `warning-outline` icon for auth errors
    - RepoListItem.tsx contains `alert-circle-outline` icon for non-auth errors
    - RepoListItem.tsx contains `ActivityIndicator` for syncing state
    - RepoListItem.tsx contains `time-outline` for pending state
    - RepoListItem.tsx renders error message text when repo has syncErrorMessage
    - RepoListItem.tsx uses `colors.warning` for auth errors and `colors.danger` for non-auth errors
  </verify>
  <done>
    RepoListItem shows: (1) warning icon + amber name tint for auth errors, (2) alert icon + red name tint for non-auth errors, (3) translated error message as third line for all failed repos, (4) spinner + "Syncing..." for syncing repos, (5) clock icon + "Waiting for sync..." for pending repos, (6) clean state (no indicators) for synced repos. TypeScript compiles without errors.
  </done>
</task>

</tasks>

<verification>
1. `pnpm build:packages` — shared types rebuild successfully
2. `pnpm --filter @lumio/android exec -- npx tsc --noEmit` — TypeScript compiles without errors
3. Visual inspection: the RepoListItem component code handles all 4 sync states (synced, syncing, pending, failed) and distinguishes auth from non-auth errors
4. Both EN and IT translations are present for all sync status messages
5. Theme has warning/warningLight colors in both light and dark palettes
</verification>

<success_criteria>
- SyncStatus type includes 'failed', Repository interface has all Phase 17 fields exposed
- Repository list shows error badge + colored name for failed repos
- Error message from Docora visible as third line on failed repos
- Auth errors visually distinct from non-auth errors (amber vs red)
- Syncing repos show spinner, pending repos show clock icon
- Synced repos show clean state with no error indicators
- TypeScript compilation passes
</success_criteria>

<output>
After completion, create `.planning/phases/18-sync-error-display/18-01-SUMMARY.md`
</output>
