---
phase: 17-sync-failure-backend
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/functions/docora-webhook/index.ts
autonomous: true
gap_closure: true
requirements: [SYNC-03]

must_haves:
  truths:
    - "When a successful update webhook arrives for an existing card in a previously failed repo, sync_status resets to 'synced' and all error fields are cleared"
    - "When a successful update webhook arrives for an image file in a previously failed repo, sync_status resets to 'synced' and all error fields are cleared"
  artifacts:
    - path: "supabase/functions/docora-webhook/index.ts"
      provides: "Auto-recovery in handleUpdate existing-card and image-file paths"
      contains: "sync_status.*synced"
  key_links:
    - from: "supabase/functions/docora-webhook/index.ts (handleUpdate existing-card)"
      to: "repositories table"
      via: "UPDATE clearing sync_status, sync_error_type, sync_error_message, is_auth_error, sync_failed_at"
      pattern: 'sync_status.*synced'
    - from: "supabase/functions/docora-webhook/index.ts (handleUpdate image-file)"
      to: "repositories table"
      via: "UPDATE clearing sync_status, sync_error_type, sync_error_message, is_auth_error, sync_failed_at"
      pattern: 'sync_status.*synced'
---

<objective>
Fix missing auto-recovery in handleUpdate for existing-card and image-file paths.

Purpose: The handleUpdate function has two code paths that successfully process webhooks but do NOT clear repository error fields — the existing-card update (lines 933-955) and the image-file update (lines 898-916). A previously-failed repo receiving update webhooks through these paths will remain stuck in 'failed' state despite the content syncing successfully. This is the most common update scenario in production.

Output: Both paths clear sync_status and error fields, completing SYNC-03 requirement.
</objective>

<execution_context>
@/home/toto/.claude/get-shit-done/workflows/execute-plan.md
@/home/toto/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-sync-failure-backend/17-01-SUMMARY.md
@supabase/functions/docora-webhook/index.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add auto-recovery to handleUpdate existing-card and image-file paths</name>
  <files>supabase/functions/docora-webhook/index.ts</files>
  <action>
Add repository error field clearing to the two handleUpdate code paths that are missing it.

**Existing-card update path (around line 947-955):**
After the card update `.eq("id", existingCard.id)` and the card_assets delete, BEFORE the `return { success: true, message: ... }` on line 955, add:

```typescript
// Mark repository as synced and clear any error state (auto-recovery)
await serviceClient
  .from("repositories")
  .update({
    sync_status: "synced",
    sync_error_message: null,
    sync_error_type: null,
    is_auth_error: false,
    sync_failed_at: null,
  })
  .eq("id", repo.id);
```

**Image-file update path (around line 910-912):**
After the `uploadImageToStorage` call succeeds, BEFORE the `return { success: true, message: ... }` on line 912, add the same repository update block:

```typescript
// Mark repository as synced and clear any error state (auto-recovery)
await serviceClient
  .from("repositories")
  .update({
    sync_status: "synced",
    sync_error_message: null,
    sync_error_type: null,
    is_auth_error: false,
    sync_failed_at: null,
  })
  .eq("id", repo.id);
```

Follow the exact same pattern used in:
- handleCreate card path (lines 781-791)
- handleUpdate README.md path (lines 864-876)
- handleUpdate new-card fallback path (lines 979-989)

Do NOT modify any other code paths — only these two missing ones.
  </action>
  <verify>
1. Run `grep -n "sync_status.*synced" supabase/functions/docora-webhook/index.ts` — should show recovery calls in ALL successful webhook paths: handleCreate README, handleCreate card, handleUpdate README, handleUpdate image, handleUpdate existing-card, handleUpdate new-card.
2. Count the recovery blocks: there should be 6 total occurrences of `sync_status: "synced"` in the file (5 existing + 0 = was wrong, now 5 existing + 2 new - wait: was 4 existing recovery blocks + handleSyncFailed sets sync_status: "failed"). After the fix: grep should find `sync_status: "synced"` exactly 6 times (README create, card create, README update, image update, existing-card update, new-card update).
3. Verify TypeScript compiles: `cd /home/toto/scm-projects/lumio && npx supabase functions serve --env-file supabase/.env.local --no-verify-jwt 2>&1 | head -20` (should not show TS errors).
  </verify>
  <done>
Both the existing-card update path and the image-file update path in handleUpdate clear sync_status to 'synced' and null out sync_error_type, sync_error_message, is_auth_error, and sync_failed_at — matching the pattern used in all other successful webhook paths.
  </done>
</task>

</tasks>

<verification>
1. Every successful webhook path (create and update) in the docora-webhook Edge Function clears repository error fields
2. The six recovery points are: handleCreate README, handleCreate card, handleUpdate README, handleUpdate image, handleUpdate existing-card, handleUpdate new-card
3. A previously-failed repo receiving an update webhook for an existing card transitions to synced
4. A previously-failed repo receiving an update webhook for an image file transitions to synced
</verification>

<success_criteria>
- `grep -c 'sync_status: "synced"' supabase/functions/docora-webhook/index.ts` returns 6
- No TypeScript compilation errors
- SYNC-03 requirement fully satisfied: all create/update paths trigger auto-recovery
</success_criteria>

<output>
After completion, create `.planning/phases/17-sync-failure-backend/17-02-SUMMARY.md`
</output>
