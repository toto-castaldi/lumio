---
phase: 27-foundation-database
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/config.toml
  - supabase/templates/confirmation.html
  - supabase/templates/recovery.html
autonomous: true
requirements:
  - INFRA-03
  - INFRA-04

must_haves:
  truths:
    - "config.toml has enable_confirmations = true under [auth.email]"
    - "config.toml has enable_manual_linking = true under [auth]"
    - "config.toml has otp_length = 6 and otp_expiry = 3600 under [auth.email]"
    - "config.toml references confirmation and recovery email templates"
    - "Confirmation email template displays a 6-digit OTP code prominently with Lumio branding"
    - "Recovery email template displays a 6-digit OTP code prominently with Lumio branding"
    - "Both templates are bilingual (English first, Italian below with separator)"
    - "Both templates include a safety disclaimer footer in both languages"
  artifacts:
    - path: "supabase/config.toml"
      provides: "Email auth configuration with confirmations, manual linking, OTP settings, and template references"
      contains: "enable_confirmations = true"
    - path: "supabase/templates/confirmation.html"
      provides: "Branded signup verification OTP email template"
      contains: "{{ .Token }}"
    - path: "supabase/templates/recovery.html"
      provides: "Branded password reset OTP email template"
      contains: "{{ .Token }}"
  key_links:
    - from: "supabase/config.toml"
      to: "supabase/templates/confirmation.html"
      via: "content_path reference in [auth.email.template.confirmation]"
      pattern: "content_path.*confirmation.html"
    - from: "supabase/config.toml"
      to: "supabase/templates/recovery.html"
      via: "content_path reference in [auth.email.template.recovery]"
      pattern: "content_path.*recovery.html"
---

<objective>
Configure Supabase for email/password authentication and create branded OTP email templates.

Purpose: Enable email confirmations, manual identity linking, and OTP-based verification in the local Supabase config. Provide Lumio-branded HTML email templates for signup verification and password reset flows.

Output: Updated config.toml with email auth settings, and two HTML email templates (confirmation + recovery) in supabase/templates/.
</objective>

<execution_context>
@/home/toto/.claude/get-shit-done/workflows/execute-plan.md
@/home/toto/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-foundation-database/27-CONTEXT.md
@.planning/phases/27-foundation-database/27-RESEARCH.md
@supabase/config.toml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update config.toml for email auth and template references</name>
  <files>supabase/config.toml</files>
  <action>
Update the existing `supabase/config.toml` to enable email authentication features. Make the following changes:

1. Under `[auth]`, add `enable_manual_linking = true` (prepares for Phase 31 account linking).

2. Under `[auth.email]`, change/add:
   - `enable_confirmations = true` (was `false`)
   - `otp_length = 6`
   - `otp_expiry = 3600` (1 hour, per user decision)

3. Add two new template sections after `[auth.email]`:

```toml
[auth.email.template.confirmation]
subject = "Lumio - Your verification code"
content_path = "./supabase/templates/confirmation.html"

[auth.email.template.recovery]
subject = "Lumio - Password reset code"
content_path = "./supabase/templates/recovery.html"
```

Do NOT modify any existing settings (Google OAuth, redirect URLs, JWT settings, etc.). Only add/change the email auth settings listed above.

Note: `content_path` is relative to the project root (where `supabase/` directory lives), not relative to config.toml.
  </action>
  <verify>
    <automated>grep -E "enable_confirmations = true|enable_manual_linking = true|otp_length = 6|otp_expiry = 3600|content_path.*confirmation.html|content_path.*recovery.html" supabase/config.toml | wc -l | grep -q "6" && echo "PASS: All 6 config settings found" || echo "FAIL: Missing config settings"</automated>
  </verify>
  <done>config.toml has enable_confirmations = true, enable_manual_linking = true, otp_length = 6, otp_expiry = 3600, and references to both email templates</done>
</task>

<task type="auto">
  <name>Task 2: Create branded OTP email templates</name>
  <files>supabase/templates/confirmation.html, supabase/templates/recovery.html</files>
  <action>
Create `supabase/templates/` directory and two HTML email templates.

**Both templates share the same layout structure (per user decisions):**
- Lumio logo at top (purple header bar with "LUMIO" text, background color #7C3AED)
- Branded HTML layout with Lumio brand colors
- Minimal and functional tone -- just the code and brief instruction, no conversational fluff
- OTP code displayed large and prominent (centered, big font like a bank PIN display -- `font-size: 36px`, `letter-spacing: 8px`, `font-weight: bold`, color #7C3AED on light purple background #f0ecf9)
- Bilingual: English section first, then a subtle horizontal rule separator, then Italian section below
- Footer with safety disclaimer in both languages: "If you didn't request this code, you can safely ignore this email." / "Se non hai richiesto questo codice, puoi ignorare questa email."
- Use `{{ .Token }}` for the OTP code (Supabase Go template variable)
- Email-safe inline CSS (no external stylesheets), table-based layout for email client compatibility

**confirmation.html** (signup verification):
- EN text: "Your verification code:" above the OTP display, "Enter this code in the app to verify your email." below
- IT text: "Il tuo codice di verifica:" above the OTP display, "Inserisci questo codice nell'app per verificare la tua email." below

**recovery.html** (password reset):
- EN text: "Your password reset code:" above the OTP display, "Enter this code in the app to reset your password." below
- IT text: "Il tuo codice per reimpostare la password:" above the OTP display, "Inserisci questo codice nell'app per reimpostare la tua password." below

Use the research code example for confirmation.html as the base. Create recovery.html with identical structure but password-reset-specific copy.

Both templates must be complete, valid HTML documents with DOCTYPE, charset meta, and viewport meta.
  </action>
  <verify>
    <automated>test -f supabase/templates/confirmation.html && test -f supabase/templates/recovery.html && grep -q "{{ .Token }}" supabase/templates/confirmation.html && grep -q "{{ .Token }}" supabase/templates/recovery.html && grep -q "7C3AED" supabase/templates/confirmation.html && grep -q "7C3AED" supabase/templates/recovery.html && grep -q "ignorare questa email" supabase/templates/confirmation.html && grep -q "ignorare questa email" supabase/templates/recovery.html && echo "PASS: Both templates exist with OTP token, branding, and bilingual content" || echo "FAIL: Templates missing or incomplete"</automated>
  </verify>
  <done>Both email templates exist in supabase/templates/ with Lumio branding, prominent OTP display using {{ .Token }}, bilingual EN/IT content, and safety disclaimer footer</done>
</task>

</tasks>

<verification>
1. `supabase/config.toml` contains all required email auth settings
2. `supabase/templates/confirmation.html` exists with {{ .Token }}, Lumio branding, bilingual content
3. `supabase/templates/recovery.html` exists with {{ .Token }}, Lumio branding, bilingual content
4. Config template paths correctly reference the template files
</verification>

<success_criteria>
- config.toml enables email confirmations, manual linking, and OTP with 6 digits / 1 hour expiry
- Two branded email templates exist and use {{ .Token }} for OTP display
- Templates are bilingual (EN first, IT below) with safety disclaimer footer
- Template content_path references in config.toml point to correct file locations
</success_criteria>

<output>
After completion, create `.planning/phases/27-foundation-database/27-01-SUMMARY.md`
</output>
