---
phase: 26-history-fix-validation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260226000003_timezone_checks_fresh_user.sql
  - packages/core/src/supabase/study.ts
autonomous: true
requirements: [HIST-01]

must_haves:
  truths:
    - "get_due_card_count accepts p_timezone and uses AT TIME ZONE for date comparison"
    - "get_study_cards_for_session accepts p_timezone and uses AT TIME ZONE for date comparison"
    - "upsert_card_review accepts p_timezone and computes next_review_at using local date"
    - "get_due_card_count includes never-reviewed cards in the count for fresh users"
    - "card_review_schedule has CHECK constraints for ease_factor >= 1.3 and interval_days >= 0"
    - "Client passes Intl.DateTimeFormat timezone to all three RPCs"
  artifacts:
    - path: "supabase/migrations/20260226000003_timezone_checks_fresh_user.sql"
      provides: "Timezone-aware RPCs, fresh-user due count, CHECK constraints"
      contains: "AT TIME ZONE"
    - path: "packages/core/src/supabase/study.ts"
      provides: "Client-side timezone passing to RPCs"
      contains: "p_timezone"
  key_links:
    - from: "packages/core/src/supabase/study.ts"
      to: "get_due_card_count RPC"
      via: "POST body with p_timezone"
      pattern: "p_timezone.*Intl.DateTimeFormat"
    - from: "packages/core/src/supabase/study.ts"
      to: "get_study_cards_for_session RPC"
      via: "POST body with p_timezone"
      pattern: "p_timezone.*timezone"
    - from: "packages/core/src/supabase/study.ts"
      to: "upsert_card_review RPC"
      via: "POST body with p_timezone"
      pattern: "p_timezone.*timezone"
---

<objective>
Add timezone awareness to all SRS RPCs, include never-reviewed cards in due count for fresh users, and add defense-in-depth CHECK constraints to card_review_schedule.

Purpose: Fix the due counter flipping at the wrong time for non-UTC users (11 PM Rome = tomorrow UTC), ensure fresh users see a non-zero due count encouraging first study, and prevent future corruption of SRS state via DB constraints.

Output: One SQL migration with updated RPCs + CHECK constraints, and updated client code passing timezone strings to all three RPCs.
</objective>

<execution_context>
@/home/toto/.claude/get-shit-done/workflows/execute-plan.md
@/home/toto/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-history-fix-validation/26-RESEARCH.md

@supabase/migrations/20260226000001_card_review_schedule.sql
@supabase/migrations/20260226000002_upsert_card_review.sql
@packages/core/src/supabase/study.ts

<interfaces>
<!-- Key functions the executor must modify in packages/core/src/supabase/study.ts -->

From packages/core/src/supabase/study.ts:
```typescript
// Line 460 — Currently passes { p_user_id: userId } only
export async function getDueCardCount(): Promise<number>

// Line 500 — Currently passes { p_user_id: userId, p_limit: limit } only
export async function getStudyCardsForSession(limit: number = 10): Promise<SRSStudyCard[]>

// Line 544 — Currently passes { p_user_id, p_card_id, p_quality, p_content_hash } only
export async function recordCardReview(cardId: string, quality: number, contentHash: string): Promise<void>
```

From supabase/migrations/20260226000001_card_review_schedule.sql:
```sql
-- get_due_card_count(p_user_id UUID) — uses CURRENT_DATE, only counts scheduled cards
-- get_study_cards_for_session(p_user_id UUID, p_limit INTEGER) — uses CURRENT_DATE
```

From supabase/migrations/20260226000002_upsert_card_review.sql:
```sql
-- upsert_card_review(p_user_id UUID, p_card_id UUID, p_quality INTEGER, p_content_hash TEXT)
-- Uses CURRENT_DATE for next_review_at calculation
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create SQL migration for timezone-aware RPCs, fresh-user due count, and CHECK constraints</name>
  <files>supabase/migrations/20260226000003_timezone_checks_fresh_user.sql</files>
  <action>
Create a new migration file `supabase/migrations/20260226000003_timezone_checks_fresh_user.sql` with three sections:

**SECTION 1: CHECK constraints on card_review_schedule**
- First, run safety UPDATE to fix any violating rows:
  - `UPDATE card_review_schedule SET ease_factor = GREATEST(ease_factor, 1.3) WHERE ease_factor < 1.3;`
  - `UPDATE card_review_schedule SET interval_days = GREATEST(interval_days, 0) WHERE interval_days < 0;`
- Then add constraints:
  - `ALTER TABLE card_review_schedule ADD CONSTRAINT chk_ease_factor_floor CHECK (ease_factor >= 1.3);`
  - `ALTER TABLE card_review_schedule ADD CONSTRAINT chk_interval_floor CHECK (interval_days >= 0);`

**SECTION 2: Replace get_due_card_count with timezone-aware version that includes new cards**
- DROP the existing function: `DROP FUNCTION get_due_card_count(UUID);`
- CREATE new function with signature: `get_due_card_count(p_user_id UUID, p_timezone TEXT DEFAULT 'UTC')`
- Compute today: `v_today := (NOW() AT TIME ZONE p_timezone)::DATE;`
- Wrap the timezone computation in `BEGIN...EXCEPTION WHEN OTHERS THEN v_today := CURRENT_DATE; END;` for invalid timezone fallback to UTC
- Count TWO populations and return their sum:
  1. **due_count**: cards with a schedule row where `(crs.next_review_at AT TIME ZONE p_timezone)::DATE <= v_today` (same JOINs as current: cards active, user_repositories, user match)
  2. **new_count**: cards with active questions but NO schedule row (LEFT JOIN card_review_schedule WHERE crs.id IS NULL). JOIN cards + user_repositories + card_questions. Group by card_id, HAVING COUNT(cq.id) > 0.
- Return `COALESCE(v_due_count, 0) + COALESCE(v_new_count, 0)`
- Use SECURITY DEFINER, plpgsql

**SECTION 3: Replace get_study_cards_for_session with timezone-aware version**
- DROP: `DROP FUNCTION get_study_cards_for_session(UUID, INTEGER);`
- CREATE new function with signature: `get_study_cards_for_session(p_user_id UUID, p_limit INTEGER DEFAULT 10, p_timezone TEXT DEFAULT 'UTC')`
- RETURNS TABLE same columns as current (card_id, repository_id, file_path, title, content, raw_content, tags, difficulty, question_count, is_review, ease_factor, interval_days, repetitions, content_hash)
- Compute today: `v_today := (NOW() AT TIME ZONE p_timezone)::DATE;`
- Wrap timezone computation in `BEGIN...EXCEPTION` fallback to CURRENT_DATE
- Replace all `crs.next_review_at::date <= CURRENT_DATE` with `(crs.next_review_at AT TIME ZONE p_timezone)::DATE <= v_today`
- Keep all other logic identical (stale deletion, overdue UNION ALL new, etc.)
- Use SECURITY DEFINER, plpgsql

**SECTION 4: Replace upsert_card_review with timezone-aware version**
- DROP: `DROP FUNCTION upsert_card_review(UUID, UUID, INTEGER, TEXT);`
- CREATE new function with signature: `upsert_card_review(p_user_id UUID, p_card_id UUID, p_quality INTEGER, p_content_hash TEXT, p_timezone TEXT DEFAULT 'UTC')`
- Compute today: `v_today := (NOW() AT TIME ZONE p_timezone)::DATE;`
- Wrap timezone computation in `BEGIN...EXCEPTION` fallback to CURRENT_DATE
- Replace `CURRENT_DATE + (v_new_interval || ' days')::INTERVAL` with `v_today + (v_new_interval || ' days')::INTERVAL` in the INSERT VALUES
- Keep all other SM-2 logic identical
- Use SECURITY DEFINER, plpgsql

**IMPORTANT:** Use `RAISE EXCEPTION` (not `RAISE NOTICE`) for any critical failures per CLAUDE.md rules. The timezone fallback should silently fall back to UTC (this is a non-critical degradation, not a failure).

Add COMMENT ON FUNCTION for all three replaced functions.
  </action>
  <verify>
    <automated>cd /home/toto/scm-projects/lumio && supabase db reset 2>&1 | tail -5</automated>
  </verify>
  <done>Migration applies cleanly: CHECK constraints exist on card_review_schedule, all three RPCs accept p_timezone parameter with AT TIME ZONE logic, get_due_card_count includes new cards in count</done>
</task>

<task type="auto">
  <name>Task 2: Pass device timezone from client to all three SRS RPCs</name>
  <files>packages/core/src/supabase/study.ts</files>
  <action>
Modify three functions in `packages/core/src/supabase/study.ts` to pass the device timezone:

1. **Add timezone helper at top of SRS section (near line 420):**
```typescript
/** Get the device's IANA timezone string for server-side date calculations */
function getDeviceTimezone(): string {
  try {
    return Intl.DateTimeFormat().resolvedOptions().timeZone || 'UTC';
  } catch {
    return 'UTC';
  }
}
```

2. **getDueCardCount() (line ~460):**
- Add timezone to the POST body:
  - Change `body: JSON.stringify({ p_user_id: userId })` to `body: JSON.stringify({ p_user_id: userId, p_timezone: getDeviceTimezone() })`

3. **getStudyCardsForSession() (line ~500):**
- Add timezone to the POST body:
  - Change `body: JSON.stringify({ p_user_id: userId, p_limit: limit })` to `body: JSON.stringify({ p_user_id: userId, p_limit: limit, p_timezone: getDeviceTimezone() })`

4. **recordCardReview() (line ~544):**
- Add timezone to the POST body:
  - Add `p_timezone: getDeviceTimezone()` to the existing JSON body alongside p_user_id, p_card_id, p_quality, p_content_hash

No signature changes needed for these functions (timezone is an internal detail, not a caller concern).

After changes, run `pnpm build:packages` to verify the TypeScript compiles.
  </action>
  <verify>
    <automated>cd /home/toto/scm-projects/lumio && pnpm build:packages 2>&1 | tail -5</automated>
  </verify>
  <done>All three RPC calls (getDueCardCount, getStudyCardsForSession, recordCardReview) pass p_timezone in the POST body. pnpm build:packages succeeds.</done>
</task>

</tasks>

<verification>
1. `supabase db reset` completes without errors (migration applies cleanly)
2. `pnpm build:packages` passes (TypeScript compiles)
3. CHECK constraints exist: `SELECT conname FROM pg_constraint WHERE conrelid = 'card_review_schedule'::regclass AND contype = 'c';` returns chk_ease_factor_floor and chk_interval_floor
4. get_due_card_count signature includes p_timezone: `SELECT pg_get_function_arguments(oid) FROM pg_proc WHERE proname = 'get_due_card_count';`
</verification>

<success_criteria>
- SQL migration applies cleanly with supabase db reset
- All three RPCs accept and use p_timezone parameter
- get_due_card_count returns due + new card count (fresh users see non-zero)
- CHECK constraints prevent ease_factor < 1.3 and interval_days < 0
- pnpm build:packages passes
- Timezone fallback to UTC on invalid timezone string (no crash)
</success_criteria>

<output>
After completion, create `.planning/phases/26-history-fix-validation/26-01-SUMMARY.md`
</output>
